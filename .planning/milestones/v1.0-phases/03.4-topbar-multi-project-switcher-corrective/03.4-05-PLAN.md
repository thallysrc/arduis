---
phase: 03.4-topbar-multi-project-switcher-corrective
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - tests/smoke/test_project_lifecycle_smoke.py
autonomous: false
requirements: [PROJ-01]
user_setup: []

must_haves:
  truths:
    - "A headless smoke proves remove/exit teardown argv shapes without a display"
    - "A human confirms the 6 success criteria on the real multi-repo roots (Livon-Saude + a second project)"
  artifacts:
    - path: "tests/smoke/test_project_lifecycle_smoke.py"
      provides: "headless broadway smoke: persist-across-restart + remove-no-live silent + exit teardown wiring"
      min_lines: 40
  key_links:
    - from: "smoke harness"
      to: "projects.json round-trip across two window constructions"
      via: "save on add, load on relaunch"
      pattern: "projects.json"
---

<objective>
Acceptance gate for Phase 03.4: a headless-broadway smoke for the persistence + lifecycle wiring that
can run without interaction, plus the live human-verify checklist for the items that genuinely need a
real display, real agents, and a process tree (the manual-only verifications from 03.4-VALIDATION.md).

Purpose: prove the 6 ROADMAP success criteria + the D-08/D-10/D-11 process-tree guarantees on the user's
ACTUAL multi-repo roots before `/gsd-verify-work`. This is the only plan with a checkpoint (autonomous:
false).
Output: a lifecycle smoke harness + a signed-off UAT checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-CONTEXT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-VALIDATION.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-UI-SPEC.md
@.planning/ROADMAP.md

<interfaces>
From src/arduis/projects_store.py: `load_projects(path)`, `save_projects(path, roots, last)`
From src/arduis/window.py: `_init_projects`, `_open_project`, `_switch_project`, `_remove_project`,
  `_on_close_request`, `_build_project_tabs`
Smoke harness pattern: tests/smoke/test_project_switch_smoke.py (Plan 03) + tests/smoke/test_topbar_chips_smoke.py
  (sandbox HOME + XDG_CONFIG_HOME, /tmp git fixtures, broadwayd on a free display, SKIP if absent).
The 6 ROADMAP success criteria for Phase 03.4 (lines 88-93) are the UAT spine.
</interfaces>
</context>

<tasks>

<task type="auto" smoke="true">
  <name>Task 1: Headless lifecycle smoke — persist-across-restart, silent remove, exit-teardown wiring</name>
  <read_first>
    - tests/smoke/test_project_switch_smoke.py (Plan 03 harness — extend/mirror)
    - tests/smoke/test_topbar_chips_smoke.py (sandbox HOME + git fixture helpers)
    - src/arduis/projects_store.py (load/save round-trip)
    - src/arduis/window.py (_init_projects, _open_project, _remove_project, _on_close_request)
  </read_first>
  <action>
    Write `tests/smoke/test_project_lifecycle_smoke.py` (runnable; SKIP exit 0 if gtk4-broadwayd absent).
    Use a sandbox HOME + an isolated XDG_CONFIG_HOME so `projects.json` lands in the sandbox, and /tmp git
    fixtures for two multi-repo roots. Assert WITHOUT manual interaction:
      (a) PERSIST (SC-2): construct the window with cwd = root A → A auto-registers (D-07); call
          `_open_project(rootB)` → projects.json now lists A + B. Destroy the window; construct a FRESH
          window (same sandbox HOME/XDG) → `_init_projects` loads A + B from disk and renders two tabs
          (persisted across "restart").
      (b) MISSING-ROOT SKIP (D-06): write a projects.json listing a non-existent root + a real one; new
          window loads only the real one (no crash, missing one dropped).
      (c) REMOVE-NO-LIVE SILENT (D-10): with two registered projects and no live tasks, `_remove_project(rootB)`
          drops B from the registry and rewrites projects.json to A only — no dialog constructed.
    Keep the live-teardown process-tree checks for the human checklist (broadway can't host real agents/docker
    reliably). Print SMOKE <name> PASS/FAIL lines like the existing harnesses; exit nonzero on any FAIL.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; GDK_BACKEND=broadway python tests/smoke/test_project_lifecycle_smoke.py 2>&amp;1 | tail -8</automated>
  </verify>
  <done>The smoke prints PASS for persist-across-restart, missing-root-skip, and silent-remove (or SMOKE_SKIP if broadwayd absent); exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full suite + all smokes; confirm green before the human checkpoint</name>
  <read_first>
    - .planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-VALIDATION.md (sampling rate / phase gate)
  </read_first>
  <action>
    Run the full unit suite and every smoke harness; capture the results into the SUMMARY. Fix any
    regression introduced across Plans 01-04 before presenting the human checkpoint (a red suite must not
    reach UAT). If the venv is needed, use the project's /tmp venv `--system-site-packages` convention.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -m pytest -q 2>&amp;1 | tail -6</automated>
  </verify>
  <done>`python -m pytest -q` fully green; the three smoke harnesses (switch, lifecycle, and the surviving Phase-6/Phase-5 smokes) print PASS or SMOKE_SKIP, none FAIL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — the 6 success criteria + D-08/D-10/D-11 process-tree checks on the real roots</name>
  <what-built>
    The topbar is now a multi-PROJECT switcher: tabs per remembered project, "+ Abrir projeto" folder
    picker, projects.json persistence, launch auto-register of the cwd project, both-alive switching, a
    global cross-project cap, per-project remove-with-teardown, and all-projects app-exit teardown.
    Member repos moved into the New-task dialog; the old repo chips are gone.
  </what-built>
  <how-to-verify>
    Run the app on the real machine (native, not broadway): `./run.sh` (or the project's launch command)
    from inside a real multi-repo root (e.g. `~/Livon-Saude`).
    1. SC-4 / D-07 (launch auto-register): the topbar shows ONE active tab = the launch project's basename;
       the New button is enabled; the sidebar shows that project's tasks.
    2. SC-5 / D-12 (repos in dialog): click "Nova task" → the per-repo checkboxes list this project's
       member repos (all checked by default), NOT topbar chips. Topbar has NO repo chips.
    3. SC-2 (open + persist): click "+ Abrir projeto", pick a SECOND multi-repo root (e.g. a `KarveLabs`
       clone or any folder with ≥1 git repo) → a second tab appears and becomes active; its tasks show in
       the sidebar. Fully QUIT arduis, relaunch → BOTH projects are still listed in the topbar (persisted).
    4. SC-1 (switch swaps): click each project tab → the sidebar (its tasks) AND the workspace swap; typing
       goes to the swapped-in terminal (focus follows — Pitfall 2).
    5. SC-3 / D-08 (both alive): start an agent (claude) on a task in project A; switch to project B; in a
       separate shell run `pgrep -af claude` (or `ps`) → A's agent process is STILL running. Switch back to
       A → its terminal shows the same live session (no respawn).
    6. D-10 (remove teardown): right-click project B's tab → Remover. With a live task it shows the
       destructive confirm "Remover {nome} e encerrar suas tasks?"; confirm → B's agents/containers die
       (`pgrep`/`docker ps` confirm) but B's worktree/task DIRS still exist on disk (verify with `ls`).
       Removing a project with no live tasks removes it silently.
    7. D-11 (no orphans on exit): with 2 projects open and live agents in each, close the arduis window.
       In a shell: `pgrep -af "claude\|zsh"` scoped to the arduis-spawned pgids shows ZERO orphans, and
       `docker ps` shows no leftover arduis task stacks.
    Report each item PASS or describe the failure.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 pass, or describe any failures (which criterion, what happened).</resume-signal>
  <action>Present the verification steps above to the user, who runs them on the real machine; collect PASS/fail per item. This task is human-gated — the executor only presents and records results.</action>
  <verify>Human confirms all 7 UAT items pass (the 6 ROADMAP success criteria + the D-08/D-10/D-11 process-tree no-orphans / dirs-preserved checks).</verify>
  <done>User types "approved" with all 7 items PASS; any failure is recorded and routed back to the owning plan (03 for switch/open/both-alive, 04 for cap/remove/exit-teardown).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke sandbox → projects.json | smoke writes/reads app-state in an isolated XDG dir (no real config touched) |
| live UAT → real agents/containers | the no-orphans check is the security-relevant teardown gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-15 | (resource leak / no-orphans) | live D-11 exit + D-10 remove | mitigate | the live process-tree UAT (`pgrep`/`docker ps` zero orphans) is the human gate that the automated regression (Plan 04) can only approximate; blocks the phase if orphans survive |
| T-03.4-16 | Tampering | smoke uses isolated XDG_CONFIG_HOME | mitigate | the lifecycle smoke never writes the user's real ~/.config/arduis/projects.json (sandboxed HOME + XDG) |
</threat_model>

<verification>
- `GDK_BACKEND=broadway python tests/smoke/test_project_lifecycle_smoke.py` → PASS (persist/skip/silent-remove) or SMOKE_SKIP
- `python -m pytest -q` → full suite green
- Human checkpoint: all 7 UAT items reported PASS (the 6 success criteria + the D-08/D-10/D-11 process-tree checks)
</verification>

<success_criteria>
- All 6 ROADMAP success criteria verified live on the user's real multi-repo roots
- D-08 both-alive, D-10 remove-teardown (dirs preserved), D-11 no-orphans-on-exit confirmed by process-tree inspection
- Headless lifecycle smoke green; full suite green
- Phase ready for `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-05-SUMMARY.md`
</output>
