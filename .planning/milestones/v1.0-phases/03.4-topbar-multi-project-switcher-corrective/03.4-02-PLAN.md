---
phase: 03.4-topbar-multi-project-switcher-corrective
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/arduis/topbar.py
  - src/arduis/window.py
  - tests/test_topbar.py
  - tests/test_caps.py
  - docs/MOTIVATION.md
autonomous: true
requirements: [PROJ-01]
user_setup: []

must_haves:
  truths:
    - "The topbar no longer renders per-repo chips (ChipState and all chip render methods are gone)"
    - "The New-task dialog defaults every member repo checked (no ChipState seeding) for the active project"
    - "The global active cap counts the UNION of every project's tasks, not just one store"
    - "MOTIVATION.md no longer claims 'Repos no topbar' and states the topbar holds multiple projects"
  artifacts:
    - path: "src/arduis/topbar.py"
      provides: "ChipState removed (file emptied or removed)"
      contains: ""
    - path: "tests/test_caps.py"
      provides: "cap-across-projects (union) test"
      contains: "def test_cap"
  key_links:
    - from: "src/arduis/caps.py at_cap"
      to: "union of all projects' tasks"
      via: "flat list comprehension over registry.all()"
      pattern: "at_cap"
    - from: "src/arduis/window.py New-task dialog"
      to: "active project member_repos"
      via: "all-checked default (no ChipState)"
      pattern: "set_active"
---

<objective>
Delete the wrong-level chip machinery (D-04) cleanly and re-wire the New-task dialog default back to
"all member repos checked" (D-12), then prove the global cap counts the UNION across projects (D-09)
with a Wave-0 test against the existing GTK-free `caps.py`. Repair the stale `docs/MOTIVATION.md`
line that still says "Repos no topbar".

This plan keeps the SINGLETON model in window.py (single `_store`, single `_member_repos`) — it only
removes chips and revises the dialog seed. The registry swap is Plan 03. Splitting it this way keeps
each window.py edit small and leaves `main` launchable after this plan (single-project app works, just
without chips).

Purpose: remove dead/wrong-level code before the structural swap, and lock the cap-union behavior at
the GTK-free level so Plan 04's wiring has a passing target.
Output: chip-free topbar code, all-checked New-task dialog, a cap-union test, repaired docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-CONTEXT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-RESEARCH.md

<interfaces>
<!-- What-to-delete table (RESEARCH) — exact symbols + line anchors. -->

window.py chip symbols to remove (D-04):
```
self._chip_state / _chip_btn_by_repo / _chip_dot_by_repo   (__init__ ~308-310)
_build_chip_bar (~2200), _make_chip (~2253), _make_chip_toggled_cb (~2286),
_on_toggle_chip (~2305), _restyle_chips (~2317), _reflect_active_chips (~2338)
callers of _reflect_active_chips: _swap_workspace (~1763), _show_hibernated_placeholder (~1810),
  and the create path (~2693)
win.toggle_chip action registration in _install_row_actions (~3969-3971)
the import at line 80: from arduis.topbar import ChipState
```

New-task dialog seed today (window.py ~2559-2572) — revert to all-checked:
```python
if self._chip_state is not None:
    default = set(self._chip_state.default_selection())
else:
    default = set(self._member_repos)
# ... check.set_active(repo in default)
# AFTER: default = set(self._member_repos)  (all member repos checked, D-12)
```

caps.py (UNCHANGED — only the argument changes; this plan tests the union):
```python
def active_count(sessions) -> int: ...   # counts Task.state.value == "active"
def at_cap(sessions, cap=ACTIVE_CAP_DEFAULT) -> bool: ...  # active_count >= cap
```

docs/MOTIVATION.md stale line ~143: "Repos no topbar, worktrees na sidebar" (contradicts line ~66
"Topbar = projetos").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ChipState + all chip render/reflect methods; revert New-task dialog to all-checked; update test_topbar.py</name>
  <read_first>
    - src/arduis/window.py lines 78-90 (the ChipState import at 80)
    - src/arduis/window.py lines 300-311 (chip map fields in __init__)
    - src/arduis/window.py lines 1748-1816 (_swap_workspace + _show_hibernated_placeholder — _reflect_active_chips callers)
    - src/arduis/window.py lines 2177-2354 (_build_chip_bar through _reflect_active_chips)
    - src/arduis/window.py lines 2536-2575 (New-task dialog seed)
    - src/arduis/window.py lines 2685-2695 (create-path _reflect_active_chips caller)
    - src/arduis/window.py lines 3944-3972 (_install_row_actions — toggle_chip registration)
    - src/arduis/topbar.py (whole file — ChipState)
    - tests/test_topbar.py (whole file — to remove/replace)
  </read_first>
  <action>
    Remove the wrong-level chip machinery (RESEARCH "What to delete", D-04). Do NOT remove the
    surrounding swap logic — only the chip-reflect calls.
    1. window.py: delete `from arduis.topbar import ChipState` (line 80).
    2. window.py __init__: delete `self._chip_state`, `self._chip_btn_by_repo`, `self._chip_dot_by_repo`
       (and the `_chip_bar` Box build at header.pack_start can stay AS AN EMPTY Box placeholder for now —
       Plan 03 reuses that exact slot for project tabs; leave a `# Plan 03 reuses this slot for project tabs`
       comment, but do NOT call `_build_chip_bar`).
    3. window.py: delete methods `_build_chip_bar`, `_make_chip`, `_make_chip_toggled_cb`, `_on_toggle_chip`,
       `_restyle_chips`, `_reflect_active_chips`. Remove the `self._build_chip_bar()` call at the end of
       `_resolve_project`. Remove the three `self._reflect_active_chips(...)` calls (in `_swap_workspace`,
       `_show_hibernated_placeholder`, and the create path) — leave the rest of those methods intact.
    4. window.py `_install_row_actions`: delete the `toggle_chip` `Gio.SimpleAction` registration block.
    5. window.py New-task dialog: replace the ChipState-seeded `default` with `default = set(self._member_repos)`
       (all member repos checked by default — D-12); remove the `if self._chip_state is not None` branch.
    6. topbar.py: delete the `ChipState` class. Leave the file with only its module docstring updated to
       "Superseded by 03.4 project switcher — ChipState removed (D-04)." (Keeping the file empty-but-present
       avoids touching the package; OR remove it — executor's choice, but if removed, ensure nothing imports it.)
       Either way: NO `ChipState` symbol may survive ANYWHERE under `src/arduis/` (see verify grep).
    7. tests/test_topbar.py: DELETE the file (its subject is gone). Project/registry tests live in
       test_projects_registry.py from Plan 01.
    The `.arduis-chip*` CSS rules in `_build_css` MAY stay (Plan 03 reuses `.arduis-chip-active` for the
    active project tab per UI-SPEC) — do NOT delete them.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; ! grep -rn "ChipState\|_reflect_active_chips\|_build_chip_bar\|_make_chip\|_restyle_chips\|toggle_chip" src/arduis/window.py &amp;&amp; { ! grep -rn 'ChipState' src/arduis/ &amp;&amp; echo CLEAN; } &amp;&amp; ! test -f tests/test_topbar.py &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; echo OK</automated>
  </verify>
  <done>No chip symbol remains in window.py; `grep -rn 'ChipState' src/arduis/` prints CLEAN (no residual ChipState anywhere in the package, not just window.py); ChipState class gone from topbar.py; test_topbar.py removed; window.py parses; the New-task dialog default is `set(self._member_repos)`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the cap-across-projects (union) test to test_caps.py (D-09)</name>
  <read_first>
    - tests/test_caps.py (whole file — existing `_task` helper + cap tests)
    - src/arduis/caps.py (active_count / at_cap — unchanged; only the fed list grows)
    - src/arduis/project.py (Project — each owns a store; the union is across registry.all())
  </read_first>
  <behavior>
    test_cap_counts_union_across_projects (D-09): build two `Project`s (Plan 01), each with its own
    store; add 4 ACTIVE tasks to project A and 3 ACTIVE tasks to project B. The flat union
    `[t for p in [A, B] for t in p.store.all()]` has `active_count == 7`; with `ACTIVE_CAP_DEFAULT == 6`,
    `at_cap(union)` is True even though neither single store (`A.store.all()` = 4, `B.store.all()` = 3)
    is at the cap alone. Pin: feeding ONE store would NOT trip the cap (regression guard for Pitfall 4).
    test_cap_union_hibernated_excluded: a HIBERNATED task in either project's store is not counted.
  </behavior>
  <action>
    Extend tests/test_caps.py. Reuse its existing `_task(task_id, state, repo_names)` helper. Build
    `Project` instances from `arduis.project`, add tasks to `p.store`, and assert over the union list.
    This is a GTK-free unit test (no gi). It locks the behavior Plan 04 will wire at the call sites.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -m pytest tests/test_caps.py -q 2>&amp;1 | tail -4</automated>
  </verify>
  <done>`pytest tests/test_caps.py -q` green including the new union + hibernated-excluded tests.</done>
</task>

<task type="auto">
  <name>Task 3: Repair docs/MOTIVATION.md stale "Repos no topbar" line</name>
  <read_first>
    - docs/MOTIVATION.md lines 60-90 (the 3-level model section, "Topbar = projetos")
    - docs/MOTIVATION.md lines 138-150 (the stale ~143 "Repos no topbar, worktrees na sidebar" line)
  </read_first>
  <action>
    Replace the stale line ~143 ("Repos no topbar, worktrees na sidebar") so it agrees with the approved
    model: the topbar holds MULTIPLE switchable PROJECTS (each a multi-repo root), the sidebar holds the
    selected project's tasks, the workspace holds the selected task's terminals. Member repos surface in
    the "Nova task" dialog, not the topbar. Keep the edit minimal and in the document's existing voice
    (pt-BR). Do not restructure other sections.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; ! grep -n "Repos no topbar" docs/MOTIVATION.md &amp;&amp; grep -niq "topbar" docs/MOTIVATION.md &amp;&amp; echo OK</automated>
  </verify>
  <done>The "Repos no topbar" phrasing is gone; the topbar is described as a multi-project switcher consistent with line ~66.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → New-task dialog | member-repo names (filesystem-derived) become CheckButton labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-05 | Tampering | New-task dialog repo labels | mitigate | repo names are set on `Gtk.CheckButton(label=...)` (plain text widget, not markup) — no `set_markup`, preserving the T-03-09 label-injection guard (RESEARCH V5) |
| T-03.4-06 | (resource leak) | global cap underflow | mitigate | cap-union test (D-09) locks that the cap counts all projects so agents can't silently exceed the machine-global limit (Pitfall 4) |
</threat_model>

<verification>
- `! grep -rn "ChipState\|_reflect_active_chips\|_build_chip_bar" src/arduis/window.py` → clean
- `! grep -rn 'ChipState' src/arduis/ && echo CLEAN` → CLEAN (no residual ChipState package-wide)
- `! test -f tests/test_topbar.py` → removed
- `python -m pytest tests/test_caps.py -q` → green (incl. union test)
- `python -m pytest -q` → full suite green (chip deletion didn't break collection/import)
- `! grep "Repos no topbar" docs/MOTIVATION.md` → repaired
- App still launches single-project (chips simply absent)
</verification>

<success_criteria>
- D-04: ChipState + all chip render/reflect methods deleted; no dangling references anywhere in src/arduis/
- D-12: New-task dialog defaults all member repos checked (no ChipState)
- D-09: cap-union behavior locked by a passing GTK-free test
- MOTIVATION.md repaired
- `main` launchable (single-project, chip-free)
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-02-SUMMARY.md`
</output>
</content>
</invoke>
