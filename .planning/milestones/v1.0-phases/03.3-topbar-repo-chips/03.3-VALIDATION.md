# Phase 03.3 — Validation Strategy (Topbar Repo Chips)

**Mode:** nyquist_validation ON. Every observable requirement maps to an automated
test; the GUI-only pieces (chip render, dialog click) are covered by a runnable
headless-broadway smoke (mechanism) plus a live human-verify checklist.

This is a CORRECTIVE phase: it fixes Phase 03.2 D-06 (topbar = name only) and the
03.2 `detect_member_repos` filter. There is no new ROADMAP requirement ID — the
phase is tagged **PAR-02** (the level-1/topbar requirement it touches). The
authoritative spec is the 6 LOCKED decisions D-01..D-06 in `03.3-CONTEXT.md`.

---

## Requirement / Decision → Test Map

| Decision | What must be TRUE | Test (automated) | Plan |
|----------|-------------------|------------------|------|
| D-01 | One chip per member repo, status bolinha reusing sidebar dot CSS, project name visible | `test_topbar_chips_smoke.py` fixture (a): chip count == members, dots use `arduis-dot-*`, WindowTitle still set | 02, 03 |
| D-02 | Chips toggleable; toggled-ON set seeds the New-task dialog (override preserved) | `test_topbar.py` (toggle + `default_selection`); smoke (a): toggle off → `default_selection` excludes it | 01, 02, 03 |
| D-03 | Active task's repos reflected/highlighted without changing the default | `test_topbar.py` (`reflect_active` non-destructive); smoke (a): `is_active` flips, `default_selection` unchanged | 01, 02, 03 |
| D-04 | `detect_member_repos` counts ONLY `.git`-DIRECTORY subdirs; `.git`-FILE worktrees excluded; degenerate preserved | `test_project.py` `test_livon_saude_shape_excludes_worktrees` + updated cases; smoke (b) | 01, 03 |
| D-05 | Overflow → show fitting chips + `+N` menu; no horizontal scroll | `_MAX_VISIBLE_CHIPS` + `+N` `Gtk.MenuButton` (code); smoke (b) confirms no overflow at 2, live checklist for >6 | 02, 03 |
| D-06 | Chip LOGIC is GTK-free (topbar.py); window.py only renders/wires | `test_topbar.py` GTK-free assertion; `grep -L "import gi" src/arduis/topbar.py` | 01 |
| criterion 5 | Degenerate 1-repo renders one chip via the identical path | `test_topbar.py` 1-member parity; smoke (c) | 01, 03 |

---

## Wave-0 Test Gaps (must be created before/with implementation)

These tests do NOT exist yet — each plan creates them as part of its TDD/smoke work
(no Wave-0-only scaffolding task is needed; the gaps are the first deliverable of
their owning plan):

| Gap | Created by | File |
|-----|-----------|------|
| ChipState unit tests | Plan 01 Task 2 | `tests/test_topbar.py` (NEW) |
| D-04 filter tests (Livon-Saude shape, `.git`-FILE exclusion) | Plan 01 Task 1 | `tests/test_project.py` (UPDATE — 2 assertions flipped + 1 new case) |
| Chip-bar headless smoke | Plan 03 Task 1 | `tests/smoke/test_topbar_chips_smoke.py` (NEW) |

The window.py render (Plan 02) has NO dedicated unit test — GTK render is asserted
only through the broadway smoke (Plan 03) and the live checklist. This is consistent
with the established pattern (window.py changes in 03.1/03.2/04/05/06 were all proven
by broadway smoke + UAT, not unit tests).

---

## Host-only / Live-only Items (cannot be fully automated)

| Item | Why live-only | Covered by |
|------|---------------|-----------|
| Chip visible + clickable in a real window | Pointer click + visual render not reliably drivable headlessly | Plan 03 live checklist steps 2-3 |
| New-task dialog (Adw.AlertDialog) render + checkbox state | AlertDialog render+click is live-only (same as 05/06 smokes) | smoke emulates via `default_selection()`; live checklist step 4 |
| The real `~/Projects/Livon-Saude` 20-worktree exclusion | Confirms the fix on the PO's ACTUAL layout (the original bug report) | Plan 03 live checklist step 2 |
| `+N` overflow menu interaction at >6 repos | Needs a real many-repo project | Plan 03 live checklist step 2 |

---

## Executor Environment

- pytest: `/tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q` (321 passing baseline; this phase ADDS `tests/test_topbar.py` and a smoke, and UPDATES `tests/test_project.py`).
- headless GTK: `gtk4-broadwayd :97 & GDK_BACKEND=broadway BROADWAY_DISPLAY=:97 …` — do NOT override `XDG_RUNTIME_DIR`. Sandbox HOME for config; never touch the real `~/.config/arduis`.
- The smoke SMOKE_SKIPs (exit 0) if `gtk4-broadwayd` is absent.

---

## Coverage Confirmation

- Every D-01..D-06 + criterion 5 has at least one automated test (unit or smoke).
- D-04 (the load-bearing fix) is double-covered: a unit test on `detect_member_repos`
  AND a smoke assertion against the full window resolution path.
- No decision is delivered as a "v1 / placeholder / static" reduction — the status
  bolinha reuses the existing dot classes (full D-01), richer per-repo container/agent
  status is a SEPARATE already-scoped phase (Phase 4 / Phase 7), not a simplification.
