---
status: partial
phase: 03.3-topbar-repo-chips
source: [03.3-03-PLAN.md]
started: 2026-06-14T10:46:39-03:00
updated: 2026-06-14T10:46:39-03:00
---

## Current Test

[awaiting human testing on a real display]

## Tests

### 1. Chips visíveis e toggleáveis no topbar (D-01/D-02)
expected: Lançar num root multi-repo limpo → o topbar mostra um chip por repo (com bolinha de status), todos ligados. Clicar num chip desliga/liga; o conjunto ligado é o default da "Nova task".
result: passed headless (confirmação ao vivo pendente) — smoke provou 3 chips renderizados, todos selecionados por padrão, toggle atualiza `default_selection`. Ao vivo: confirmar o visual e o clique.

### 2. [O GATE] Livon-Saude mostra 2 repos, não 22 (D-04)
expected: `cd ~/Projects/Livon-Saude && ~/Projects/arduis/run.sh` → o topbar mostra só os repos reais (`backend`, `frontend`, … `.git`-DIR), NÃO as ~20 worktrees `backend-*`/`frontend-*` (que têm `.git`-arquivo).
result: passed headless (confirmação ao vivo pendente) — smoke com fixture de 2 repos + 20 worktrees provou exatamente 2 chips, sem overflow; `detect_member_repos` retorna os 2. Ao vivo: confirmar no seu Livon-Saude real.

### 3. Nova task herda os chips ligados (D-02)
expected: Desligar um chip no topbar → abrir "Nova task" → o repo desligado vem desmarcado por padrão (ainda dá pra marcar manualmente — override por task preservado).
result: [pending — ao vivo] — o seeding do diálogo a partir de `ChipState.selected` está no código; confirmar o fluxo no display.

### 4. Chips refletem a task ativa (D-03)
expected: Selecionar uma task na sidebar → os chips dos repos daquela task ficam destacados; voltar ao "main" pinado limpa o destaque.
result: passed headless (confirmação ao vivo pendente) — `reflect_active`/clear provados; confirmar o destaque visual ao trocar de task.

### 5. Overflow +N (D-05) e caso degenerado (criterion 5)
expected: Num projeto com >6 repos, chips até caber + menu "+N". Num repo único, 1 chip, sem erro.
result: passed headless para o caso degenerado (1 chip); o +N overflow tem o cap (_MAX_VISIBLE_CHIPS=6) no código — confirmar ao vivo num meta-repo com muitos repos se você tiver um.

### 6. Espaço reservado para a Fase 7 (containers)
expected: O lado direito do header (pack_end/título) fica livre — os badges de container (`db :5433`, `compose shared`) do 07-04 vão entrar ali sem retrabalho.
result: [pending — depende do 07-04] — o chip bar foi packado no pack_start; confirmar a coexistência quando a Fase 7 fechar.

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

(Note: itens 1/2/4/5 são headless-provados e aguardam só confirmação visual; 3 e 6 são ao-vivo/dependentes. O gate é o item 2 — sua reclamação original.)

## Gaps
