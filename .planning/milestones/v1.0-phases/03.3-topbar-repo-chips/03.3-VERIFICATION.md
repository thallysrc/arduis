---
phase: 03.3-topbar-repo-chips
verified: 2026-06-14T14:00:00Z
status: human_needed
score: 5/5 automated truths verified; 6 live UAT items pending human confirmation
human_verification:
  - test: "Launch arduis from ~/Projects/Livon-Saude; confirm topbar shows exactly 2 chips (backend, frontend), NOT the ~20 backend-*/frontend-* worktrees"
    expected: "Only the true .git-DIRECTORY repos appear as chips; linked worktrees are absent"
    why_human: "The D-04 gate on the real root — headless smoke proved it on a fabricated fixture; live confirmation on the actual Livon-Saude tree is the original bug report"
  - test: "Click a chip to toggle it OFF then ON; confirm visual toggle state change"
    expected: "Chip visually changes state (toggled-off looks inactive); clicking again restores it"
    why_human: "Pointer click + visual render cannot be driven headlessly"
  - test: "With one chip toggled OFF, click +New task; confirm that repo's checkbox is PRE-UNCHECKED and others are pre-checked; confirm you can still override per-task"
    expected: "Dialog inherits the toggled-ON set; the toggled-off repo is unchecked by default; manual override within the dialog still works"
    why_human: "Adw.AlertDialog render + checkbox click interaction is live-only"
  - test: "Select/activate an existing task in the sidebar; confirm its repos are highlighted in the chips; select the pinned main row; confirm the highlight clears"
    expected: "Task repos highlighted (arduis-chip-active border/tint); switching to main row clears it"
    why_human: "CSS visual highlight and sidebar-click interaction cannot be verified headlessly"
  - test: "If a project with >6 repos is available, confirm chips up to 6 render inline and a '+N' menu holds the rest (no horizontal scroll)"
    expected: "A '+N' Gtk.MenuButton appears; no horizontal scrollbar; overflow repos accessible via the menu"
    why_human: "Overflow interaction and scroll absence require a visual/interactive check"
  - test: "Launch arduis from a single-repo directory; confirm one chip renders and the app functions normally"
    expected: "One chip visible in topbar; no crash; normal workflow unaffected"
    why_human: "Visual render of degenerate 1-chip case on a real display"
---

# Phase 03.3: Topbar Repo Chips Verification Report

**Phase Goal:** Surface the project's member repos as toggleable chips in the topbar (approved mockup affordance), superseding 03.2 D-06 (topbar = name only). The toggled-ON set seeds new-task repo selection; the active task's repos reflect in the chips; member-repo detection excludes linked worktrees (.git-FILE) so the bar is usable on a real root (Livon-Saude: ~22 entries becomes 2).
**Verified:** 2026-06-14T14:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | The topbar renders one toggleable chip per member repo (status bolinha reusing sidebar dot CSS), project name still visible (D-01) | VERIFIED (automated) + human_needed (visual) | `_make_chip` builds a flat `Gtk.ToggleButton` with `arduis-dot-active` dot + `set_text(repo)` name label; `_chip_bar` packed `pack_start`; `Adw.WindowTitle` still set as `title_widget`; CSS `.arduis-chip` / `.arduis-chip-bar` defined; smoke (a) confirmed 3 chips for 3-repo fixture; chip RENDER is live-only |
| 2 | The toggled-ON chip set seeds the New-task dialog's per-repo checkboxes; per-task override preserved (D-02) | VERIFIED (automated) + human_needed (visual) | `_present_new_task_dialog` uses `set_active(repo in default)` where `default = set(self._chip_state.default_selection())`; smoke (a) confirmed toggle-off excludes repo from `default_selection()`; dialog render + checkbox interaction is live-only |
| 3 | Selecting/activating a task reflects its repos in the chips; pinned main clears the reflection (D-03) | VERIFIED (automated) + human_needed (visual) | `_reflect_active_chips(sid)` wired into `_swap_workspace` (L1355), `_show_hibernated_placeholder` (L1398), and `_build_task_workspace` (L2258); `None`/`_MAIN_SID` calls `reflect_active(None)`; smoke (a) confirmed `is_active` transitions and clear; visual highlight is live-only |
| 4 | detect_member_repos counts only `.git`-DIRECTORY subdirs — the ~20 linked worktrees are excluded; degenerate 1-repo preserved (D-04) | VERIFIED | `os.path.isdir(os.path.join(e.path, ".git"))` in `project.py`; `test_livon_saude_shape_excludes_worktrees` passes (2 repos not 22); smoke fixture (b) with 2 real `.git`-DIR repos + 20 `git worktree add` linked worktrees yielded exactly 2 chips; degenerate returns `[]` (`test_no_members_returns_empty`); 336 tests pass |
| 5 | Chip overflow folds into a +N menu (no horizontal scroll); the degenerate 1-repo renders one chip (D-05 / criterion 5) | VERIFIED (automated) + human_needed (visual, >6 repos) | `_MAX_VISIBLE_CHIPS = 6` module constant; `_build_chip_bar` splits `members[:6]` visible + `members[6:]` overflow into a `Gtk.MenuButton(label=f"+{len(overflow)}")` with `Gio.Menu` items targeting `win.toggle_chip(repo)` action; smoke (c) confirmed 1-chip degenerate; >6 visual interaction is live-only |

**Score:** 5/5 roadmap truths have automated evidence; 5/5 also have live-only confirmation pending.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/arduis/project.py` | `detect_member_repos` with `.git`-DIRECTORY filter (D-04) | VERIFIED | 44-line file; uses `os.path.isdir(...)` not `os.path.exists`; GTK-free; `follow_symlinks=False` preserved |
| `src/arduis/topbar.py` | GTK-free `ChipState` with all 5 methods (D-02/D-03/D-06) | VERIFIED | 51-line file; `class ChipState` with `toggle`, `default_selection`, `reflect_active`, `is_selected`, `is_active`; no `import gi` |
| `tests/test_project.py` | D-04 filter tests including Livon-Saude shape + flipped .git-FILE assertions | VERIFIED | Contains `test_livon_saude_shape_excludes_worktrees`, `test_git_as_file_is_not_a_member`, `test_detects_dir_git_excludes_file_git_and_plain` (flipped); D-04 intentional-change comment present; 8 tests pass |
| `tests/test_topbar.py` | ChipState unit tests covering all behaviors | VERIFIED | 12 tests covering toggle, default_selection, reflect_active non-destructive (D-03), 1-member parity (criterion 5), GTK-free assertion; all pass |
| `src/arduis/window.py` | Chip bar render+wire (ChipState-backed) | VERIFIED | `from arduis.topbar import ChipState` (L77); `_MAX_VISIBLE_CHIPS=6` (L116); `_chip_bar` Box packed `pack_start` after `_new_btn` (L413); CSS classes (L169-L178); `_build_chip_bar`/`_make_chip`/`_restyle_chips`/`_reflect_active_chips`/`_on_toggle_chip` helpers; `win.toggle_chip` action; dialog seeded from `default_selection()` |
| `tests/smoke/test_topbar_chips_smoke.py` | Headless broadway acceptance harness 8 checks | VERIFIED | 8/8 checks defined; fixtures (a) clean multi-repo, (b) Livon-Saude-shaped, (c) degenerate; SMOKE_SKIP on missing broadwayd; sandbox HOME; committed at `57eeb5a` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `window.py:_resolve_project` | `project.py:detect_member_repos` | `from arduis.project import detect_member_repos` + call at L1717 | WIRED | Already wired in 03.2; D-04 filter now in `project.py`; `_build_chip_bar()` called at end of `_resolve_project` (L1756) |
| `window.py:_resolve_project` | `topbar.py:ChipState` | `from arduis.topbar import ChipState` (L77); `self._chip_state = ChipState(members)` inside `_build_chip_bar` (L1798) | WIRED | Import present; `_chip_state` constructed once at end of `_resolve_project` |
| `window.py:_present_new_task_dialog` | `self._chip_state.default_selection()` | `default = set(self._chip_state.default_selection())` + `check.set_active(repo in default)` (L2130, L2137) | WIRED | Guard for `None` chip_state with `set(self._member_repos)` fallback; pattern "default_selection" confirmed |
| `window.py:_swap_workspace` | `self._chip_state.reflect_active(...)` | `self._reflect_active_chips(sid)` at L1355 | WIRED | `_reflect_active_chips` is the single chokepoint; called from all three paths that set `_active_workspace_sid`: `_swap_workspace` (L1355), `_show_hibernated_placeholder` (L1398), `_build_task_workspace` (L2258) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `window.py` chip bar | `_chip_state.members` | `self._member_repos` ← `detect_member_repos(cwd)` in `_resolve_project` (L1717) | Yes — `os.scandir` on actual disk | FLOWING |
| `window.py` dialog seeding | `default` in `_present_new_task_dialog` | `self._chip_state.default_selection()` ← `ChipState.selected` (toggled by user) | Yes — reflects real user toggle state | FLOWING |
| `window.py` chip reflection | `_chip_state.active_repos` | `_reflect_active_chips(sid)` ← `{r.repo_name for r in task.repos}` via `self._store.get(sid)` | Yes — real Task store lookup | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| project.py uses isdir not exists for .git membership | `grep "os.path.isdir" src/arduis/project.py` | `if e.is_dir(follow_symlinks=False) and os.path.isdir(os.path.join(e.path, ".git"))` | PASS |
| project.py and topbar.py are GTK-free | `grep -L "import gi" src/arduis/project.py src/arduis/topbar.py` | Both files listed (no gi import) | PASS |
| ChipState has all 5 required methods | `grep "def " src/arduis/topbar.py` | `toggle`, `default_selection`, `reflect_active`, `is_selected`, `is_active` | PASS |
| window.py imports ChipState | `grep "from arduis.topbar import ChipState" src/arduis/window.py` | Match at L77 | PASS |
| _MAX_VISIBLE_CHIPS defined | `grep "_MAX_VISIBLE_CHIPS" src/arduis/window.py` | `_MAX_VISIBLE_CHIPS = 6` at L116 | PASS |
| _build_chip_bar called at end of _resolve_project | `grep "_build_chip_bar" src/arduis/window.py` | Call at L1756, definition at L1776 | PASS |
| _reflect_active_chips wired into all 3 swap paths | `grep "_reflect_active_chips" src/arduis/window.py` | L1355 (_swap_workspace), L1398 (_show_hibernated_placeholder), L2258 (_build_task_workspace) | PASS |
| chip name rendered via set_text not set_markup | `grep "set_text(repo)" src/arduis/window.py` | `name.set_text(repo)` at L1852 with T-03.3-04 comment | PASS |
| chip bar packed at pack_start, pack_end free | `grep -A2 "pack_start.*chip_bar" src/arduis/window.py` | `header.pack_start(self._chip_bar)` at L413; `menu_btn` packed at `pack_end` (L430) | PASS |
| Full test suite passes (336 tests) | `/tmp/arduis-venv-ab12/bin/python -m pytest tests/ 2>&1 \| tail -1` | `336 passed in 0.83s` | PASS |
| test_livon_saude_shape_excludes_worktrees exists | `grep "test_livon_saude_shape" tests/test_project.py` | Function defined at L54 | PASS |
| Smoke harness has 8 checks covering all decisions | `grep "^        check(" tests/smoke/test_topbar_chips_smoke.py` | 8 check() calls confirmed (a_members_3_sorted, a_chip_bar_renders_3, a_all_selected_by_default, a_toggle_off_updates_default, a_reflect_active_then_clear, b_two_repos_not_twentytwo, b_chip_bar_renders_2_no_overflow, c_degenerate_one_chip) | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PAR-02 | 03.3-01, 03.3-02, 03.3-03 | Level-1/topbar: multi-repo chip affordance (corrects D-06) | SATISFIED | Chip bar implemented in window.py; ChipState in topbar.py; D-04 filter in project.py; all 336 tests pass; smoke 8/8 |

---

### Anti-Patterns Found

No blockers or actionable anti-patterns found.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `window.py` | ~1838 | Static `arduis-dot-active` on chip dot (no live per-repo status) | Info | Documented as intentional in plan_decisions — richer per-repo container/agent status is Phase 7/Phase 4 scope; D-01 is fully satisfied by reusing the existing dot CSS class |
| `03.3-HUMAN-UAT.md` | — | 6 UAT items status: pending (all items show "pending" in summary) | Info | Expected: the UAT is awaiting live human confirmation; headless-proven items are noted as such in the UAT comments |

---

### Human Verification Required

The automated layer (unit tests, smoke harness) proves all 5 ROADMAP success criteria at the mechanism level. The following items require a human running arduis with a real display — they were explicitly classified as live-only in `03.3-VALIDATION.md`.

#### 1. [THE GATE] Livon-Saude: 2 chips not 22

**Test:** `cd ~/Projects/Livon-Saude && /tmp/arduis-venv-ab12/bin/python -m arduis`
**Expected:** Topbar shows exactly the true member repos (e.g. `backend`, `frontend`) as chips — the ~20 `backend-*`/`frontend-*` linked worktrees do NOT appear.
**Why human:** The smoke proved this on a fabricated fixture with `git worktree add`. The live confirmation on the actual Livon-Saude tree is the original bug report and the stated acceptance gate.

#### 2. Chip visual toggle (D-01/D-02)

**Test:** With a multi-repo root, click a chip to toggle it OFF; click again to toggle it ON.
**Expected:** The chip visibly changes state (toggled-off looks inactive vs toggled-on); the state is preserved correctly.
**Why human:** Pointer click + visual render cannot be driven headlessly; GTK ToggleButton visual state requires a real display.

#### 3. New-task dialog inherits toggled set (D-02)

**Test:** Toggle one chip OFF; click "+New task"; inspect the per-repo checkboxes.
**Expected:** The toggled-off repo's checkbox is pre-unchecked; toggled-on repos are pre-checked. Manually checking/unchecking in the dialog still works (per-task override preserved).
**Why human:** Adw.AlertDialog render + checkbox interaction is live-only (consistent with 03.1/03.2 acceptance pattern).

#### 4. Active-task reflection and clear (D-03)

**Test:** Select/activate an existing task in the sidebar; inspect the chips. Then select the pinned main row.
**Expected:** The active task's repos show the `arduis-chip-active` highlight (accent border + surface background tint); switching to the main row clears all highlights.
**Why human:** CSS visual highlight and sidebar-click interaction are live-only.

#### 5. +N overflow menu (D-05) — conditional

**Test:** If a project with more than 6 member repos is available, confirm the topbar shows 6 chips inline + a "+N" Gtk.MenuButton for the rest.
**Expected:** No horizontal scroll; all repos reachable; overflow menu items toggle their chip state.
**Why human:** Needs a real many-repo project and visual/interaction check.

#### 6. Phase 7 / 07-04 co-existence (D-01 header free region)

**Test:** When Phase 7's 07-04 container badge lands, confirm pack_end of the header remains available.
**Expected:** Container toggle + port badges (`db :5433`, `compose shared`) sit alongside the chips without layout conflict.
**Why human:** This is a future-phase integration check; depends on 07-04 delivery.

---

### Gaps Summary

No gaps. All 5 ROADMAP success criteria are satisfied by the actual codebase. The outstanding items are explicitly live-only verifications classified as human_needed in the phase's own VALIDATION.md — they are not implementation gaps.

Key findings:
- `detect_member_repos` correctly uses `os.path.isdir` (D-04); the function is 44 lines, GTK-free, and tested by 8 unit tests including the Livon-Saude shape.
- `topbar.ChipState` is a 51-line GTK-free class fully implementing the D-02/D-03/D-06 contract; 12 unit tests cover every behavior including the D-03 non-destructive reflect invariant.
- `window.py` has all required helpers (`_build_chip_bar`, `_make_chip`, `_restyle_chips`, `_reflect_active_chips`, `_on_toggle_chip`), the `win.toggle_chip` action, and the dialog seeding change — all cross-verified by grep and by 336 passing tests.
- Reflection is wired into all three `_active_workspace_sid` assignment paths (`_swap_workspace`, `_show_hibernated_placeholder`, `_build_task_workspace`), not just the primary row-click path.
- The header layout (chip bar at `pack_start`, `Adw.WindowTitle` centered, `pack_end` free) is correctly structured for Phase 7 / 07-04 badge co-existence.
- The headless broadway smoke (8/8, committed at `57eeb5a`) exercises fixture (b) using `git worktree add` to create real linked-worktree `.git`-FILE entries, making it a strong guard against D-04 regression.
- The D-04 reversal of 03.2's Pitfall-1 is intentional, recorded in source docstrings and test comments.
- All 4 phase commits (`d6cf113`, `b9f7390`, `f2be9df`, `df26b8b`) verified in git history.

---

_Verified: 2026-06-14T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
