---
phase: 02-core-loop-new-worktree-env-agent
plan: 01
type: tdd
wave: 1
depends_on: [00]
files_modified:
  - src/arduis/worktree.py
  - src/arduis/session.py
  - src/arduis/swarm/__init__.py
autonomous: true
requirements: [WT-01, WT-02, WT-03, RAM-01]
must_haves:
  truths:
    - "All eight Wave-0 unit tests turn GREEN (the pure domain layer satisfies the contract)"
    - "The git-argv builders never emit --force and build argv as lists (no shell strings)"
    - "Branch names with slashes/path-traversal sanitize to a safe sibling dir basename"
    - "Default-branch detection has an origin→local fallback (no hardcoded 'main')"
    - "SessionStore is GTK-free, serializable to JSON, and carries the rss_kb RAM field from day one"
    - "AGENT_FEED is the bytes literal b\"claude\\n\" (not str)"
  artifacts:
    - path: "src/arduis/worktree.py"
      provides: "Pure git-argv builders + parsers (default-branch fallback, add argv, sanitize, porcelain parse, infer new/existing)"
      contains: "def argv_worktree_add_new"
      min_lines: 60
    - path: "src/arduis/session.py"
      provides: "GTK-free serializable SessionStore + WorktreeSession + SessionState + AGENT_FEED + hibernate_fields"
      contains: "AGENT_FEED"
      min_lines: 50
    - path: "src/arduis/swarm/__init__.py"
      provides: "Named empty swarm seam (roadmap requirement — no code in v1)"
  key_links:
    - from: "src/arduis/worktree.py"
      to: "git CLI argv"
      via: "list-form argv routed through HostRunner by the caller"
      pattern: "\\[\"git\", \"-C\""
    - from: "src/arduis/session.py"
      to: "dataclasses.asdict"
      via: "to_dict / to_list serialization"
      pattern: "asdict"
---

<objective>
Implement the pure, GTK-free domain layer that turns the Plan-00 RED tests GREEN:
`worktree.py` (git-argv builders + parsers) and `session.py` (the serializable `SessionStore`
with day-one RAM fields and the `b"claude\n"` feed constant), plus the named-empty `swarm/`
seam the roadmap requires.

This is the brain of the core loop with ZERO GTK: branch inference (D-06), default-branch
fallback (D-04), sibling-dir derivation + sanitization (D-05), `worktree add` argv (D-05/D-07,
never `--force`), porcelain already-checked-out detection (D-07), and the in-memory
serializable session model (D-13) with the hibernate model transition (D-11). Plan 02 wires
this into GTK.

Purpose: Keep the swarm seam free and the logic unit-tested (the established Phase-1 GTK-free
discipline) so the UI layer is thin glue.
Output: worktree.py, session.py, swarm/__init__.py — all eight Wave-0 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-new-worktree-env-agent/02-RESEARCH.md
@.planning/phases/02-core-loop-new-worktree-env-agent/02-CONTEXT.md
@tests/test_worktree.py
@tests/test_session.py
@src/arduis/spawn.py
@src/arduis/exit_status.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GREEN — src/arduis/worktree.py (pure git-argv builders + parsers)</name>
  <files>src/arduis/worktree.py</files>
  <read_first>
    - tests/test_worktree.py (the RED contract this task must satisfy — exact signatures and literals)
    - .planning/phases/02-core-loop-new-worktree-env-agent/02-RESEARCH.md (§ Code Examples — default-branch chain, add argv new/existing, porcelain parse; § Pitfall 2/3/6)
    - src/arduis/spawn.py (the GTK-free module style + the T-01-01/T-01-02 list-argv discipline to continue)
  </read_first>
  <behavior>
    - parse_default_branch("refs/remotes/origin/main\n") -> "main" (strip "refs/remotes/origin/" + whitespace)
    - argv_default_branch_via_origin(repo) -> ["git","-C",repo,"symbolic-ref","refs/remotes/origin/HEAD"]
    - argv_default_branch_local(repo)  -> ["git","-C",repo,"symbolic-ref","--short","HEAD"]
    - argv_worktree_add_new(repo,branch,dir,base) -> ["git","-C",repo,"worktree","add","-b",branch,dir,base]  (NO --force)
    - argv_worktree_add_existing(repo,dir,branch) -> ["git","-C",repo,"worktree","add",dir,branch]  (NO --force)
    - sanitize_branch_for_dir("feature/foo") -> "feature-foo"; strips/blocks path-traversal so the basename never contains ".." or os.sep
    - worktree_dir_for(repo_root,branch) -> sibling: dirname(repo_root)/(basename(repo_root)+"-"+sanitize(branch))
    - parse_local_branches("  master\n* feat\n") -> ["master","feat"] (strip "* " current-marker + whitespace)
    - infer_new_vs_existing(branch, existing) -> "existing" if in list else "new"
    - parse_worktrees(porcelain) -> list of {"path","branch"(or None for detached),"locked"?}
    - branch_checked_out_path(branch, parsed) -> path string if branch present else None
    - argv_list_local_branches(repo) and argv_worktree_list_porcelain(repo) return the list-form argv the tests assert
  </behavior>
  <action>
    Create src/arduis/worktree.py — pure stdlib only (import re, os; NO `gi`, NO GTK). Module docstring cites D-04/D-05/D-06/D-07 and threat T-02-01/T-02-02 (list-form argv, sanitized sibling dir).

    Implement each function to the literals in tests/test_worktree.py. Key concrete values:
    - default-branch via origin argv: `["git", "-C", repo, "symbolic-ref", "refs/remotes/origin/HEAD"]`; local fallback: `["git", "-C", repo, "symbolic-ref", "--short", "HEAD"]`. parse_default_branch strips the `refs/remotes/origin/` prefix and `.strip()`.
    - add-new argv: `["git", "-C", repo, "worktree", "add", "-b", branch, worktree_dir, base]`; add-existing: `["git", "-C", repo, "worktree", "add", worktree_dir, branch]`. Assert in code review that `"--force"` appears NOWHERE (D-07).
    - sanitize_branch_for_dir: replace os.sep ("/") and any char not in `[A-Za-z0-9._-]` with "-"; collapse runs of "-"; explicitly strip leading/trailing "." and any "" path piece so the result can NEVER be "..", "." or contain a separator (T-02-02 path-traversal guard).
    - worktree_dir_for: `os.path.join(os.path.dirname(repo_root.rstrip("/")), os.path.basename(repo_root.rstrip("/")) + "-" + sanitize_branch_for_dir(branch))`.
    - parse_local_branches: for each line, `line.replace("*","",1).strip()` then skip empties; (or parse `%(refname:short)` output one-per-line). argv_list_local_branches(repo) -> `["git","-C",repo,"for-each-ref","--format=%(refname:short)","refs/heads"]`.
    - parse_worktrees + branch_checked_out_path + argv_worktree_list_porcelain: copy the verified parser from 02-RESEARCH.md § "Porcelain pre-check parse" verbatim (record split on blank lines; `worktree`/`branch refs/heads/<name>`/`detached`/`locked` keys; branch with `.removeprefix("refs/heads/")`). argv_worktree_list_porcelain(repo) -> `["git","-C",repo,"worktree","list","--porcelain"]`. branch_checked_out_path returns the matching record's "path" or None.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worktree.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/arduis/worktree.py exists and imports no `gi` (grep -L "import gi" passes)
    - src/arduis/worktree.py contains `def argv_worktree_add_new`
    - src/arduis/worktree.py contains `def sanitize_branch_for_dir`
    - src/arduis/worktree.py contains `def branch_checked_out_path`
    - `"--force"` does NOT appear in src/arduis/worktree.py
    - `.venv/bin/python -m pytest tests/test_worktree.py -q` exits 0 (all five tests GREEN)
  </acceptance_criteria>
  <done>worktree.py turns all five test_worktree.py tests GREEN; argv are list literals, no --force, sanitization blocks path traversal, default-branch has the origin→local fallback.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — src/arduis/session.py + swarm/ seam (serializable store, bytes feed, hibernate model)</name>
  <files>src/arduis/session.py, src/arduis/swarm/__init__.py</files>
  <read_first>
    - tests/test_session.py (the RED contract — AGENT_FEED bytes, store CRUD/serialize, hibernate_fields transition)
    - .planning/phases/02-core-loop-new-worktree-env-agent/02-RESEARCH.md (§ Pattern 1 SessionStore example — copy the dataclass shape; § Pitfall 1 bytes feed; § Hibernate/Resume)
    - .planning/ROADMAP.md (§ Cross-Cutting: Swarm Seams — "leave swarm/ a named empty directory")
  </read_first>
  <behavior>
    - AGENT_FEED is `b"claude\n"` (bytes; D-08/Pitfall 1)
    - SessionState is a str-Enum with ACTIVE="active", HIBERNATED="hibernated" (serializes cleanly)
    - WorktreeSession dataclass: session_id, branch, worktree_dir, repo_root, state=ACTIVE, pid=None, pgid=None, rss_kb=None (RAM field day-one, D-13); to_dict() via dataclasses.asdict
    - SessionStore: add/get/by_branch/all/to_list; to_list() is JSON-serializable (str-Enum dumps as its value)
    - hibernate_fields(session): set state=HIBERNATED, pid=None, pgid=None, leave worktree_dir/repo_root/rss_kb untouched (D-11 — kill agent, KEEP directory; the actual os.killpg lives in Plan 02 window.py)
  </behavior>
  <action>
    Create src/arduis/session.py — pure stdlib (from __future__ import annotations; from dataclasses import dataclass, field, asdict; from enum import Enum). NO `gi`. Module docstring cites D-08/D-11/D-13.

    Copy the SessionStore/WorktreeSession/SessionState shape from 02-RESEARCH.md § Pattern 1 verbatim. Add at module top: `AGENT_FEED: bytes = b"claude\n"  # D-08 — bytes, not str (0.76 feed_child TypeError on str)`. Implement to_dict as `asdict(self)` and to_list as `[s.to_dict() for s in self._sessions.values()]`. Add the free function:
    ```python
    def hibernate_fields(session: WorktreeSession) -> None:
        session.state = SessionState.HIBERNATED
        session.pid = None
        session.pgid = None
        # worktree_dir kept on disk (D-11); rss_kb left as-is
    ```
    Also create src/arduis/swarm/__init__.py with only a module docstring: `"""Swarm seam — intentionally empty in v1 (roadmap swarm-seam note). No code until v1 is dogfooded."""` (the named-empty directory the roadmap requires).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_session.py -q</automated>
  </verify>
  <acceptance_criteria>
    - src/arduis/session.py exists and imports no `gi`
    - src/arduis/session.py contains `AGENT_FEED` and the literal `b"claude\n"`
    - src/arduis/session.py contains `rss_kb` (RAM field present day-one)
    - src/arduis/session.py contains `def hibernate_fields`
    - src/arduis/session.py contains `asdict`
    - src/arduis/swarm/__init__.py exists
    - `.venv/bin/python -m pytest tests/test_session.py -q` exits 0 (all three tests GREEN)
  </acceptance_criteria>
  <done>session.py turns all three test_session.py tests GREEN: bytes feed constant, JSON-serializable store with the rss_kb RAM field, hibernate model transition that keeps the directory; swarm/ exists as a named-empty seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user branch name → git argv | An untrusted branch name (from the Phase-3/Plan-02 dialog) flows into `git worktree add` argv and into a filesystem sibling path. This module is where it is made safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / EoP | worktree.py argv builders | mitigate | All argv are Python list literals (`["git","-C",repo,...]`), branch passed as a discrete element — never joined into a shell string. The caller (Plan 02) routes them through HostRunner; no `shell=True` anywhere. Continues Phase-1 spawn.py discipline. Verified by test_add_argv. |
| T-02-02 | Tampering (path traversal) | worktree.py sanitize_branch_for_dir / worktree_dir_for | mitigate | Branch name is sanitized to `[A-Za-z0-9._-]` with `/` and traversal pieces (`..`, leading/trailing `.`) stripped before becoming a sibling dir basename; the dir is computed from repo_root's parent, never from raw input. Verified by test_sanitize_dir. |
| T-02-03 | Tampering | git worktree add --force | mitigate | `--force` is never emitted (D-07); already-checked-out branches are detected via porcelain parse and handled by the caller (focus/abort). Verified by test_add_argv (`--force` absent) + test_detect_checked_out. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` (full suite): the 4 Phase-1 files + the 2 Wave-0 files all GREEN (8 new tests pass; no regression).
- `grep -rL "import gi" src/arduis/worktree.py src/arduis/session.py` confirms the GTK-free-core rule (these modules never import gi).
</verification>

<success_criteria>
- All eight Wave-0 unit tests pass (`tests/test_worktree.py` + `tests/test_session.py`).
- worktree.py and session.py contain no `gi` import (GTK-free domain).
- `--force` appears nowhere in worktree.py.
- swarm/__init__.py exists as a named-empty seam.
- Full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-new-worktree-env-agent/02-01-SUMMARY.md`
</output>
