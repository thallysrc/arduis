---
phase: 08-review-cleanup
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/arduis/window.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02, GIT-01]
user_setup: []

must_haves:
  truths:
    - "A row context-menu 'Ver diff ▸ <repo>' (per repo for a multi-repo task; one entry for 1-repo) opens a NEW read-only VTE leaf in the active workspace running `git --no-pager diff` for that repo's worktree, with terminal.set_input_enabled(False) so keystrokes never reach the child (REVIEW-01, D-02)"
    - "The sidebar row subline shows branch + ahead/behind + PR status (e.g. 'feat/x · ↑3 ↓0 · PR #42 open' / 'feat/x · sem PR' / 'gh ausente' / 'gh não autenticado') rendered by review_cache + gh/review formatters, read via run_git_async with cwd=worktree (GIT-01, REVIEW-02, D-03)"
    - "Status reads are THROTTLED: a 'Atualizar status' row menu item forces a re-read (bypasses TTL); auto-reads fire ONCE on task create and ONCE on row-activation, each gated by the TTL cache (git 30s / gh 120s) + an in-flight debounce (_pr_busy set mirroring _compose_busy) so rapid switching never spams gh (GIT-01, D-03)"
    - "An 'Abrir PR (web)' row menu item runs `gh pr create --web` via run_git_async (cwd=worktree) — the ONE allowed write — and is omitted/disabled when gh is absent (REVIEW-02, D-05/D-06)"
    - "gh absent (gh_available False) → never calls gh, subline shows 'gh ausente'; gh exit-4 → 'gh não autenticado'; parse garbage is caught and the loop never crashes (D-06)"
  artifacts:
    - path: "src/arduis/window.py"
      provides: "_open_diff_leaf, _refresh_task_status (branch+ahead/behind+PR behind TTL+debounce), _on_refresh_status_action, _on_ver_diff_action, _on_open_pr_action wired into _make_row_menu_cb + _install_row_actions; auto-read on create + row-activate"
      contains: "set_input_enabled(False)"
  key_links:
    - from: "src/arduis/window.py _open_diff_leaf"
      to: "review.argv_diff + Vte.Terminal.set_input_enabled(False)"
      via: "spawn a read-only leaf running git --no-pager diff for the repo worktree"
      pattern: "argv_diff|set_input_enabled"
    - from: "src/arduis/window.py _refresh_task_status"
      to: "gh.argv_pr_view / review.argv_current_branch + run_git_async(cwd=worktree)"
      via: "TTL-cache + debounce-gated read, parse, render the subline"
      pattern: "argv_pr_view|argv_current_branch|_pr_busy"
---

<objective>
Wire the READ surfaces of Phase 8 into `window.py`: the read-only diff VTE leaf (REVIEW-01), the
branch + ahead/behind + PR-status subline (GIT-01, REVIEW-02) behind the manual-refresh + TTL-cache
+ in-flight-debounce throttle (D-03), and the 'Abrir PR (web)' menu item that runs the ONE allowed
write `gh pr create --web` (REVIEW-02, D-05). This is the FIRST of two sequential window.py waves —
it owns the diff/status read paths; Wave 3 (the conclude teardown) is the SECOND, so the two never
edit window.py in the same wave.

Purpose: window.py is pure glue here — it calls the Plan-01/02 GTK-free argv builders via the
extended `run_git_async(cwd=...)`, reacts to the parsed result on the GLib loop, and reuses the
EXISTING sidebar-subline (`_subline_by_sid`), row-context-menu (`_make_row_menu_cb`), action
registration (`_install_row_actions`), busy-guard (`_compose_busy` pattern), and spawn/leaf
machinery (`_make_terminal`/`_make_leaf`/`_swap_workspace`). No new widget type — the subline and
context menu already exist. The throttle is manual-refresh + TTL + debounce, NEVER a poll (gh is
network/rate-limited).
Output: modified `src/arduis/window.py`; full suite green (no regression); the headless smoke in
Wave 4 proves the wiring shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-review-cleanup/08-CONTEXT.md
@.planning/phases/08-review-cleanup/08-RESEARCH.md
@.planning/phases/08-review-cleanup/08-01-SUMMARY.md
@.planning/phases/08-review-cleanup/08-02-SUMMARY.md

<interfaces>
<!-- The EXISTING window.py seams to reuse (read in this session — no exploration needed). The new
     code is glue around these; the Plan-01/02 SUMMARYs carry the exact review/gh/cache/cwd signatures. -->

run_git_async now (Plan 02): run_git_async(argv, on_done, runner=None, cwd=None) — pass cwd=worktree_dir.

Row context menu builder (extend the ACTIVE and HIBERNATED branches):
```python
def _make_row_menu_cb(self, sid: str, row: Gtk.ListBoxRow):
    def _on_secondary(_gesture, _n_press, x, y) -> None:
        self._menu_target_sid = sid
        session = self._store.get(sid)
        menu = Gio.Menu()
        if session is not None and session.state == SessionState.ACTIVE:
            menu.append("Hibernar", "win.hibernate")
            if len(session.repos) > 1:
                # "Fechar repositório ▸" submenu — one item per repo via
                # item.set_action_and_target_value("win.close_repo", GLib.Variant.new_string(repo.repo_name))
                ...
        else:
            menu.append("Retomar", "win.resume")
        self._append_isolation_menu_item(menu, sid)   # Phase 7
        # >>> Phase 8 entries appended here (Ver diff ▸, Atualizar status, Abrir PR (web)) <<<
        ...
```

Action registration (add the Phase 8 actions here, mirroring close_repo's string-target action):
```python
def _install_row_actions(self) -> None:
    ...
    close_repo = Gio.SimpleAction.new("close_repo", GLib.VariantType.new("s"))
    close_repo.connect("activate", self._on_close_repo)
    self.add_action(close_repo)
    # >>> Phase 8: win.ver_diff(repo_name), win.refresh_status, win.open_pr <<<
```

Subline handle + the per-repo path + worktree dir:
```python
self._subline_by_sid: dict[str, Gtk.Label]   # set in _make_row; set_text(...) to update in place
def _member_repo_path(self, name: str) -> str # ABSOLUTE source member-repo path (degenerate 1-repo aware)
# Task.repos[*].worktree_dir is the per-repo worktree (<task_dir>/<repo_name>); repo.branch is its branch
self._compose_busy: set[str]                  # the EXISTING in-flight busy-guard pattern to MIRROR for _pr_busy
```

Spawn a read-only leaf (reuse the pinned-main / worktree spawn machinery):
```python
terminal = self._make_terminal()
terminal.set_input_enabled(False)     # VTE 0.76 — read-only viewer (scroll/copy still work)
leaf = self._make_leaf(diff_tid, f"diff {repo_name}", terminal, badge_label="diff")
# add leaf to the ACTIVE workspace LayoutModel (mirror how _spawn_into / split adds a leaf), then
# terminal.spawn_async(..., cwd=worktree_dir, argv=zsh -l -i -c "git --no-pager diff", ...)
```
</interfaces>
</context>

<plan_decisions>
- **Diff leaf spawn (D-02, REVIEW-01):** open a NEW read-only leaf in the ACTIVE workspace. Spawn
  the login shell so PATH/pager/color resolve, running the diff then dropping to a shell so the pane
  stays alive for scroll/copy:
  `["zsh","-l","-i","-c", "git --no-pager diff; exec zsh -i"]` with `spawn_async(..., cwd=worktree_dir)`.
  Call `terminal.set_input_enabled(False)` BEFORE/right-after creating it so keystrokes never reach
  the child (the `exec zsh -i` keeps the buffer; input-disabled makes it a viewer). Reuse
  `_make_terminal` + `_make_leaf(badge_label="diff")` and the SAME leaf-insertion path a split uses
  (add to the active LayoutModel, `_reflect_layout`). Use a stable tid like `f"{sid}:diff:{repo_name}"`.
  Do NOT register a state-file / agent env for this leaf (it is not an agent). Per-repo: the menu
  has one "Ver diff" entry per repo; a 1-repo task is the degenerate single entry. The diff leaf is
  ephemeral — closing it uses the existing `_close_terminal` path (it is just a leaf with a pid;
  ensure its TerminalRecord, if any, is handled like a split — OR skip the record entirely and rely
  on `_close_terminal`'s layout/widget cleanup; PIN the chosen approach in the SUMMARY).
  IMPORTANT: confirm `Vte.Terminal.set_input_enabled` exists at the 0.76 floor during execution
  (08-RESEARCH cites it as available); if a binding quirk appears, the fallback is to not feed input
  by leaving the pane on a finished command — but the research says it is present, so use it.
- **Status read + subline (D-03, GIT-01/REVIEW-02) — the throttle:**
  - Add `self._review_cache = ReviewCache()` and `self._pr_busy: set[str] = set()` in `__init__`
    (mirror `_compose_busy`).
  - `_refresh_task_status(self, task, *, force: bool=False)`: for the task's PRIMARY repo (use
    `task.repos[0]` — aggregate-in-subline per A6) read branch + ahead/behind (git, cheap, TTL
    `GIT_TTL_S`) and PR (gh, network, TTL `GH_TTL_S` + debounce):
    - branch: `run_git_async(review.argv_current_branch(repo.worktree_dir), _on_branch, cwd=repo.worktree_dir)`
      then ahead/behind via `review.argv_ahead_behind(repo.worktree_dir, repo.branch)` →
      `review.parse_ahead_behind` → `gh.format_branch_subline(branch, ahead, behind)`.
    - PR: gate on `gh.gh_available()` FIRST — if absent, set subline to `gh.GH_ABSENT_MSG` and skip
      gh entirely. Else, unless `force`, skip if a fresh cached payload exists
      (`self._review_cache.fresh_payload(task.task_id, now, GH_TTL_S)`) — render the cached subline.
      Else, if `task.task_id in self._pr_busy` DROP (debounce — do not queue). Else add to `_pr_busy`,
      `run_git_async(gh.argv_pr_view(repo.branch), _on_pr, cwd=repo.worktree_dir)`.
    - `_on_pr(rc, out, err)`: ALWAYS `self._pr_busy.discard(task.task_id)`. If rc == GH_EXIT_NEEDS_AUTH
      → subline `gh.GH_UNAUTH_MSG`; elif rc != 0 or not out.strip() → "sem PR"; else
      `try: pr = gh.parse_pr_view(out)` / `except (ValueError, TypeError): return` (never crash),
      `self._review_cache.put(task.task_id, pr, now)`, render
      `f"{branch_sub} · {gh.format_pr_subline(pr)}"`. Update via the EXISTING
      `self._subline_by_sid.get(sid)` label `set_text(...)` (in place — no row rebuild). Guard for a
      missing label (the row may have been rebuilt) — `if label is not None`.
    - `now = time.monotonic()` (import time at module top if not already).
  - **Auto-read gating (D-03):** call `_refresh_task_status(task)` (force=False) ONCE on task create
    (at the end of the create flow, after the task is in the store) and ONCE on row-activation
    (`_on_row_activated`, only for an ACTIVE task — a hibernated one shows the placeholder). The TTL
    cache makes rapid row switching a no-op for gh. NEVER add a poll tick.
- **'Atualizar status' menu item (D-03):** `menu.append("Atualizar status", "win.refresh_status")`
  in BOTH ACTIVE and HIBERNATED branches (a hibernated task can still show its on-disk branch/PR).
  `win.refresh_status` (no target — uses `_menu_target_sid` like hibernate) calls
  `_refresh_task_status(task, force=True)`.
- **'Abrir PR (web)' menu item (D-05, REVIEW-02):** append ONLY when `gh.gh_available()` is True.
  `win.open_pr` (no target) runs `run_git_async(gh.argv_pr_create_web(), _on_pr_create, cwd=repo.worktree_dir)`
  for the primary repo; `_on_pr_create` toasts the result (rc 0 → "PR aberto no navegador"; rc 4 →
  GH_UNAUTH_MSG; else surface a short pt-BR error). After a successful create, fire
  `_refresh_task_status(task, force=True)` so the new PR shows. This is the ONE allowed write.
- **'Ver diff ▸' menu item (D-02):** for a multi-repo task build a `Gio.Menu` submenu with one
  item per repo (`win.ver_diff` string-target = repo_name, mirroring `win.close_repo`); for a 1-repo
  task append a single "Ver diff" item targeting that repo. `_on_ver_diff(action, param)` resolves
  the repo by name and calls `_open_diff_leaf(task, repo)`.
- **Action registration:** in `_install_row_actions` add `refresh_status` (no target),
  `open_pr` (no target), and `ver_diff` (GLib.VariantType.new("s") string target = repo_name),
  mirroring the existing `close_repo`/`hibernate` registration exactly.
- **NO new dependency, NO poll, NO scrape.** All reads go through `run_git_async` (async, GLib loop)
  with cwd=worktree. gh is gated on availability + TTL + debounce.
- **This plan does NOT touch the conclude/teardown path** (that is Plan 04, the next sequential
  window.py wave). Do not add "Concluir task" here.
</plan_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Read-only diff leaf (REVIEW-01) — _open_diff_leaf + 'Ver diff ▸' menu/action</name>
  <files>src/arduis/window.py</files>
  <read_first>
    - src/arduis/window.py: _make_terminal (~938), _make_leaf (~990), _spawn_into (~2969) + how a leaf is added to a LayoutModel and _reflect_layout; _make_row_menu_cb (~1356); _install_row_actions (~3388); _on_close_repo registration; _close_terminal (~1088)
    - 08-RESEARCH.md Pattern 2 (read-only VTE leaf, set_input_enabled(False)) + DESIGN §2
  </read_first>
  <action>
Add `_open_diff_leaf(self, task, repo)`: make a terminal via `_make_terminal()`, call
`terminal.set_input_enabled(False)`, build a leaf with `_make_leaf(f"{sid}:diff:{repo.repo_name}",
f"diff {repo.repo_name}", terminal, badge_label="diff")`, insert it into the ACTIVE workspace's
LayoutModel exactly the way a split adds a leaf (study `_spawn_into`/the split path), register it in
`_leaf_by_sid`/`_term_by_sid`, `_reflect_layout()`, then `terminal.spawn_async(Vte.PtyFlags.DEFAULT,
repo.worktree_dir, ["zsh","-l","-i","-c","git --no-pager diff; exec zsh -i"], TERM_ENV-equiv,
GLib.SpawnFlags.DEFAULT, None, None, -1, None, <a benign spawn cb that just records the pid for
teardown>)`. Decide and PIN whether the diff leaf gets a TerminalRecord (recommend: a lightweight
"shell"-kind record so `_close_terminal` tears it down cleanly, OR no record + rely on layout
cleanup — choose the path that closes cleanly with the existing `_close_terminal`). Add the
'Ver diff ▸' menu (submenu per repo for multi-repo, single item for 1-repo) into `_make_row_menu_cb`
(both ACTIVE and HIBERNATED branches — a hibernated task still has worktrees on disk). Register the
`win.ver_diff` string-target action in `_install_row_actions` and add `_on_ver_diff(self, _action,
param)` that resolves the menu-target task + the repo by `param.get_string()` and calls
`_open_diff_leaf`. Confirm `set_input_enabled` exists at the 0.76 floor.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && /tmp/arduis-venv-ab12/bin/python -c "import ast,sys; src=open('src/arduis/window.py').read(); assert 'set_input_enabled(False)' in src; assert '_open_diff_leaf' in src; assert 'win.ver_diff' in src or 'ver_diff' in src; ast.parse(src); print('OK diff-leaf wiring + parses')" && /tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Right-clicking a task row offers 'Ver diff ▸ <repo>' (per repo / single for 1-repo); selecting it opens a read-only (`set_input_enabled(False)`) VTE leaf running `git --no-pager diff` for that worktree; window.py parses and the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Branch + PR status subline + throttle (GIT-01/REVIEW-02) — _refresh_task_status behind TTL cache + _pr_busy debounce + 'Atualizar status'</name>
  <files>src/arduis/window.py</files>
  <read_first>
    - src/arduis/window.py: __init__ (~263, where _compose_busy/_subline_by_sid live), _make_row subline (~1302), _on_row_activated (~1721), the create-flow tail (~2589 _store.add / ~2820 spawn), _compose_busy usage (~1471 add / ~1526 discard) as the debounce pattern
    - 08-01-SUMMARY.md + 08-02-SUMMARY.md (review/review_cache/gh signatures), 08-RESEARCH §"Throttle + Cache Design" + DESIGN §1
  </read_first>
  <action>
In `__init__` add `self._review_cache = ReviewCache()` and `self._pr_busy: set[str] = set()` (import
ReviewCache/GIT_TTL_S/GH_TTL_S from arduis.review_cache; review + gh from arduis). Implement
`_refresh_task_status(self, task, *, force=False)` exactly per <plan_decisions>: read branch +
ahead/behind (git, cwd=worktree) → `gh.format_branch_subline`; gate PR on `gh.gh_available()` →
TTL cache (`fresh_payload`) → `_pr_busy` debounce → `run_git_async(gh.argv_pr_view(repo.branch),
_on_pr, cwd=repo.worktree_dir)`; `_on_pr` discards `_pr_busy`, handles rc==GH_EXIT_NEEDS_AUTH /
rc!=0 / parse-garbage (try/except, never crash), caches the payload, and updates
`self._subline_by_sid.get(sid)` in place (guard None). Use `time.monotonic()` for `now`. Wire the
auto-read: call `_refresh_task_status(task)` ONCE at the end of the create flow (after `_store.add`)
and ONCE in `_on_row_activated` for an ACTIVE task. Add 'Atualizar status' (`win.refresh_status`,
no target) to BOTH menu branches + register the action + `_on_refresh_status(self, _a, _p)` that
resolves `_menu_session()` and calls `_refresh_task_status(task, force=True)`. NEVER add a poll.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && /tmp/arduis-venv-ab12/bin/python -c "import ast; src=open('src/arduis/window.py').read(); assert '_refresh_task_status' in src; assert '_pr_busy' in src; assert 'gh_available' in src; assert 'win.refresh_status' in src or 'refresh_status' in src; assert 'monotonic' in src; ast.parse(src); print('OK status+throttle wiring + parses')" && /tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>The sidebar subline shows branch + ahead/behind + PR status; reads are gated by the TTL cache + _pr_busy debounce + gh_available (no poll, no spam); gh absent → "gh ausente", exit-4 → "gh não autenticado", garbage caught; auto-reads fire on create + row-activate; 'Atualizar status' forces a re-read; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: 'Abrir PR (web)' — the ONE allowed write (REVIEW-02) + full suite green</name>
  <files>src/arduis/window.py</files>
  <read_first>
    - src/arduis/window.py: _make_row_menu_cb, _install_row_actions, _toast (~3374), the _refresh_task_status from Task 2
    - 08-02-SUMMARY.md (argv_pr_create_web, GH_UNAUTH_MSG)
  </read_first>
  <action>
Append 'Abrir PR (web)' to the row menu ONLY when `gh.gh_available()` is True (both ACTIVE and
HIBERNATED branches). Register `win.open_pr` (no target) in `_install_row_actions` and add
`_on_open_pr(self, _a, _p)`: resolve `_menu_session()`, take `task.repos[0]`, run
`run_git_async(gh.argv_pr_create_web(), _on_create, cwd=repo.worktree_dir)`. `_on_create(rc,out,err)`:
rc==0 → `self._toast("PR aberto no navegador")` + `_refresh_task_status(task, force=True)`;
rc==GH_EXIT_NEEDS_AUTH → `self._toast(gh.GH_UNAUTH_MSG)`; else `self._toast` a short pt-BR error from
err's first line. This is the ONLY write arduis performs. Then run the FULL suite and confirm zero
regression across the ~344 baseline (the Wave-1 review/gh/cache tests + everything prior).
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && /tmp/arduis-venv-ab12/bin/python -c "import ast; src=open('src/arduis/window.py').read(); assert '_on_open_pr' in src; assert 'argv_pr_create_web' in src; assert 'win.open_pr' in src or 'open_pr' in src; ast.parse(src); print('OK open-pr wiring + parses')" && /tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>'Abrir PR (web)' appears only when gh is available and runs `gh pr create --web` (the ONE allowed write) via run_git_async, toasting the result and refreshing status on success; the full suite is green with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch / worktree dir → git/gh argv + cwd | user-chosen branch + arduis-built worktree dir cross into git/gh invocations (argv elements + cwd). |
| git diff / gh JSON output → read-only display | shown in a read-only VTE leaf / a subline; never eval'd, never used to build a path/command. |
| diff VTE leaf → the child process | the leaf must not accept keystrokes that reach the child. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | EoP | the diff leaf accepting input to the child | mitigate | `terminal.set_input_enabled(False)` makes the diff pane a read-only viewer (scroll/copy only) — no keystrokes reach the child (REVIEW-01 / Pitfall: read-only display). |
| T-08-09 | Injection | branch/repo injected into the spawned diff/gh command | mitigate | argv are Python LISTS via the spawn/run machinery routed through HostRunner; the diff is `["zsh","-l","-i","-c","git --no-pager diff; exec zsh -i"]` with cwd=worktree (no branch interpolated into the string); gh argv carry the branch as a discrete element. |
| T-08-10 | DoS (self) | gh rate-limit storm from status reads | mitigate | reads are gated by `gh_available()` (no call when absent) + the TTL cache (GH_TTL_S=120) + `_pr_busy` in-flight debounce (drop, never queue); auto-reads fire only on create + row-activate; NO poll. |
| T-08-11 | Tampering (the ONE write) | `gh pr create --web` is the only sanctioned write | accept | roadmap-sanctioned (REVIEW-02); `--web` keeps it browser-driven; offered only when gh is available; everything else stays read-only. |
</threat_model>

<verification>
- window.py parses (`ast.parse`) and the full suite is green: `/tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q` exits 0
- `set_input_enabled(False)` present on the diff leaf; `_open_diff_leaf`, `_refresh_task_status`, `_on_open_pr` exist
- `_pr_busy` debounce + `gh_available` gate + `monotonic`-based TTL present; no poll tick added
- The Wave-4 headless smoke proves the menu entries appear and the argv shapes are correct
</verification>

<success_criteria>
- REVIEW-01: a read-only diff VTE leaf opens per repo from the row menu.
- GIT-01/REVIEW-02: the subline shows branch + ahead/behind + PR status behind the TTL + debounce throttle (no poll); gh absent/exit-4 degrade gracefully.
- REVIEW-02: 'Abrir PR (web)' runs the ONE allowed write only when gh is available.
- No regression; window.py stays glue over the Plan-01/02 GTK-free builders.
</success_criteria>

<output>
After completion, create `.planning/phases/08-review-cleanup/08-03-SUMMARY.md` documenting:
the new window.py methods + actions (`_open_diff_leaf`, `_refresh_task_status`, `_on_ver_diff`,
`_on_refresh_status`, `_on_open_pr`, the `win.ver_diff`/`win.refresh_status`/`win.open_pr` actions),
the chosen diff-leaf teardown approach (record vs layout-only), the `_review_cache`/`_pr_busy`
fields, the auto-read trigger points (create + row-activate), and any 0.76 API confirmations
(`set_input_enabled`). Note for Plan 04: the row menu now has Ver diff / Atualizar status / Abrir PR
— Plan 04 adds "Concluir task" to the SAME `_make_row_menu_cb`.
</output>
