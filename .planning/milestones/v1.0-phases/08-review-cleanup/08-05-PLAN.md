---
phase: 08-review-cleanup
plan: 05
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - tests/smoke/test_review_cleanup_smoke.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, GIT-01]
user_setup: []

must_haves:
  truths:
    - "A runnable headless smoke (broadway, skips cleanly if gtk4-broadwayd absent) proves the Phase-8 row menu wires up: 'Ver diff ▸', 'Atualizar status', 'Abrir PR (web)' (when gh available), and 'Concluir task' all appear on a real task row built from a sandbox-HOME fixture (REVIEW-01/02/03, GIT-01)"
    - "The smoke exercises the CONCLUDE clean-gate against a REAL dirty git fixture under sandbox $HOME: a clean task concludes (worktrees removed, source repo + branch intact); a DIRTY task is REFUSED (no worktree removed, refusal surfaced) — proving criterion 4 end-to-end with real git, not just mocks (REVIEW-03)"
    - "The smoke proves D-10 on disk: after a clean conclude the source repo dir + its branch still exist and a task-folder symlink TARGET (a root file) is untouched while the LINK is gone (REVIEW-03)"
    - "The smoke proves the gh-degrade argv path: with gh forced 'absent' (PATH-stubbed) the subline shows 'gh ausente' and gh is never invoked; the gh argv shapes (pr view --json, pr create --web) match the builders — gh pr create is NEVER executed (REVIEW-02/GIT-01, D-06)"
  artifacts:
    - path: "tests/smoke/test_review_cleanup_smoke.py"
      provides: "headless broadway acceptance: menu entries present, real-git clean-vs-dirty conclude (criterion 4), D-10 on-disk proof, gh-absent degrade + argv-shape check; SKIPS exit 0 if broadwayd absent; NEVER runs gh pr create"
  key_links:
    - from: "tests/smoke/test_review_cleanup_smoke.py"
      to: "ArduisWindow row menu + _conclude_task + review/gh builders"
      via: "build a sandbox-HOME multi-repo task fixture, drive the conclude, assert on-disk + argv shapes"
      pattern: "Concluir|argv_worktree_remove|porcelain|gh ausente"
---

<objective>
Write the Phase-8 HEADLESS acceptance smoke (`tests/smoke/test_review_cleanup_smoke.py`) — a runnable
broadway harness (skips exit-0 if `gtk4-broadwayd` is absent) that proves the wiring END-TO-END
against REAL git fixtures under a sandbox `$HOME`: the row menu offers Ver diff / Atualizar status /
Abrir PR (web) / Concluir task; the conclude clean-gate REMOVES a clean task's worktrees (source +
branch intact, D-10) and REFUSES a dirty task (criterion 4); and the gh-absent degrade shows
"gh ausente" without invoking gh. This closes the gap that mocked unit tests cannot: a real
`git worktree remove` against a real dirty tree, and real on-disk D-10 proof. `gh pr create` is
NEVER executed.

Purpose: Plan 04's unit test proves the conclude ORDER + refusal with mocked channels; this smoke
proves the SAME safety with REAL git on disk — the dirty-tree refusal and the never-delete-source
rule are the security items, so they get a real-git acceptance check. The smoke follows the existing
`tests/smoke/test_topbar_chips_smoke.py` harness shape (sandbox HOME, /tmp git fixtures, broadwayd on
a high display, killed in finally, SKIP exit-0 if broadwayd absent). It does NOT override
XDG_RUNTIME_DIR.
Output: `tests/smoke/test_review_cleanup_smoke.py`; runnable headless; SKIPS cleanly without
broadway; gh never executed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-review-cleanup/08-CONTEXT.md
@.planning/phases/08-review-cleanup/08-RESEARCH.md
@.planning/phases/08-review-cleanup/08-03-SUMMARY.md
@.planning/phases/08-review-cleanup/08-04-SUMMARY.md

<interfaces>
<!-- The EXISTING smoke harness to mirror exactly. Read it before writing — same broadway lifecycle,
     same sandbox-HOME + /tmp git fixtures, same SKIP-on-absent, same finally-kill. -->
@tests/smoke/test_topbar_chips_smoke.py

<!-- Key shapes the smoke asserts (from the Plan 03/04 SUMMARYs):
     - row menu after Phase 8: Ver diff ▸ / Atualizar status / Abrir PR (web) [if gh] / Concluir task
     - _conclude_task: kill → down → porcelain gate → worktree remove (NO --force) → prune →
       unlink symlinks (links only) → store.remove
     - gh.GH_ABSENT_MSG == "gh ausente"; gh.gh_available() == shutil.which("gh") is not None
     - review.argv_worktree_remove has NO --force ; parse_porcelain_clean("")==True
-->
</interfaces>
</context>

<plan_decisions>
- **Mirror `tests/smoke/test_topbar_chips_smoke.py` verbatim** for the harness: a sandbox HOME
  (tempdir), real /tmp git fixtures (`git init` + a commit so worktrees can be added), broadwayd on
  a fresh high display (e.g. `:96`), `GDK_BACKEND=broadway`, killed in `finally`; `SMOKE_SKIP` +
  `sys.exit(0)` if `gtk4-broadwayd` is absent. DO NOT override `XDG_RUNTIME_DIR` (per MEMORY).
- **Real multi-repo task fixture:** create a meta-root with 2 member repos (each `git init` + initial
  commit), build a Task via the project's create path (or construct the Task + materialize worktrees
  via `git worktree add` + `task_layout.symlink_plan` + a real relative symlink to a root file like
  `CLAUDE.md`), so the conclude operates on REAL worktrees + a REAL symlink.
- **Menu-presence check:** build the `Gio.Menu` for the task row (call `_make_row_menu_cb`'s body or
  the menu builder) and assert the labels "Ver diff", "Atualizar status", "Concluir task" are
  present; "Abrir PR (web)" present iff `gh.gh_available()`. (Inspect the Gio.Menu model items.)
- **CLEAN conclude (criterion 4 + D-10 on disk):** with both worktrees clean (committed),
  drive `_conclude_task(task)` (confirm path auto-accepted or call the inner chain directly), then
  assert: both worktree dirs are GONE; the SOURCE repo dirs still EXIST; the branch still exists in
  the source repo (`git branch --list` shows it); the symlink LINK in the task folder is gone but the
  TARGET root file still exists; the task is dropped from the store.
- **DIRTY conclude (criterion 4 refusal):** dirty ONE worktree (write an uncommitted file), drive
  the conclude, then assert: NEITHER worktree was removed (both dirs still exist), the source repos
  + branch intact, the task still in the store, and a refusal was surfaced (capture the toast/dialog
  message or a recorded refusal flag naming the dirty repo). This is the real-git proof that mocks
  cannot give.
- **gh-absent degrade:** run a sub-section with `PATH` stubbed so `shutil.which("gh")` is None (e.g.
  set `os.environ["PATH"]` to a dir without gh for that check), assert `gh.gh_available()` is False
  and the status subline renders `gh.GH_ABSENT_MSG` ("gh ausente") and gh is NOT invoked. Assert the
  argv builders' shapes (`gh.argv_pr_view(branch)`, `gh.argv_pr_create_web()`) match the verified
  forms. **NEVER execute `gh pr create`** — argv-shape assertion only.
- **Driving the async conclude in the smoke:** mirror the drive method Plan 04's SUMMARY pins (either
  pump a short `GLib.MainLoop` with a timeout, or use the synchronous-on_done stub style). For the
  REAL-git conclude, `run_git_async` actually runs git (real removes) — pump the GLib loop with a
  generous timeout (cap ~10s) so the chain completes; never hang.
- **Self-reporting harness:** print `SMOKE <name> PASS/FAIL` per check (like the existing smoke) and
  exit non-zero if any FAIL, so pytest collecting the file (or a direct run) flags failures. Keep it
  runnable both via `pytest tests/smoke/test_review_cleanup_smoke.py` and directly.
- **This plan adds ONLY the smoke file** — no src changes (Plan 03/04 own window.py).
</plan_decisions>

<tasks>

<task type="auto" smoke="true">
  <name>Task 1: Headless acceptance smoke — menu entries + real-git clean/dirty conclude + D-10 on-disk + gh-absent degrade</name>
  <files>tests/smoke/test_review_cleanup_smoke.py</files>
  <read_first>
    - tests/smoke/test_topbar_chips_smoke.py (the harness to mirror — broadway lifecycle, sandbox HOME, /tmp fixtures, SKIP-on-absent, finally-kill)
    - 08-04-SUMMARY.md (the conclude method names + async-drive method + pt-BR refusal messages) + 08-03-SUMMARY.md (menu entries + gh degrade)
    - src/arduis/review.py + src/arduis/gh.py (the builder shapes to argv-assert) + src/arduis/task_layout.py (symlink_plan)
  </read_first>
  <behavior>
    A runnable broadway smoke (SKIPS exit-0 if gtk4-broadwayd absent; sandbox HOME; real /tmp git
    fixtures; broadwayd killed in finally; XDG_RUNTIME_DIR untouched):
    - MENU: a real task row's Gio.Menu contains "Ver diff", "Atualizar status", "Concluir task";
      "Abrir PR (web)" iff gh.gh_available().
    - CLEAN CONCLUDE (criterion 4 + D-10): a 2-repo task with both worktrees clean → conclude →
      both worktree dirs removed; both SOURCE repo dirs still exist; the branch still exists in each
      source repo; the task-folder symlink LINK gone but its TARGET root file still exists; task
      dropped from the store.
    - DIRTY CONCLUDE (criterion 4 refusal): one worktree made dirty (uncommitted file) → conclude →
      NEITHER worktree removed; sources + branch intact; task still in store; refusal surfaced
      naming the dirty repo. (NO --force anywhere.)
    - GH-ABSENT DEGRADE: with gh forced absent (PATH without gh) → gh.gh_available() False, subline
      shows "gh ausente", gh NOT invoked. argv shapes: gh.argv_pr_view("feat/x") and
      gh.argv_pr_create_web() match the verified forms. gh pr create is NEVER executed.
    - Prints SMOKE <name> PASS/FAIL per check; exits non-zero on any FAIL.
  </behavior>
  <action>
Copy the harness shape from tests/smoke/test_topbar_chips_smoke.py (broadway start/kill, sandbox
HOME, SKIP-on-absent). Build a real meta-root with 2 git-init'd member repos (each with a commit),
materialize a Task's worktrees with real `git worktree add` + a real relative symlink to a root file
(via task_layout.symlink_plan), construct the ArduisWindow against the sandbox HOME, and implement
the four checks per <behavior>. Drive the async conclude with the method Plan 04 pinned (pump a
GLib.MainLoop with a ~10s timeout for the real-git removes). For the gh-absent section stub PATH so
shutil.which("gh") is None and assert the subline + that gh is never invoked; assert the gh argv
shapes directly from the builders. NEVER call `gh pr create`. Emit SMOKE PASS/FAIL lines and a
non-zero exit on failure.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && /tmp/arduis-venv-ab12/bin/python tests/smoke/test_review_cleanup_smoke.py; echo "exit=$?"</automated>
  </verify>
  <done>The smoke runs headless (or SKIPS exit-0 without broadwayd): the Phase-8 menu entries are present; a clean 2-repo task concludes with worktrees removed and source+branch+symlink-target intact (D-10); a dirty task is REFUSED with no worktree removed (criterion 4); gh-absent shows "gh ausente" without invoking gh; gh pr create is never executed; the run exits 0.</done>
  </task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke fixture git repos → real git worktree remove | the smoke runs REAL `git worktree remove` against sandbox-HOME fixtures — it must never touch anything outside the tempdir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering (test-time) | the smoke's real-git removes touching the developer's real repos | mitigate | the smoke operates ONLY under a sandbox `$HOME` tempdir with /tmp git fixtures (mirrors the existing smoke); all paths are tempdir-rooted; cleaned in finally. |
| T-08-18 | Tampering (the ONE write) | accidentally running `gh pr create` in CI/smoke | mitigate | the smoke argv-asserts `gh pr create --web` shape ONLY and NEVER executes it; the gh-absent section stubs PATH so gh cannot be invoked. |
</threat_model>

<verification>
- `/tmp/arduis-venv-ab12/bin/python tests/smoke/test_review_cleanup_smoke.py` exits 0 (or SMOKE_SKIP exit 0 without broadwayd)
- The clean conclude leaves source repos + branches + symlink TARGET intact (real-git D-10 proof)
- The dirty conclude removes NOTHING and surfaces a refusal (real-git criterion-4 proof)
- gh-absent shows "gh ausente"; gh pr create is never executed
</verification>

<success_criteria>
- REVIEW-01/02/03 + GIT-01 wiring is proven end-to-end headless: menu entries, real-git clean/dirty conclude (criterion 4), on-disk D-10, gh-absent degrade.
- The security items (dirty refusal, never-delete-source) get a REAL-git acceptance check beyond the mocked unit test.
- gh pr create is never executed; the smoke SKIPS cleanly without broadway.
</success_criteria>

<output>
After completion, create `.planning/phases/08-review-cleanup/08-05-SUMMARY.md` documenting:
the smoke's checks + how it builds the real multi-repo fixture, the async-drive method used, the
real-git clean/dirty results observed, and whether broadwayd was present (ran) or absent (skipped)
in this environment.
</output>
