---
phase: 08-review-cleanup
plan: 06
type: execute
wave: 4
depends_on: [08-03, 08-04]
files_modified:
  - .planning/phases/08-review-cleanup/08-HUMAN-UAT.md
autonomous: false
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, GIT-01]
user_setup: []

must_haves:
  truths:
    - "The user visually confirms a read-only diff renders WITH COLOR in a real VTE leaf and rejects keystrokes (REVIEW-01)"
    - "The user confirms 'Abrir PR (web)' opens a real GitHub PR form and the subline then shows the real PR status via `gh pr view --json` (REVIEW-02, GIT-01)"
    - "The user confirms 'Concluir task' on a CLEAN task removes the worktrees + container stack while the source repos + branch + history stay intact, and on a DIRTY task is REFUSED with a clear pt-BR message (REVIEW-03 criterion 4)"
    - "The user confirms status is throttled (manual refresh works; rapid row switching does not spam gh) and gh-absent/unauthed degrades gracefully in the subline (GIT-01, D-06)"
  artifacts:
    - path: ".planning/phases/08-review-cleanup/08-HUMAN-UAT.md"
      provides: "the live acceptance checklist the user runs against real git/gh on the host (diff color + read-only, real PR create+status, clean vs dirty conclude, throttle, gh degrade)"
  key_links:
    - from: ".planning/phases/08-review-cleanup/08-HUMAN-UAT.md"
      to: "the running arduis app on the host (real git 2.43 + gh 2.93 authed)"
      via: "the user follows the numbered steps and records pass/fail"
      pattern: "Concluir|Ver diff|Abrir PR"
---

<objective>
Produce the LIVE human-acceptance checklist (`08-HUMAN-UAT.md`) and pause for the user to run it
against the real arduis app with real git/gh on the host. The headless smoke (Plan 05) proves argv
shapes + real-git conclude under sandbox; this checkpoint covers what only a human at a real display
+ real GitHub can confirm: the diff renders WITH COLOR and is genuinely read-only, `gh pr create`
opens a real PR and the subline reflects its real status, and "Concluir" on a clean vs dirty real
task behaves correctly (worktrees + containers gone, source + branch + history intact; dirty
refused).

Purpose: the conclude clean-gate (never destroy uncommitted work) and the read-only diff are the
load-bearing UX/safety items; a human must SEE the color diff, TRY to type into it, open a REAL PR,
and confirm a dirty task is REFUSED with a clear message — none of which a headless smoke can fully
assert. This is the only non-autonomous plan in the phase.
Output: `.planning/phases/08-review-cleanup/08-HUMAN-UAT.md`; a `checkpoint:human-verify` gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-review-cleanup/08-CONTEXT.md
@.planning/phases/08-review-cleanup/08-RESEARCH.md
@.planning/phases/08-review-cleanup/08-03-SUMMARY.md
@.planning/phases/08-review-cleanup/08-04-SUMMARY.md
</context>

<plan_decisions>
- **Task 1 (autonomous) writes the checklist;** Task 2 is the `checkpoint:human-verify` gate. The
  checklist is concrete: real repo, real branch, real PR, real dirty file, exact menu labels, exact
  expected outcomes (worktree dirs gone, source dirs + branch present, container stack down).
- **The checklist must NOT ask the user to do anything arduis automates** — they only OBSERVE and
  CONFIRM (open the app, right-click a task, click the menu items, watch the result). The one
  genuinely external step is the GitHub PR form in the browser (`gh pr create --web`) and confirming
  the PR was created — that is inherently human.
- **Cover all four requirements + the degrade:** REVIEW-01 (diff color + read-only), REVIEW-02 (PR
  create + status), REVIEW-03 (clean conclude keeps source/branch + removes worktrees/containers;
  dirty conclude refused), GIT-01 (throttle: manual refresh works, rapid switch does not spam;
  gh-absent/unauthed subline).
- **Safety emphasis:** the dirty-conclude step explicitly instructs the user to leave an uncommitted
  change and confirm arduis REFUSES with a clear pt-BR message and removes NOTHING — this is the
  criterion-4 acceptance the whole phase exists for.
</plan_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Write the live human-acceptance checklist (08-HUMAN-UAT.md)</name>
  <files>.planning/phases/08-review-cleanup/08-HUMAN-UAT.md</files>
  <read_first>
    - 08-03-SUMMARY.md + 08-04-SUMMARY.md (exact menu labels + conclude behavior + pt-BR messages)
    - 08-RESEARCH.md §"Validation Architecture" (the live UAT row) + §"DESIGN" (menu shape)
  </read_first>
  <action>
Write `.planning/phases/08-review-cleanup/08-HUMAN-UAT.md` as a numbered checklist with these
sections, each step stating the ACTION and the EXPECTED RESULT (pass/fail box):

1. SETUP: launch arduis in a real multi-repo (or 1-repo) project; create a task on a branch with a
   real diff vs its base; ensure gh is authed (`gh auth status`).
2. REVIEW-01 (read-only diff): right-click the task row → "Ver diff ▸ <repo>". EXPECT: a new pane
   opens showing `git diff` WITH COLOR; scrolling + selecting/copying works; TYPING does nothing
   (the pane is read-only). Confirm for each repo on a multi-repo task.
3. GIT-01 (branch/PR status + throttle): observe the row subline shows branch + ↑/↓ ahead-behind.
   Click "Atualizar status" → it re-reads. Switch rapidly between task rows → gh is NOT spammed
   (no lag/rate-limit; status updates respect the cache).
4. REVIEW-02 (open PR + read status): right-click → "Abrir PR (web)". EXPECT: the browser opens the
   GitHub PR form; create the PR. Back in arduis click "Atualizar status" → the subline now shows
   the real PR (e.g. "PR #N open").
5. D-06 degrade: (optional, if easy) temporarily rename/hide gh or sign out → the subline shows
   "gh ausente" / "gh não autenticado"; arduis does NOT crash; diff + branch status still work.
6. REVIEW-03 CLEAN conclude (criterion 4): on a task whose worktrees are CLEAN (all committed),
   right-click → "Concluir task" → confirm the dialog (note it says source + branch stay intact).
   EXPECT: the worktree dirs are removed; the SOURCE repos still exist; the branch still exists
   (`git branch --list <branch>` in a source repo); if isolation was on, the container stack is
   down (`docker compose ls` shows no arduis-<branch>); the task disappears from the sidebar.
7. REVIEW-03 DIRTY conclude (criterion 4 — THE safety): on a task, leave an UNCOMMITTED change in a
   worktree, then right-click → "Concluir task" → confirm. EXPECT: arduis REFUSES with a clear
   pt-BR message naming the dirty repo; NOTHING is removed (the worktree dirs + uncommitted change
   still exist); the task stays in the sidebar. (No --force, no data loss.)

End with a "Result: PASS / FAIL + notes" line. Keep it tight and observation-only (the user clicks
menu items and watches; the only external action is creating the real PR in the browser).
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && test -f .planning/phases/08-review-cleanup/08-HUMAN-UAT.md && grep -qi "Concluir" .planning/phases/08-review-cleanup/08-HUMAN-UAT.md && grep -qi "Ver diff" .planning/phases/08-review-cleanup/08-HUMAN-UAT.md && grep -qi "Abrir PR" .planning/phases/08-review-cleanup/08-HUMAN-UAT.md && echo "OK UAT checklist present with all surfaces"</automated>
  </verify>
  <done>08-HUMAN-UAT.md exists with numbered steps covering REVIEW-01 (color + read-only diff), GIT-01 (status + throttle), REVIEW-02 (real PR create + status), D-06 degrade, and REVIEW-03 clean + dirty conclude (criterion 4), each with an expected result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live human-verify — run 08-HUMAN-UAT.md against real git/gh</name>
  <action>Pause for the user to run the live acceptance checklist on the host. This is a blocking checkpoint — no automation; the user observes and confirms the Phase-8 surfaces against real git + gh.</action>
  <what-built>
    Phase 8 read + cleanup surfaces wired into arduis: a read-only color diff VTE leaf (REVIEW-01),
    branch + PR status in the sidebar subline behind a manual-refresh + TTL throttle (GIT-01/REVIEW-02),
    'Abrir PR (web)' running the one allowed write `gh pr create --web` (REVIEW-02), and 'Concluir task'
    executing the safe ordered teardown — kill agents → compose down → porcelain clean-gate → git worktree
    remove (NEVER --force) → prune → unlink symlinks → drop — refusing a dirty tree (REVIEW-03 criterion 4).
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/08-review-cleanup/08-HUMAN-UAT.md`.
    2. Launch arduis in a real project and run every numbered step against real git + gh.
    3. Pay special attention to the two safety-critical steps:
       - Step 2: the diff pane shows COLOR and rejects typing (read-only).
       - Step 7: a DIRTY task's "Concluir" is REFUSED with a clear pt-BR message and removes NOTHING.
    4. Confirm Step 6 leaves the source repos + branch + history intact after a clean conclude.
  </how-to-verify>
  <verify>Manual: the user completes every step of 08-HUMAN-UAT.md and records PASS or the failing step.</verify>
  <done>The user has run the live checklist and typed "approved" (all pass) or named the failing step(s) for a gap-closure plan.</done>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed (esp. the diff read-only / color, the real PR status, or the clean/dirty conclude behavior) so a gap-closure plan can address it.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/08-review-cleanup/08-HUMAN-UAT.md` exists and covers REVIEW-01/02/03 + GIT-01 + D-06
- The human-verify gate is reached; the user runs the live checklist against real git/gh
- Special focus: diff color + read-only (REVIEW-01); dirty-conclude refusal (REVIEW-03 criterion 4)
</verification>

<success_criteria>
- The user has a concrete live checklist and has confirmed (or flagged) every Phase-8 surface against real git/gh.
- The two load-bearing safety items — read-only color diff and the dirty-tree conclude refusal — are human-verified.
- Any failure is captured precisely enough to seed a `--gaps` plan.
</success_criteria>

<output>
After approval, create `.planning/phases/08-review-cleanup/08-06-SUMMARY.md` recording the UAT
outcome: which steps passed, any failures + notes, and whether a gap-closure plan is needed.
</output>
