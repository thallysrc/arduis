---
phase: 03.2-projects-and-cross-repo-tasks
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/arduis/window.py
autonomous: true
requirements: [PAR-01, PAR-02]
user_setup: []

must_haves:
  truths:
    - "Launching arduis in a multi-repo root detects member repos and shows the project NAME in the topbar (no switcher)"
    - "Launching arduis in a single git repo (no member subdirs) is the degenerate 1-repo project (03.1 behavior preserved)"
    - "'New task' picks 1+ member repos + a branch; materializes ../<root>-tasks/<branch>/ with the chosen repos as worktrees and everything else relative-symlinked to root"
    - "A 2-repo task opens with 4 terminals: one column per repo (agent on top, shell below) — a default, not a structural rule"
    - "The task workspace becomes visible on creation; the sidebar lists the task; selecting it swaps the whole workspace"
    - "A repo that aborts (branch checked out elsewhere) shows a clear per-repo message; the repos that succeeded are kept (best-effort, never --force)"
    - "The pinned row is <project> · zsh — one terminal at the PROJECT ROOT"
  artifacts:
    - path: "src/arduis/window.py"
      provides: "project-root resolution, New-task dialog, per-repo async create chain + symlink materializer, Task-keyed sidebar/swap"
      contains: "detect_member_repos"
  key_links:
    - from: "src/arduis/window.py"
      to: "src/arduis/project.py:detect_member_repos"
      via: "import + call at startup (replaces _resolve_repo_root single rev-parse)"
      pattern: "detect_member_repos"
    - from: "src/arduis/window.py"
      to: "src/arduis/task_layout.py:symlink_plan + task_dir_for + repo_worktree_dir + resolve_repo_add"
      via: "materialize task folder then per-repo worktree add"
      pattern: "symlink_plan|task_dir_for|resolve_repo_add"
    - from: "src/arduis/window.py"
      to: "src/arduis/session.py:Task + RepoCheckout + default_repo_terminals"
      via: "store a Task per created task; one RepoCheckout per chosen repo"
      pattern: "Task\\(|RepoCheckout\\(|default_repo_terminals"
---

<objective>
Pivot `window.py` structurally from worktree to project/task: detect member repos at startup, show the project name in the topbar (name-only, no switcher — D-06), present a "New task" dialog (multi-repo pick + branch type-or-pick), and create a task by materializing the task folder (chosen repos as real worktrees, everything else relative-symlinked to root — D-08/D-09) then running a per-repo `git worktree add` chain off the GTK loop. The task workspace opens with one column per repo (agent+shell — D-01/D-02) and the sidebar lists tasks.

Purpose: This is the visible level-1 pivot — the user opens a multi-repo root and creates cross-repo tasks. Lifecycle (hibernate/RAM/cap/close-repo/startup-scan/teardown) is Plan 03.
Output: `src/arduis/window.py` with the model migrated to `Task`, the New-task flow, and the materializer. Hibernate/resume call sites are left compiling against `Task` but their full re-target is Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-RESEARCH.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-01-SUMMARY.md

<interfaces>
<!-- Plan 01 produced these. Wire against them directly. -->

From src/arduis/project.py:
```python
def detect_member_repos(root: str) -> list[str]   # sorted member dir names; [] = degenerate 1-repo
```
From src/arduis/task_layout.py:
```python
def task_dir_for(root: str, branch: str) -> str            # ../<root_base>-tasks/<sanitized-branch>
def repo_worktree_dir(task_dir: str, repo_name: str) -> str # <task_dir>/<repo_name>
def symlink_plan(root, task_dir, chosen_repos: set[str]) -> list[tuple[str,str]]  # (src_abs, dst); materialize with os.symlink(os.path.relpath(src, task_dir), dst)
def resolve_repo_add(repo_path, branch, existing_branches, parsed_worktrees, base, worktree_dir) -> tuple[str, ...]
    # ("new", argv) | ("existing", argv) | ("abort", reason) — NEVER --force (D-13)
```
From src/arduis/session.py (Plan 01):
```python
AGENT_FEED: bytes = b"claude\n"
class SessionState(str, Enum): ACTIVE; HIBERNATED
@dataclass class TerminalRecord: term_id; kind; pid; pgid; rss_kb; repo_name
def default_repo_terminals(task_id, repo_name) -> [agent {task}:{repo}:t0, shell {task}:{repo}:t1]
@dataclass class RepoCheckout: repo_name; worktree_dir; branch; terminals
@dataclass class Task: task_id; branch; task_dir; repos: list[RepoCheckout]; state
class SessionStore: add/get(task_id)/by_branch/all/to_list
def hibernate_fields(task: Task)   # clears every repo's terminals
```

From src/arduis/git_service.py (unchanged): `run_git_async(argv, on_done, runner)` — on_done(status, stdout, stderr) on the GLib loop.
From src/arduis/worktree.py (unchanged): `argv_default_branch_via_origin/local`, `parse_default_branch`, `argv_repo_has_commit`, `argv_list_local_branches`, `parse_local_branches`, `argv_worktree_list_porcelain`, `parse_worktrees`.
From src/arduis/layout.py (unchanged): `LayoutModel`, `LeafNode`, `SplitNode`; `model.split(a, b, "v"|"h")`, `model.touch`, `model.focused_id`.
From src/arduis/spawn.py (unchanged): `build_spawn_command(runner)`, `build_worktree_spawn(runner)`.
</interfaces>

<existing_window_map>
<!-- The exact window.py methods being re-targeted (line refs at plan time). Read the file first. -->
- `__init__` (170) — owns `self._store`, `self._layouts`, `self._repo_root`/`self._repo_name`, header with `_new_btn`. ADD: `self._member_repos: list[str]`, `self._project_root`, `self._project_name`; topbar title shows project name.
- `_resolve_repo_root` (863) — single `git rev-parse --show-toplevel`. REPLACE with project resolution: `members = detect_member_repos(os.getcwd())`; if members → project_root=cwd, member_repos=members; elif cwd is a git repo (existing rev-parse) → degenerate 1-repo project with member_repos=[basename]; else disable +.
- `_on_new_worktree_clicked`/`_begin_new_worktree`/`_present_new_worktree_dialog` (893/906/957) — RENAME concept to "New task"; dialog gains a repo multi-pick (Gtk.CheckButton per member repo; for a 1-repo project, auto-select the sole repo, no picker noise).
- `_create_worktree`/`_continue_create_worktree`/`_resolve_base_then_add`/`_open_and_add` (986/1005/1030/1048) — REPLACE with a per-repo CHAINED async sequence (RESEARCH Pattern 2): materialize task folder + symlinks FIRST, then loop repo-by-repo (porcelain pre-check → branches → base → resolve_repo_add → add → spawn that repo's 2 terminals), then finalize.
- `_open_and_add` builds the workspace LayoutModel — generalize to 2×N leaves, one column per repo (D-02).
- `_rebuild_sidebar` (559) — already iterates `self._store.all()`; rows are now Tasks (`task.task_id`/`task.branch`). Pinned row label becomes `<project> · zsh` (D-12). The hibernate/resume menu + RAM subline polish is Plan 03; keep rows rendering.
- `_make_wt_spawn_cb` (1195) / `_spawn_into` (1166) — write pid/pgid onto the matching TerminalRecord; now look up the record across `task.repos[*].terminals` by `term_id`.
- `_session_for_worktree_dir` (1373), `_menu_session` (1407), `_on_hibernate` (1413), `_on_resume` (1449), `_teardown_session_terminals` (1514), `_poll_ram` (1221) — these reference `WorktreeSession`. For THIS plan: make them COMPILE against `Task` (iterate `task.repos[*].terminals`) but their full behavioral re-target (per-task hibernate/resume default layout, per-task RAM sum, startup scan) is Plan 03. Minimal change here = keep them not crashing; Plan 03 owns correctness.
</existing_window_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project resolution + name-only topbar + New-task dialog (project scan, repo multi-pick)</name>
  <read_first>
    - src/arduis/window.py §`__init__` (170-278), `_resolve_repo_root` (863), `_on_new_worktree_clicked`→`_present_new_worktree_dialog` (893-983)
    - src/arduis/project.py (detect_member_repos)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md (D-05/D-06/D-07 discovery; Discretion: topbar widget strategy + New-task dialog design)
  </read_first>
  <action>
1. In `__init__`, add instance state: `self._project_root: str | None = None`, `self._project_name: str | None = None`, `self._member_repos: list[str] = []`. Keep `self._repo_root`/`self._repo_name` as aliases of project root/name for the pinned-leaf code that still reads them (set both).
2. Topbar (D-06, name-only, NO switcher): set the header title widget to show the project name. Replace `Adw.WindowTitle(title="arduis")` usage so that after resolution the title shows `self._project_name` (fall back to "arduis" when launched outside any repo). Use `header.set_title_widget(Adw.WindowTitle(title=<project_name or "arduis">))` and update it inside the resolution method via a stored reference `self._title_widget`.
3. Replace `_resolve_repo_root` with project resolution (keep the method name OR rename to `_resolve_project`; if renamed, update the `__init__` call site at line ~269):
```python
def _resolve_project(self) -> None:
    cwd = os.getcwd()
    members = detect_member_repos(cwd)          # D-05 direct-subdir .git scan
    if members:
        self._project_root = cwd
        self._project_name = os.path.basename(cwd.rstrip("/"))
        self._member_repos = members
    else:
        # D-07: cwd itself a git repo → degenerate 1-repo project (03.1 behavior)
        argv = self._runner.wrap_argv(["git", "-C", cwd, "rev-parse", "--show-toplevel"])
        try:
            proc = subprocess.run(argv, capture_output=True, text=True, timeout=5)
            top = proc.stdout.strip() if proc.returncode == 0 else ""
        except (OSError, subprocess.SubprocessError):
            top = ""
        if top:
            self._project_root = top
            self._project_name = os.path.basename(top)
            self._member_repos = [os.path.basename(top)]   # the sole member is the repo itself
        else:
            self._project_root = None; self._project_name = None; self._member_repos = []
    # keep the pinned-leaf aliases working
    self._repo_root = self._project_root
    self._repo_name = self._project_name
    enabled = self._project_root is not None
    self._new_btn.set_sensitive(enabled)
    self._new_btn.set_tooltip_text("Nova task" if enabled else _NO_REPO_HINT)
    if getattr(self, "_title_widget", None) is not None:
        self._title_widget.set_title(self._project_name or "arduis")
```
   NOTE the degenerate case: when `cwd` itself is a git repo, `member_repos = [basename]` but the member's PATH is the project root itself (not a subdir). Store member→path resolution so Task creation in Task 2 knows: a member name maps to `os.path.join(project_root, name)` UNLESS it equals project_name in the degenerate case, where it maps to `project_root`. Implement a helper `self._member_repo_path(name) -> str` returning `project_root` when `name == project_name and len(member_repos)==1 and not os.path.isdir(join(project_root,name))`, else `os.path.join(project_root, name)`.
4. Rename the dialog flow to "New task". In `_present_new_worktree_dialog` (rename to `_present_new_task_dialog`), keep the branch type-or-pick `Gtk.ComboBoxText.new_with_entry()` AND add a repo multi-pick: a `Gtk.CheckButton` per `self._member_repos` inside a vertical box, all checked by default. For a degenerate 1-repo project, still render the single check (checked, the user sees it) — no `len==1` special-case branch in the materialization path (criterion 5). Collect chosen repos = names whose CheckButton `get_active()`. On "Criar", require ≥1 repo chosen and a non-empty branch, then call `self._create_task(branch, chosen_repos, existing)` (Task 2).
5. Update `_begin_new_worktree`→`_begin_new_task` and `_on_new_worktree_clicked` to fetch branches from the PROJECT ROOT's primary repo for the type-or-pick list (use the first member repo's path via `_member_repo_path(member_repos[0])`), then present the dialog with the member repos + existing branches.
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -c "import ast,sys; ast.parse(open('src/arduis/window.py').read()); print('parse ok')" && /tmp/arduis-venv-ab12/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/arduis/window.py` contains `from arduis.project import detect_member_repos` (or `from arduis import project`) and calls `detect_member_repos`
    - `src/arduis/window.py` contains `self._member_repos` and `self._project_root`
    - `src/arduis/window.py` contains a `_member_repo_path` helper returning `project_root` for the degenerate single-repo case
    - The New-task dialog builds a `Gtk.CheckButton` per member repo (`grep -n "CheckButton" src/arduis/window.py` non-empty)
    - The header title is updated to the project name (`grep -n "_title_widget\|set_title" src/arduis/window.py` shows the project-name wiring)
    - `window.py` still parses (ast.parse ok) and the full pytest suite (GTK-free) still exits 0
  </acceptance_criteria>
  <done>Startup detects member repos, topbar shows the project name, the New-task dialog offers a repo multi-pick + branch type-or-pick; degenerate 1-repo resolves with no special-case branch.</done>
</task>

<task type="auto">
  <name>Task 2: Per-repo async create chain + relative-symlink task-folder materializer + 2×N workspace</name>
  <read_first>
    - src/arduis/window.py §`_create_worktree`→`_open_and_add` (986-1120), `_make_wt_spawn_cb`/`_spawn_into` (1166-1212), `_workspace_layout`/`_reflect_layout` (475/1270)
    - src/arduis/task_layout.py (task_dir_for, repo_worktree_dir, symlink_plan, resolve_repo_add)
    - src/arduis/session.py (Task, RepoCheckout, default_repo_terminals)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-RESEARCH.md §"Pattern 2/3/4" + §"Common Pitfalls" (Pitfalls 2/3/4/6)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md (D-01/D-02/D-08/D-09/D-13)
  </read_first>
  <action>
Replace the single-worktree create flow (`_create_worktree`/`_continue_create_worktree`/`_resolve_base_then_add`/`_open_and_add`) with a per-repo CHAINED async sequence (RESEARCH Pattern 2 — chained `run_git_async`, NEVER threads/asyncio):

1. `_create_task(self, branch, chosen_repos: list[str], existing_in_primary)`:
   - Compute `task_dir = task_dir_for(self._project_root, branch)`.
   - Create the task folder: `os.makedirs(task_dir, exist_ok=True)`.
   - Materialize symlinks FIRST (D-09, before any worktree add so relative compose/build contexts resolve — Pitfall 4): for `(src, dst) in symlink_plan(self._project_root, task_dir, set(chosen_repos))`: `os.symlink(os.path.relpath(src, task_dir), dst)` guarded by `if not os.path.lexists(dst)`. Wrap each in try/except OSError and collect failures into a list shown via `_show_error` at the end (do not abort the whole task on one symlink — best-effort, D-10/OQ2).
   - Build the `Task(task_id=branch, branch=branch, task_dir=task_dir, repos=[], state=ACTIVE)` and add it to the store NOW (so the row appears) — repos get appended as each succeeds.
   - Build the workspace LayoutModel with 2×N leaves, one COLUMN per repo (D-01/D-02): for each chosen repo, agent id `f"{branch}:{repo}:t0"` on top, shell id `f"{branch}:{repo}:t1"` below (vertical split within the column); columns joined horizontally. Use `model.split(..., "v")` within a column and `"h"` between columns. For a 1-repo task this is exactly the 03.1 side-by-side default (one column = agent over shell). Make the new task the visible workspace (`self._active_workspace_sid = branch`) and reflect.
   - Create the VTE leaves for all 2×N terminals up front (via `_make_terminal` + `_make_leaf`, badge "claude" for agents, "zsh" for shells), register in `_leaf_by_sid`/`_term_by_sid`.
   - Kick the per-repo chain: `self._add_repo(task, chosen_repos, 0, branch)`.
2. `_add_repo(self, task, repos, i, branch)` — one repo at a time:
   - If `i >= len(repos)`: finalize (rebuild sidebar, focus) and return.
   - `name = repos[i]`; `repo_path = self._member_repo_path(name)`; `wt_dir = repo_worktree_dir(task.task_dir, name)`.
   - Chain async (each callback launches the next git call for THIS repo, then advances to repo i+1):
     a. born-HEAD guard: `argv_repo_has_commit(repo_path)` — non-zero → record a per-repo abort message, skip to repo i+1.
     b. porcelain pre-check: `argv_worktree_list_porcelain(repo_path)` → `parse_worktrees(out)`.
     c. branch list: `argv_list_local_branches(repo_path)` → `parse_local_branches(out)`.
     d. base detection (only needed for the "new" case): `argv_default_branch_via_origin(repo_path)` → fallback `argv_default_branch_local` → `parse_default_branch`.
     e. `kind, payload = resolve_repo_add(repo_path, branch, existing, parsed, base, wt_dir)`.
        - "abort" → collect `payload` (reason) into a per-repo errors list; advance to i+1 WITHOUT spawning (best-effort, D-13/OQ2 — never --force).
        - else `run_git_async(payload, _add_done, runner)`: on status 0 → append `RepoCheckout(name, wt_dir, branch, default_repo_terminals(branch, name))` to `task.repos`, then `_spawn_repo_terminals(task, name, wt_dir)`; on non-zero → collect `err` into per-repo errors. Then advance to i+1.
   - After the last repo, if the per-repo errors list is non-empty, show ONE consolidated `_show_error("Algumas repos não foram criadas", "<joined reasons>")` (best-effort surfaced, D-10).
3. `_spawn_repo_terminals(self, task, repo_name, wt_dir)`: spawn the repo's agent (fed `AGENT_FEED`) + shell eagerly (D-03) via `_spawn_into`, passing the `Task` so the spawn callback can write pid/pgid onto the matching TerminalRecord.
4. Update `_spawn_into` / `_make_wt_spawn_cb` signature to accept a `Task` and find the TerminalRecord by `term_id` across `task.repos[*].terminals`:
```python
record = next((t for r in task.repos for t in r.terminals if t.term_id == term_id), None)
```
   (was: iterate `session.terminals`.) Keep the agent-feed-only-when-kind=="agent" + bytes `AGENT_FEED` behavior.
5. Delete the now-dead `_continue_create_worktree`/`_resolve_base_then_add`/`_open_and_add` once `_create_task`/`_add_repo` replace them; remove dangling references.
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read()); print('parse ok')" && /tmp/arduis-venv-ab12/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/arduis/window.py` contains `os.symlink(os.path.relpath(` (relative symlink materialization, A1/Pattern 3)
    - `src/arduis/window.py` materializes symlinks via `symlink_plan(` BEFORE the worktree-add chain (`grep -n "symlink_plan" src/arduis/window.py` appears earlier in `_create_task` than `_add_repo`'s `run_git_async`)
    - `src/arduis/window.py` contains `def _add_repo` and `def _spawn_repo_terminals` (per-repo chained sequence)
    - `src/arduis/window.py` contains `resolve_repo_add(` and never adds `--force` (`grep -c "force" src/arduis/window.py` shows no `--force` git argv)
    - `src/arduis/window.py` constructs `Task(` and `RepoCheckout(` and calls `default_repo_terminals(`
    - The spawn callback finds the record across `task.repos` (`grep -n "for r in.*repos.*for t in" src/arduis/window.py` non-empty)
    - The dead `_open_and_add`/`_resolve_base_then_add`/`_continue_create_worktree` are removed (`grep -c "_open_and_add\|_resolve_base_then_add\|_continue_create_worktree" src/arduis/window.py` == 0)
    - `window.py` parses and the full GTK-free pytest suite exits 0
  </acceptance_criteria>
  <done>Creating a task materializes the mirrored task folder (relative symlinks, chosen repos as worktrees), runs a per-repo chained worktree-add, spawns 2 terminals per repo in one-column-per-repo layout, and best-effort-surfaces per-repo aborts without --force.</done>
</task>

<task type="auto">
  <name>Task 3: Task-keyed sidebar + pinned project row + spawn record migration; compile the lifecycle seams against Task</name>
  <read_first>
    - src/arduis/window.py §`_rebuild_sidebar`/`_make_row`/`_swap_workspace` (559-688), `_session_for_worktree_dir`/`_menu_session`/`_on_hibernate`/`_on_resume`/`_teardown_session_terminals`/`_poll_ram` (1221-1525)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md (D-12 pinned project row, D-14 cap counts tasks)
  </read_first>
  <action>
1. `_rebuild_sidebar`: rows now iterate `self._store.all()` as Tasks. Each row uses `task.task_id` as the sid and `task.branch` as the label. The pinned main row label becomes `self._project_name or "main"` with subline `<project> · zsh` (D-12) — the pinned leaf is ONE terminal at the project root (it already opens at `self._repo_root` via `_open_shell_leaf`, which is the project root — keep that). The hibernate/resume context-menu still attaches to non-main rows.
2. Migrate the lifecycle seams to COMPILE and not crash against `Task` (full behavioral correctness is Plan 03 — this task only ensures the refactor type-checks and basic create/swap works):
   - `_session_for_worktree_dir(path)`: match `path` against any `task.repos[*].worktree_dir` and return the owning `Task`.
   - `_menu_session()`: `self._store.get(self._menu_target_sid)` already returns a Task.
   - `_teardown_session_terminals(task)`: iterate `for r in task.repos: for t in r.terminals: if t.pid: self._teardown_pgid(t.pid)` (every repo's every terminal — Pitfall 3).
   - `_poll_ram`: sum `t.rss_kb` across `task.repos[*].terminals` instead of `session.terminals` (the per-repo grouping refinement is Plan 03; here just make it iterate the new shape without crashing).
   - `_on_hibernate`/`_on_resume`: leave the bodies referencing `Task` so they compile; the prefix-based widget cleanup uses `f"{sid}:"` which still works because terminal ids are `{task}:{repo}:tN` (prefix `{task}:` still matches). The default-layout resume rebuild (one column per repo) is Plan 03 — for now resume MAY rebuild a minimal valid layout; mark with a `# Plan 03: re-target per-repo default` comment. (Do NOT leave `_on_resume` referencing the removed `default_terminals`/`WorktreeSession`.)
3. Update the `_prompt_hibernate_then` cap-gate text and chooser to list `task.branch` for active Tasks (it already reads `self._store.all()` and `caps.at_cap` — confirm it compiles against Task; `caps` needs no change per Plan 01).
4. Run the app headlessly to confirm it starts and the GTK-free suite stays green. Manual GTK verification of the full create flow is Plan 03's UAT.
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read()); print('parse ok')"; test "$(grep -c 'WorktreeSession' src/arduis/window.py)" = 0 && /tmp/arduis-venv-ab12/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/arduis/window.py` no longer references `WorktreeSession` (`grep -c "WorktreeSession" src/arduis/window.py` == 0)
    - `src/arduis/window.py` no longer calls the removed `default_terminals(` (only `default_repo_terminals(`): `grep -c "default_terminals(" src/arduis/window.py` == 0
    - `_teardown_session_terminals` iterates `task.repos` (`grep -n "for r in" src/arduis/window.py` near the teardown body)
    - `_poll_ram` sums across `repos` (`grep -n "repos" src/arduis/window.py` appears in `_poll_ram`)
    - The pinned row subline contains the project name (`grep -n "zsh" src/arduis/window.py` shows the `<project> · zsh` pattern)
    - `window.py` parses and the full GTK-free pytest suite exits 0
  </acceptance_criteria>
  <done>Sidebar rows are Tasks, the pinned row is `<project> · zsh`, every lifecycle seam compiles against `Task` (iterating all repos' terminals); no dangling `WorktreeSession`/`default_terminals` references. Behavioral correctness of hibernate/resume/RAM/scan is Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed branch → task folder path + symlink targets | branch becomes a dir leaf; symlinks created under it |
| user-typed branch → per-repo git worktree-add argv | branch is a discrete git argument per repo |
| chosen repo names (from disk scan + checkboxes) → git `-C <path>` | member dir names feed git invocations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-05 | Tampering/EoP | `os.symlink` target escaping the task folder (V12) | mitigate | Materializer only consumes `symlink_plan` pairs (top-level root entries → positions inside `task_dir`) and uses `os.path.relpath(src, task_dir)` relative targets guarded by `os.path.lexists`. No user input becomes a symlink target. |
| T-03.2-06 | Tampering | branch → task-dir leaf path traversal | mitigate | `task_dir_for` (Plan 01) reuses `sanitize_branch_for_dir`; window.py never builds the leaf from raw input. |
| T-03.2-07 | Tampering/EoP | per-repo `git worktree add` clobbering a branch checked out elsewhere | mitigate | `resolve_repo_add` returns `("abort", reason)` when checked out elsewhere; NEVER `--force` (D-13). window.py advances to the next repo (best-effort) and surfaces the reason. |
| T-03.2-08 | DoS / resource | partial task creation leaving half-materialized folder | accept | D-10 forbids arduis deleting from disk; best-effort keeps succeeded repos, surfaces a consolidated error, lets the user clean up manually. Documented, not auto-rolled-back. |
| T-03.2-09 | Tampering | malicious scanned dir name in argv | mitigate | dir names land only as discrete `-C <path>` / path list elements via the GTK-free builders; no shell, no string interpolation. |
</threat_model>

<verification>
- `window.py` parses: `/tmp/arduis-venv-ab12/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read())"`
- No dangling old-model references: `grep -c "WorktreeSession" src/arduis/window.py` == 0; `grep -c "_open_and_add\|_resolve_base_then_add\|_continue_create_worktree" src/arduis/window.py` == 0.
- Symlinks are relative: `grep -n "os.path.relpath" src/arduis/window.py` present in the materializer.
- Full GTK-free suite green: `/tmp/arduis-venv-ab12/bin/python -m pytest -q` exits 0 (GTK widget behavior is verified manually in Plan 03's UAT).
- Headless smoke (optional, MEMORY: gtk4-broadwayd): app constructs the window without crashing in a degenerate 1-repo cwd.
</verification>

<success_criteria>
- Startup detects member repos (multi-repo root) or resolves the degenerate 1-repo project; topbar shows the project name (no switcher).
- "New task" picks 1+ repos + a branch; the task folder is materialized (chosen repos as worktrees, everything else relative-symlinked, meta `.git` excluded); per-repo worktree-add runs as a chained async sequence; 2 terminals per repo spawn one-column-per-repo.
- A repo checked out elsewhere aborts with a clear message; succeeded repos are kept (best-effort, never --force).
- The model is fully migrated to `Task`; all lifecycle seams compile against it (full behavioral re-target is Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-02-SUMMARY.md` documenting: the project-resolution + degenerate-1-repo handling, the `_member_repo_path` mapping, the per-repo chained create sequence + symlink materializer, the spawn-record migration, and exactly which lifecycle seams are "compiles-only" pending Plan 03 (hibernate/resume default-layout, per-task RAM grouping, startup scan).
</output>
