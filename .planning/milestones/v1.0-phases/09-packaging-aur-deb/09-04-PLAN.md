---
phase: 09-packaging-aur-deb
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - PKGBUILD
  - README.md
autonomous: true
requirements: [DIST-02, DIST-04]
user_setup: []

must_haves:
  truths:
    - "PKGBUILD parses (bash -n) and declares arch=('any') + the 5 CLAUDE.md Arch depends + makedepends=(meson)"
    - "PKGBUILD wraps meson via arch-meson/meson compile/meson install --destdir with NO .install scriptlet"
    - "pkgver is 1.0.0, maintainer comment is thallys <thallys.costa@livon.io>, url is the GitHub repo"
    - "README documents how to build/install on both Ubuntu (.deb) and Arch (AUR)"
  artifacts:
    - path: "PKGBUILD"
      provides: "Arch native package definition wrapping meson"
      contains: "arch=('any')"
    - path: "README.md"
      provides: "Packaging/install section for Ubuntu + Arch (D-06)"
      contains: "makepkg"
  key_links:
    - from: "PKGBUILD"
      to: "meson.build"
      via: "arch-meson + meson install --destdir $pkgdir"
      pattern: "arch-meson"
    - from: "PKGBUILD"
      to: "system vte4/gtk4/libadwaita/python-gobject"
      via: "depends array"
      pattern: "vte4"
---

<objective>
Author the Arch `PKGBUILD` (DIST-02) wrapping the Plan-02 meson def via
`arch-meson`/`meson compile`/`meson install --destdir "$pkgdir"`, `arch=('any')` (pure Python),
`depends=('python-gobject' 'gtk4' 'libadwaita' 'vte4' 'python')`, `makedepends=('meson')`, `pkgver=1.0.0`
(D-03), maintainer comment `thallys <thallys.costa@livon.io>` (D-04), and NO `.install` scriptlet
(RESEARCH Pitfall 3 — pacman hooks refresh caches). Write the README packaging section (D-06)
documenting build/install on both distros.

Purpose: The Arch channel (DIST-02) — `makepkg -si` is the team-install story for Arch. The real
build runs only on an Arch box (no makepkg/pacman on the dev host) — that half is the live UAT
(Plan 05); here we make the definition parse-clean and declare the right deps, and ship the README.
Output: `PKGBUILD`, `README.md` packaging section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-packaging-aur-deb/09-CONTEXT.md
@.planning/phases/09-packaging-aur-deb/09-RESEARCH.md
@.planning/phases/09-packaging-aur-deb/09-VALIDATION.md
@.planning/phases/09-packaging-aur-deb/09-02-SUMMARY.md

<interfaces>
<!-- CLAUDE.md §Packaging is AUTHORITATIVE for the Arch dep list. -->
Arch depends: python-gobject, gtk4, libadwaita, vte4, python
makedepends: meson  (pulls ninja)
arch=('any')  (pure Python — RESEARCH); license=('MIT'); url=https://github.com/thallysrc/arduis

RESEARCH Pattern 5 — canonical meson PKGBUILD:
```bash
# Maintainer: thallys <thallys.costa@livon.io>
pkgname=arduis; pkgver=1.0.0; pkgrel=1
pkgdesc="Orquestra agentes de IA em paralelo sobre git worktrees"
arch=('any'); url="https://github.com/thallysrc/arduis"; license=('MIT')
depends=('python-gobject' 'gtk4' 'libadwaita' 'vte4' 'python')
makedepends=('meson')
source=(...)   # local: produced via `git archive` until a GitHub release exists (D-06, Open Q1)
build()   { arch-meson "$pkgname-$pkgver" build; meson compile -C build; }
package() { meson install -C build --destdir "$pkgdir"; }
# NO .install scriptlet (Pitfall 3)
```
RESEARCH Open Q1: source=("$pkgname-$pkgver.tar.gz") documented as produced from `git archive`
locally until a published GitHub release tarball exists (publish is deferred to v1.1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PKGBUILD (arch=any, meson wrap, exact deps, no .install)</name>
  <read_first>
    - 09-RESEARCH.md Pattern 5 (PKGBUILD) + Pitfall 3 (no .install) + Open Q1 (source under D-06) + Assumption A1 (vte4 0.84)
    - CLAUDE.md §Packaging (authoritative Arch dep list) + §What NOT to Use
    - 09-CONTEXT.md D-03 (pkgver 1.0.0) + D-04 (maintainer/URL) + D-06 (definitions only)
  </read_first>
  <files>
    - PKGBUILD
  </files>
  <action>
    1. Write `PKGBUILD` per Pattern 5: `# Maintainer: thallys <thallys.costa@livon.io>` comment,
       `pkgname=arduis`, `pkgver=1.0.0`, `pkgrel=1`, pkgdesc, `arch=('any')`, url, `license=('MIT')`,
       `depends=('python-gobject' 'gtk4' 'libadwaita' 'vte4' 'python')`, `makedepends=('meson')`.
    2. `source=("$pkgname-$pkgver.tar.gz")` with a comment that it is produced LOCALLY via
       `git archive --format=tar.gz --prefix=arduis-1.0.0/ -o arduis-1.0.0.tar.gz HEAD` until a
       published GitHub release exists (D-06 / Open Q1). Set `sha256sums=('SKIP')` (local tarball).
    3. `build()` → `arch-meson "$pkgname-$pkgver" build; meson compile -C build`.
       `package()` → `meson install -C build --destdir "$pkgdir"`. Optional `check()` → `meson test
       -C build` (no-op skip if no tests). NO `.install=` line (Pitfall 3 — pacman hooks refresh
       icon/desktop caches; an `.install` would be namcap-flagged).
    Do NOT use `arch=('x86_64')`, no pip, no Snap, no VTE bundling. The actual `makepkg -si` build +
    Wayland launch is the Arch hardware UAT (Plan 05) — cannot run on this Ubuntu host.
  </action>
  <verify>
    <automated>bash -n PKGBUILD && grep -q "arch=('any')" PKGBUILD && grep -q "depends=('python-gobject' 'gtk4' 'libadwaita' 'vte4' 'python')" PKGBUILD && grep -q "makedepends=('meson')" PKGBUILD && grep -q "pkgver=1.0.0" PKGBUILD && grep -q "thallys.costa@livon.io" PKGBUILD && grep -q "arch-meson" PKGBUILD && grep -q 'meson install -C build --destdir' PKGBUILD && ! grep -q "^install=" PKGBUILD</automated>
  </verify>
  <done>`bash -n PKGBUILD` parses clean; PKGBUILD declares arch=('any'), the 5 exact depends, makedepends=(meson), pkgver 1.0.0, maintainer email, the arch-meson/install meson wrap, and ships no `.install`.</done>
</task>

<task type="auto">
  <name>Task 2: README packaging/install section (D-06)</name>
  <read_first>
    - 09-CONTEXT.md D-06 (README packaging section) + §specifics ("instalável facilmente por um time")
    - 09-RESEARCH.md §Installation (the build commands per distro)
  </read_first>
  <files>
    - README.md
  </files>
  <action>
    Create `README.md` (none exists). Include: a one-paragraph project intro (from CLAUDE.md Core
    Value), then a "Packaging / Install" section with two subsections:
    - **Ubuntu (.deb):** build deps (`sudo apt install meson ninja-build debhelper`), build
      (`dpkg-buildpackage -us -uc -b` from repo root → `../arduis_1.0.0_all.deb`), install
      (`sudo apt install ./arduis_1.0.0_all.deb`), runtime deps note (system VTE 0.76, no pip/no
      Flatpak in v1).
    - **Arch (AUR):** `git archive` to produce the source tarball, then `makepkg -si` from the
      PKGBUILD (depends on system `vte4` 0.84 etc.).
    - A **Dev** note: `run.sh` / `python3 src/main.py` still works for development (unchanged).
    Mention publishing (AUR push / apt repo) is deferred to v1.1 (D-06). Keep it concise and accurate
    to the commands the plans actually produce.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "dpkg-buildpackage" README.md && grep -q "makepkg" README.md && grep -q "apt install" README.md && grep -qi "arch" README.md && grep -qi "ubuntu" README.md</automated>
  </verify>
  <done>README.md exists with a Packaging/Install section covering both the Ubuntu `.deb` (dpkg-buildpackage + apt install) and Arch AUR (`makepkg -si`) flows plus the dev `run.sh` note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PKGBUILD build()/package() → root at install | makepkg runs build as user, pacman installs as root |
| source=() → supply chain | where the build pulls source from |
| declared depends → supply chain | the runtime depends ARE the trusted set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Tampering | PKGBUILD `source=()` | mitigate | Source is a LOCAL `git archive` tarball of the repo (not a remote URL that could be swapped); `sha256sums=('SKIP')` only because it is locally produced — documented; no network fetch in build() |
| T-09-11 | Elevation of privilege | PKGBUILD build()/package() + absence of .install | mitigate | build()/package() are the canonical arch-meson/meson lines — no curl\|sh, no eval; NO `.install` scriptlet runs as root at install (pacman hooks handle caches) |
| T-09-12 | Tampering | declared `depends` (supply chain) | mitigate | List ONLY the CLAUDE.md Arch system deps; never a pip wheel; arch=('any') (no native objects to trust) |
</threat_model>

<verification>
- `bash -n PKGBUILD` parses; PKGBUILD declares arch=('any'), the 5 exact depends, makedepends=(meson),
  pkgver 1.0.0, maintainer (D-04), the arch-meson/meson-install wrap, NO `.install`.
- README has the Ubuntu `.deb` + Arch AUR + dev flows.
- Full regression pytest stays green; `run.sh` dev flow unaffected.
- (Real `makepkg -si` + Wayland launch on Arch is deferred to the Plan-05 hardware UAT.)
</verification>

<success_criteria>
- Arch PKGBUILD is parse-clean, wraps meson, declares the exact CLAUDE.md deps, ships no `.install`.
- README documents team-install on both distros (D-06).
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-aur-deb/09-04-SUMMARY.md`
</output>
