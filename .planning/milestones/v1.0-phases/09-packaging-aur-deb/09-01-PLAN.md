---
phase: 09-packaging-aur-deb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/io.github.thallys.Arduis.metainfo.xml
  - data/io.github.thallys.Arduis.desktop
  - data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg
  - tests/test_packaging_native_noop.py
  - tests/test_packaging_version.py
  - tests/test_packaging_install_tree.py
autonomous: true
requirements: [DIST-02, DIST-03, DIST-04]
user_setup: []

must_haves:
  truths:
    - "appstreamcli validate passes on the metainfo (homepage url + 1.0.0 release present)"
    - "A scalable SVG icon exists at the hicolor path the .desktop/.metainfo reference"
    - "Criterion-4 test proves HostRunner is a native no-op (identity wrap_argv, _FLATPAK is False, no live flatpak-spawn)"
    - "Build/lint tooling (meson, ninja-build, debhelper, lintian, desktop-file-utils, appstream) is installed so later build gates can run"
  artifacts:
    - path: "data/io.github.thallys.Arduis.metainfo.xml"
      provides: "AppStream metainfo with homepage url + <release version=1.0.0>"
      contains: "release version=\"1.0.0\""
    - path: "data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg"
      provides: "Scalable app icon (dracula palette)"
      min_lines: 5
    - path: "tests/test_packaging_native_noop.py"
      provides: "Criterion-4 HostRunner no-op assertions"
    - path: "tests/test_packaging_version.py"
      provides: "metainfo <release> == project version 1.0.0 assertion"
    - path: "tests/test_packaging_install_tree.py"
      provides: "meson DESTDIR install-tree + importlib.resources hook smoke (skips if meson absent)"
  key_links:
    - from: "data/io.github.thallys.Arduis.metainfo.xml"
      to: "appstreamcli validate"
      via: "homepage url + release element"
      pattern: "url type=\"homepage\""
    - from: "tests/test_packaging_native_noop.py"
      to: "src/arduis/host_runner.py"
      via: "import + wrap_argv identity assertion"
      pattern: "wrap_argv"
---

<objective>
Wave-0 foundation for Phase 9 packaging: install the build/lint toolchain that is absent on
the dev host, fix the two metainfo defects that currently FAIL `appstreamcli validate`, generate
the placeholder SVG icon (D-05), and write the three packaging test files that the meson and
package plans verify against. Nothing here changes `src/arduis/` runtime behavior — `run.sh` and
the dev flow stay green.

Purpose: The downstream meson (Plan 02), `.deb` (Plan 03), and PKGBUILD (Plan 04) plans cannot
have green lint/build gates until the metainfo validates, the icon exists, and the tools are
installed. This plan removes those blockers first.
Output: Validated metainfo, a scalable icon at the hicolor path, three packaging test files, an
installed build toolchain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-packaging-aur-deb/09-CONTEXT.md
@.planning/phases/09-packaging-aur-deb/09-RESEARCH.md
@.planning/phases/09-packaging-aur-deb/09-VALIDATION.md

<interfaces>
<!-- Existing assets the executor edits/references. Use these directly — no exploration needed. -->

Current data/io.github.thallys.Arduis.metainfo.xml (FAILS appstreamcli: url-homepage-missing + no <release>):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.thallys.Arduis</id>
  <name>arduis</name>
  <summary>Orquestra agentes de IA em paralelo sobre git worktrees</summary>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>MIT</project_license>
  <developer id="io.github.thallys"><name>Thallys</name></developer>
  <description>...</description>
  <launchable type="desktop-id">io.github.thallys.Arduis.desktop</launchable>
  <content_rating type="oars-1.1"/>
</component>
```

Current data/io.github.thallys.Arduis.desktop (passes validate WITH a hint — multiple main categories):
```ini
[Desktop Entry]
Name=arduis
Exec=arduis
Icon=io.github.thallys.Arduis
Categories=Development;Utility;
```

src/arduis/host_runner.py (Criterion-4 target — already a native no-op):
```python
_FLATPAK = False
class HostRunner:
    def wrap_argv(self, argv: list[str]) -> list[str]:
        if _FLATPAK:  # v2 path, dead
            ...
        return list(argv)   # identity on native
```

src/arduis/attention.py:324 (importlib.resources hook load — the smoke target):
    files("arduis.hooks").joinpath("arduis_hook.py").read_text(encoding="utf-8")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install build/lint toolchain + generate the SVG icon (D-05)</name>
  <read_first>
    - 09-RESEARCH.md §Environment Availability (the apt-install list + which tools are missing)
    - 09-CONTEXT.md D-05 (icon spec: geometric, dracula palette, hicolor scalable path)
    - 09-RESEARCH.md Pitfall 3 (NO .install scriptlet — pacman hooks/dh triggers refresh caches)
  </read_first>
  <files>
    - data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg
  </files>
  <action>
    1. Install the Ubuntu build/lint tools that the research probe found ABSENT on the dev host
       (so Plans 02–04 build/lint gates can run):
       `sudo apt install -y meson ninja-build debhelper lintian desktop-file-utils appstream`
       (`desktop-file-validate` and `appstreamcli` may already be present — the meta packages are
       `desktop-file-utils` and `appstream`; re-installing is idempotent.) If `sudo` triggers a
       permission/auth prompt the executor cannot satisfy, surface it as an auth gate rather than
       failing the task.
    2. Generate `data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg` — a simple
       hand-authored geometric SVG in the Dracula palette (background #282a36, accents #bd93f9 /
       #50fa7b / #ff79c6) evoking embedded-terminal + parallel-worktrees (e.g. stacked rounded
       rects = parallel panes with a `>` prompt glyph). 256x256 viewBox. Pure SVG, no raster, no
       external refs. This is the D-05 placeholder, replaceable later.
    Do NOT add an Arch `.install` scriptlet (RESEARCH Pitfall 3 overrides the CONTEXT D-05 mention
    of one — pacman hooks + dh triggers refresh icon/desktop caches automatically).
  </action>
  <verify>
    <automated>test -f data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg && head -1 data/icons/hicolor/scalable/apps/io.github.thallys.Arduis.svg | grep -q "svg\|xml" && command -v meson && command -v lintian && command -v appstreamcli && command -v desktop-file-validate</automated>
  </verify>
  <done>The SVG exists at the hicolor scalable path and is valid XML; `meson`, `lintian`, `appstreamcli`, `desktop-file-validate` all resolve on PATH.</done>
</task>

<task type="auto">
  <name>Task 2: Fix metainfo (homepage url + 1.0.0 release) so appstreamcli validate passes (D-03/Pitfall 4)</name>
  <read_first>
    - 09-RESEARCH.md Pitfall 4 (exact failures: url-homepage-missing + no <release>)
    - 09-CONTEXT.md D-03 (version 1.0.0) + D-04 (upstream URL https://github.com/thallysrc/arduis)
  </read_first>
  <files>
    - data/io.github.thallys.Arduis.metainfo.xml
    - data/io.github.thallys.Arduis.desktop
  </files>
  <action>
    1. In the metainfo, add `<url type="homepage">https://github.com/thallysrc/arduis</url>`
       (D-04 upstream URL) and a `<releases><release version="1.0.0" date="2026-06-15"/></releases>`
       block (D-03 single-source version — the meson `project()` version will be asserted equal to
       this in Task 4). Keep `id`, `name`, `summary`, licenses, developer, description, launchable,
       content_rating unchanged.
    2. In the `.desktop` (optional, addresses Pitfall 5 hint): reduce to a single main category —
       `Categories=Development;` — to avoid the duplicate-menu-entry hint. Keep Keywords. This is a
       hint not an error, but cleaning it makes the desktop lint fully clean.
  </action>
  <verify>
    <automated>appstreamcli validate data/io.github.thallys.Arduis.metainfo.xml && desktop-file-validate data/io.github.thallys.Arduis.desktop && grep -q 'release version="1.0.0"' data/io.github.thallys.Arduis.metainfo.xml && grep -q 'url type="homepage"' data/io.github.thallys.Arduis.metainfo.xml</automated>
  </verify>
  <done>`appstreamcli validate` exits 0 (no warnings); `desktop-file-validate` exits 0; metainfo contains the homepage url and the 1.0.0 release element.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the three packaging test files (native no-op, version, install-tree)</name>
  <read_first>
    - 09-RESEARCH.md §Validation Architecture → Wave 0 Gaps + the Criterion-4 grep test example
    - 09-VALIDATION.md Per-Task Verification Map (SC-4 grep, D-03 version, D-01/D-02 install tree)
    - src/arduis/host_runner.py (the interface block above — `_FLATPAK`, `wrap_argv`)
  </read_first>
  <files>
    - tests/test_packaging_native_noop.py
    - tests/test_packaging_version.py
    - tests/test_packaging_install_tree.py
  </files>
  <behavior>
    - native_noop: `host_runner._FLATPAK is False`; `HostRunner().wrap_argv(["zsh","-l","-i"]) == ["zsh","-l","-i"]`; a repo-grep test asserts NO LIVE `flatpak-spawn` token in argv across `src/` (allow comment/docstring/`NotImplementedError`-branch lines — match the line, then exclude lines that are comments or string literals).
    - version: parse `data/io.github.thallys.Arduis.metainfo.xml`, assert exactly one `<release>` with `version="1.0.0"`. (After Plan 02 lands `meson.build`, extend/assert it equals the meson `project(... version: ...)` string parsed from `meson.build`; until then, guard the meson.build read with `pytest.skip` if the file is absent so this test is green NOW and tightens after Plan 02.)
    - install_tree: run `meson setup` + `meson install --destdir <tmp>`; assert `<stage>/usr/bin/arduis` is an executable file, the SVG installed under the hicolor path exists, and `PYTHONPATH=<stage purelib> python3 -c "import arduis.hooks, importlib.resources; importlib.resources.files('arduis.hooks').joinpath('arduis_hook.py').read_text()"` succeeds. **Guard the whole test with `pytest.skip` when `meson.build` does not yet exist** (it lands in Plan 02) so it is a no-op skip in this plan and becomes the real install-tree gate once Plan 02 runs.
  </behavior>
  <action>
    Create the three pytest files implementing the behaviors above. The grep-style test for
    flatpak-spawn should walk `src/arduis/**/*.py`, collect lines containing `flatpak-spawn`, and
    fail only if a matching line is NOT a comment (`#`) and NOT inside a string/docstring and NOT in
    the dead `if _FLATPAK:` branch — practically: assert every match is on a `#`-comment or string
    line (research confirms only docstring/comment + guarded-branch mentions exist). Keep tests fast
    (<5s) and skip-guarded so the suite stays green before Plan 02 exists.
  </action>
  <verify>
    <automated>/tmp/arduis-venv/bin/python -m pytest -q tests/test_packaging_native_noop.py tests/test_packaging_version.py tests/test_packaging_install_tree.py</automated>
  </verify>
  <done>All three test files pass (install_tree + the meson-equality half of version skip cleanly because `meson.build` does not exist yet); native_noop and the metainfo-release half of version assert green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata → public registries | metainfo/desktop ship maintainer email + URLs that become public on publish |
| dev host shell ← apt install | installing build tooling runs apt as root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information disclosure | metainfo homepage url + maintainer identity | accept | Public URL + work email are intentional per D-04 (PO-accepted); no secrets in metadata |
| T-09-02 | Tampering | `apt install` of build tools | mitigate | Install ONLY from distro repos (no PPA, no curl\|sh); exact package list from RESEARCH; idempotent |
| T-09-03 | Tampering | generated SVG icon | mitigate | Hand-authored static SVG — no external refs, no script elements, no embedded raster/network fetch |
</threat_model>

<verification>
- `appstreamcli validate` exits 0 on the metainfo (homepage + 1.0.0 release present).
- `desktop-file-validate` exits 0 (single main category).
- SVG icon exists at the hicolor scalable path and is valid XML.
- `meson`, `ninja-build`, `debhelper`, `lintian`, `desktop-file-validate`, `appstreamcli` resolve.
- `pytest -q tests/test_packaging_*.py` green (install_tree + meson-version half skip until Plan 02).
- Full regression: `/tmp/arduis-venv/bin/python -m pytest -q` stays green (no src behavior change).
</verification>

<success_criteria>
- Metainfo validates clean; icon exists; toolchain installed; three packaging test files green/skip-guarded.
- `run.sh` dev flow unaffected; `main` still launchable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-aur-deb/09-01-SUMMARY.md`
</output>
