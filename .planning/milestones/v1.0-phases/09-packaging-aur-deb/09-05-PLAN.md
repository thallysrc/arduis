---
phase: 09-packaging-aur-deb
plan: 05
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified: []
autonomous: false
requirements: [DIST-02, DIST-03, DIST-04]
user_setup: []

must_haves:
  truths:
    - "Before the human gate: full pytest green, .deb builds + lints, metainfo + desktop validate, PKGBUILD parses, HostRunner no-op confirmed"
    - "A clean .deb install launches arduis under real Wayland on Ubuntu 24.04 with a working embedded VTE shell"
    - "A makepkg -si install launches arduis under real Wayland on Arch with a working embedded VTE shell"
    - "The arduis icon shows in the app grid on both distros (not a generic placeholder)"
  artifacts: []
  key_links:
    - from: ".deb / PKGBUILD"
      to: "real Ubuntu + Arch Wayland session"
      via: "clean install + launch + embedded VTE works"
      pattern: "manual hardware gate"
---

<objective>
Final v1 acceptance: the DIST-04 hardware gate. First re-run the full automated gate (everything
the prior plans produced), then hand the PO the clean-install-and-launch-under-real-Wayland checks
on BOTH Ubuntu 24.04 and Arch — the irreducibly manual half (no makepkg/pacman on the dev host;
Wayland launch + VTE rendering need real hardware). This closes Phase 9 and the v1 milestone.

Purpose: SC-3 ("clean install runs on both Ubuntu and Arch under real Wayland") is a hard gate that
no command can satisfy — it requires the PO on real machines, matching prior phases' live UATs.
Output: PO sign-off on the two install/launch flows + icon visibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-packaging-aur-deb/09-CONTEXT.md
@.planning/phases/09-packaging-aur-deb/09-VALIDATION.md
@.planning/phases/09-packaging-aur-deb/09-03-SUMMARY.md
@.planning/phases/09-packaging-aur-deb/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the full automated phase gate</name>
  <read_first>
    - 09-VALIDATION.md §Validation Sign-Off + Per-Task Verification Map
    - 09-RESEARCH.md Criterion-4 grep test + §Sampling Rate (phase gate)
  </read_first>
  <files></files>
  <action>
    Run, from repo root, the complete automated gate and confirm each is green BEFORE handing to the
    PO. Capture the results into the SUMMARY:
    1. `/tmp/arduis-venv/bin/python -m pytest -q` — full suite green (includes the three packaging
       tests; no regression from packaging files).
    2. `appstreamcli validate data/io.github.thallys.Arduis.metainfo.xml` — clean.
    3. `desktop-file-validate data/io.github.thallys.Arduis.desktop` — clean.
    4. `meson setup /tmp/g && meson install -C /tmp/g --destdir /tmp/gstage` — staged tree has
       `bin/arduis`, the SVG, purelib `arduis/hooks/arduis_hook.py`; importlib.resources import works.
    5. `dpkg-buildpackage -us -uc -b && lintian ../arduis_1.0.0_all.deb` — builds; no ERROR tags.
    6. `bash -n PKGBUILD` — parses.
    7. SC-4 grep: no LIVE `flatpak-spawn` in argv (`grep -rn 'flatpak-spawn' src/` → only
       comment/string lines) + `tests/test_packaging_native_noop.py` green.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis && /tmp/arduis-venv/bin/python -m pytest -q && appstreamcli validate data/io.github.thallys.Arduis.metainfo.xml && desktop-file-validate data/io.github.thallys.Arduis.desktop && bash -n PKGBUILD && dpkg-buildpackage -us -uc -b && lintian ../arduis_1.0.0_all.deb; test $? -lt 2</automated>
  </verify>
  <done>Full pytest green; metainfo + desktop validate clean; `.deb` builds + lints (no errors); PKGBUILD parses; HostRunner no-op confirmed. The automated half of SC-1/SC-2/SC-4 is satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hardware gate — clean install + Wayland launch on real Ubuntu + Arch (DIST-04 / SC-3)</name>
  <what-built>
    The complete Phase-9 packaging set, with the automated gate green: meson build def, generated
    `bin/arduis` launcher (purelib package, hook ships as data), validated metainfo + SVG icon, a
    locally-built `arduis_1.0.0_all.deb` (lints clean), and a parse-clean `PKGBUILD`. README documents
    both install flows.
  </what-built>
  <action>
    HUMAN VERIFICATION (no automation possible — needs real hardware + Wayland sessions). Hand the PO
    the install/launch checks below for both distros; this cannot run on the dev host (no
    makepkg/pacman; Wayland launch + VTE rendering need real machines).
  </action>
  <how-to-verify>
    **Ubuntu 24.04 (real Wayland session):**
    1. Copy `../arduis_1.0.0_all.deb` to a clean Ubuntu 24.04 box (or confirm no dev `src/` on PATH).
    2. `sudo apt install ./arduis_1.0.0_all.deb` — installs without dependency errors (pulls
       gir1.2-vte-3.91, python3-gi, gtk4, adw if missing).
    3. Confirm you are in a **Wayland** session (`echo $XDG_SESSION_TYPE` → `wayland`).
    4. Open the GNOME app grid → the **arduis icon** appears (not a generic placeholder).
    5. Launch arduis from the grid → a GTK4 window opens with an embedded VTE terminal running your
       host `zsh`; type a command (e.g. `ls`), confirm it runs; Ctrl+C interrupts a process.
    6. `sudo apt remove arduis` → removes cleanly.

    **Arch (real Wayland session):**
    7. On an Arch box, produce the source tarball
       (`git archive --format=tar.gz --prefix=arduis-1.0.0/ -o arduis-1.0.0.tar.gz HEAD`) next to the
       PKGBUILD, then `makepkg -si` → builds + installs (pulls vte4, python-gobject, gtk4,
       libadwaita). Optionally `namcap PKGBUILD` → no errors.
    8. In a Wayland session, launch arduis (app grid or `arduis`) → window + embedded VTE works;
       icon shows in the grid.
    9. `sudo pacman -R arduis` → removes cleanly.

    Report any dependency-resolution failure, missing icon, VTE-render issue, or Wayland-launch
    problem (esp. anything implicating a VTE API above the 0.76 floor on Ubuntu).
  </how-to-verify>
  <verify>
    <automated>MISSING — manual hardware gate (clean install + Wayland launch on real Ubuntu 24.04 + Arch); no automatable command, by design (SC-3)</automated>
  </verify>
  <done>PO confirms both distros: clean install, Wayland launch, embedded VTE works, icon in app grid, clean removal.</done>
  <resume-signal>Type "approved" (both distros install + launch + icon + VTE OK under Wayland), or describe the issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package → clean target system | install runs maintainer/build code as root on the PO's real machines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Elevation of privilege | clean install on real machines | mitigate | The `.deb` ships no maintainer scripts and the PKGBUILD ships no `.install`; install only places data files + the launcher; deps resolve from official distro repos — verified in the human gate |
| T-09-14 | Denial of service | VTE 0.76 API floor on Ubuntu | mitigate | UAT runs on real Ubuntu 24.04 (VTE 0.76) — confirms no API above the floor crashes the embedded terminal (DIST-04 / RESEARCH Pitfall 6) |
</threat_model>

<verification>
- Automated gate (Task 1) green before the human gate.
- PO confirms: clean `.deb` install + Wayland launch + embedded VTE on Ubuntu 24.04; `makepkg -si`
  install + Wayland launch + embedded VTE on Arch; arduis icon in the app grid on both; clean removal.
</verification>

<success_criteria>
- All four ROADMAP success criteria satisfied: SC-1 (AUR installs+runs on Arch), SC-2 (.deb
  installs+runs on Ubuntu 24.04), SC-3 (clean install under real Wayland on both — hard gate),
  SC-4 (HostRunner native no-op). DIST-02/03/04 done; v1 milestone complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-aur-deb/09-05-SUMMARY.md`
</output>
