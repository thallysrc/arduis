---
phase: 03.1
slug: worktree-as-terminal-workspace
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-10
validated: 2026-06-15
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | none — existing `tests/` directory, run via `python -m pytest` |
| **Quick run command** | `/tmp/arduis-venv/bin/python -m pytest tests/test_session.py tests/test_layout.py -q` |
| **Full suite command** | `/tmp/arduis-venv/bin/python -m pytest tests/ -q` |
| **Estimated runtime** | ~2 seconds (phase subset); ~2 seconds (full suite 437 tests) |

---

## Sampling Rate

- **After every task commit:** Run `/tmp/arduis-venv/bin/python -m pytest tests/test_session.py tests/test_layout.py -q`
- **After every plan wave:** Run `/tmp/arduis-venv/bin/python -m pytest tests/ -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

> The GTK-free seams (N-terminal session model, layout tree) carry the automatable
> logic and are pytest-testable; the live-GTK surface (workspace swap rendering,
> Paned sizing, C-Space capture under Wayland, RAM polling UI) is manual acceptance.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.1-01-01 | 01 | 0 (RED) | PAR-01, RAM-03 | T-03.1-02 | hibernate clears ALL terminal pgids (Pitfall 3 — no group forgotten) | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_session.py -q` (20 tests) | ✅ | ✅ green |
| 3.1-01-02 | 01 | 0 (GREEN) | PAR-01, RAM-03 | T-03.1-02 | `import gi` absent from session.py — GTK-free invariant | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_session.py -q -k gtk_free` (1 test) | ✅ | ✅ green |
| 3.1-01-03 | 01 | 0 (regression) | PAR-01, LAYOUT-01 | — | N/A | unit | `/tmp/arduis-venv/bin/python -m pytest tests/ -q` (437 tests, no regression) | ✅ | ✅ green |
| 3.1-02-01 | 02 | 1 | PAR-01, LAYOUT-01 | T-03.1-04 | branch labels via `set_text` never `set_markup` | parse-check | `python3 -c "import ast; ast.parse(open('src/arduis/window.py').read()); print('parse-ok')"` | ✅ | ✅ green |
| 3.1-02-02 | 02 | 1 | PAR-01, PAR-02, LAYOUT-01 | T-03.1-03 | argv via HostRunner list-literals; no shell=True | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_layout.py -q` (9 tests) | ✅ | ✅ green |
| 3.1-03-01 | 03 | 2 | PAR-03, RAM-02, RAM-03 | T-03.1-06, T-03.1-07 | RAM sub-lines via `set_text`; no orphan processes | unit | `/tmp/arduis-venv/bin/python -m pytest tests/ -q` (437 tests — keymap, caps, resource_monitor green) | ✅ | ✅ green |
| 3.1-03-02 | 03 | 2 | PAR-03 | — | N/A | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_keymap.py -q` | ✅ | ✅ green |
| 3.1-03-03 | 03 | 2 (UAT round 1) | LAYOUT-01 | T-03.1-06 | close-terminal path cleans widget maps, no orphan panes | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_layout.py -q` (9 tests — 3-terminal split + close-empty regression tests green) | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> **Post-execution reconcile (2026-06-15):** All test files created during execution
> pass green (437 total; 20 session + 9 layout = 29 directly phase-relevant). The
> 3-terminal split regression (`test_three_terminal_split_keeps_all_visible`) and the
> empty-tree close regression (`test_close_every_terminal_empties_the_tree`) were
> added to `tests/test_layout.py` during UAT fix round 1 (`68fa101`) — they pin the
> model contract behind the nested-Gtk.Paned split-collapse root-cause fix. The GTK
> widget surface (swap rendering, Paned sizing, keyboard capture, RAM polling) was
> validated by the human UAT approved 2026-06-10. `keymap.py`, `caps.py`, and
> `resource_monitor.py` were never modified across all three plans.

---

## Wave 0 Requirements

- [x] `tests/test_session.py` — N-terminal session model contract: `test_default_terminals` (term_id/kind/agent/shell shape), `test_hibernate_clears_all_terminals` (every pgid cleared), `test_store_serializable` (asdict recurses terminals), `test_hibernate_model` (terminals-list form), plus GTK-free invariant — for PAR-01, RAM-03
- [x] `tests/test_layout.py` — binary split/leaf tree mutations, visibility decoupling, zoom roundtrip, focus-or-swap (resolve_selection), plus UAT-round-1 regression tests for 3-terminal split and empty-tree close — for LAYOUT-01, PAR-01
- [x] Existing `tests/` infrastructure (keymap, caps, resource_monitor, spawn_argv, host_runner, theme) covers the reused seams that plans 02/03 depend on
- [x] No new test framework needed — existing pytest infrastructure covers the GTK-free model surface

*The GTK-free seams are the Nyquist sampling surface; the manual UAT covers the live-GTK/process surface.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Workspace swap renders correctly — selecting a sidebar row unparents all leaves and re-hangs the active worktree's terminals without respawning (A1 architectural claim) | PAR-02, PAR-01 | Live GTK render + PTY child survival across VTE unparent — needs a display and running processes | Launch app, create a worktree (2 terminals appear), type in the agent pane, switch to `main` then back — verify the `claude` process and scrollback survived unparent (A1); approved 2026-06-10 |
| C-Space h/j/k/l moves focus between the visible worktree's terminals; C-Space n/p and number switch the whole workspace | PAR-03 | Live keyboard input capture under Wayland; prefix state machine fires on actual key events | Arm C-Space prefix, test h/k/j/l terminal focus moves, n/p workspace cycling, and 1-indexed number jump; approved 2026-06-10 |
| Nested Gtk.Paned proportional split: 3+ terminals appear at ~equal widths (not collapsed to a sliver) | LAYOUT-01 | Live GTK Paned sizing — `notify::max-position` proportional positioner only fires with a real display allocation | Create 2+ extra splits; verify all terminals occupy visible width; drag a divider; verify ratios are learned; approved 2026-06-10 after headless broadway root-cause fix (`65a3989`) |
| Per-worktree RAM sub-line updates every ~2s; footer aggregates active worktrees; splits do NOT increment the worktree count (D-11) | RAM-03, RAM-02 | Live UI render of GLib-polled values + process-group RSS — requires live PTY children | Open worktrees with running agents, observe sidebar sub-line and footer; confirm splits add to RAM but not to the worktree count; approved 2026-06-10 |
| Hibernate kills EVERY terminal of the worktree (zero surviving processes), keeps the directory; canvas falls back to main if the hibernated worktree was visible | RAM-03, RAM-02 | Live process teardown + GTK canvas swap — needs running processes and a display | Hibernate a worktree; run `ps -ef | grep -E "claude|zsh"` to confirm zero survivors for that worktree dir; confirm canvas fell back to main; approved 2026-06-10 |
| Resume rebuilds the default 2-terminal layout (agent + shell); extra splits are NOT restored | RAM-03 | Live spawn + GTK canvas — needs a display | Resume a hibernated worktree; verify 2 terminals appear (no extra splits from before hibernate); approved 2026-06-10 |
| Window close leaves zero orphaned claude/zsh processes | PAR-01, RAM-03 | Live OS-level process teardown — no automated GTK test | Close the arduis window; run `ps -ef | grep -E "claude|zsh -l -i"` to confirm no orphans; approved 2026-06-10 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-06-15 (post-execution reconcile)

---

## Validation Audit 2026-06-15

| Metric | Count |
|--------|-------|
| Gaps found | 0 (all Wave-0 tests created during execution and confirmed green) |
| Resolved | 8 task rows reconciled to ✅ green (437 tests total; 29 directly phase-relevant) |
| Escalated | 0 |

The `03.1-VALIDATION.md` was left as a raw template with `{placeholder}` tokens and
`status: draft, nyquist_compliant: false` — never filled during execution. Reconstructed
post-execution (2026-06-15) by reading all three PLAN.md and SUMMARY.md files for plans 01,
02, and 03, the VERIFICATION.md (score 11/11), and the actual test files.

**Requirement → test mapping established:**

- **PAR-01** (each worktree has its own terminal set, all remain alive during swap): covered by
  `tests/test_session.py` — `test_default_terminals`, `test_hibernate_clears_all_terminals`,
  `test_store_serializable`; plus the manual A1 UAT (swap preserves live agents).
- **PAR-02** (sidebar selects workspace): manual-only — sidebar row activation calls
  `_swap_workspace` (verified by `grep` + parse-check during execution, and approved UAT).
- **PAR-03** (tmux-style prefix keys switch workspaces): `tests/test_keymap.py` (keymap
  dispatcher untouched, green throughout) + manual UAT (C-Space h/j/k/l / n/p / number).
- **LAYOUT-01** (free pane layout — split/drag paned dividers): `tests/test_layout.py` —
  `test_split_focused`, `test_close_collapses`, `test_zoom_roundtrip`, `test_preset_subset`,
  `test_visibility_decoupled`, `test_focus_or_swap`, `test_three_terminal_split_keeps_all_visible`
  (UAT regression), `test_close_every_terminal_empties_the_tree` (UAT regression),
  `test_layout_is_gtk_free`; plus manual UAT (Paned proportional sizing, nested split widths).
- **RAM-02** (cap counts active worktrees, not terminals): `tests/test_caps.py` (cap logic
  unchanged — green throughout); cap gate wired via `caps.at_cap` + `caps.active_count` in
  `window.py`; confirmed by manual UAT (split does not increment footer count).
- **RAM-03** (per-worktree RAM visibility): `tests/test_resource_monitor.py` (group_rss_kb
  unchanged — green throughout); `test_session.py` pins the N-terminal model that the RAM
  poll iterates; RAM sub-line and footer confirmed by manual UAT.

No automatable gap remained unresolved. The manual UAT (plan 03 Task 3, blocking
human-verify checkpoint) was approved 2026-06-10 after two fix rounds (commits `b487dfe`,
`68fa101`, `65a3989`). A1 (workspace swap preserves live terminals + scrollback) was
confirmed: the rebuild-on-switch approach (VTE widgets survive `unparent`) holds, and the
`Gtk.Stack` fallback was not needed.
