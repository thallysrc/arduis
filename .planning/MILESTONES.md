# Milestones

## v1.0 MVP (Shipped: 2026-07-01)

**Phases completed:** 13 phases, 52 plans, 89 tasks

**Key accomplishments:**

- Established the GTK-free, unit-tested Phase-1 foundation — HostRunner no-op/Flatpak-stub seam, Dracula palette, raw-waitstatus exit decode, and the spawn argv/env builder — with pytest infrastructure; 15 tests green, zero GTK imports.
- Wired the GTK4/libadwaita arduis window with one VTE terminal running host `zsh -l -i` over a direct native PTY through Plan-01's tested seams — app-owned Dracula palette, no-orphan SIGHUP->SIGKILL teardown on the child PGID, raw child-exit decode, a native `run.sh`, and the manual Phase-1 acceptance checklist. 15 unit tests still green; runtime/interactive acceptance is a human gate (D-14).
- RED pytest contracts (tests/test_worktree.py + tests/test_session.py) pinning the GTK-free git-worktree argv/parse builders and the serializable SessionStore + bytes-feed/hibernate model, before any Phase-2 domain code exists.
- Task 1 — `src/arduis/worktree.py` (pure git-argv builders + parsers):
- Adw.TabView core loop wiring the Plan-01 domain into the live app: a "+New worktree" dialog creates a worktree via async `git worktree add` off the auto-detected default branch and opens a tab whose VTE terminal spawns `zsh -l -i` in the worktree dir and feeds `claude` as bytes, with right-click Hibernate/Resume process-group teardown (RAM-01) and a generalized no-orphan close.
- 1. [Rule 3 - Blocking] Verification interpreter switched to the repo .venv
- 1. [Rule 3 - Blocking] Verification interpreter is the repo .venv
- 1. [Rule 1 - Bug] RAM conversion factors matched to the RED contract
- 1. [Rule 3 - Blocking] Verification interpreter is the repo .venv
- The Phase-3 window is complete: a capture-phase C-Space prefix state machine, a ~2s process-group RAM poll feeding the sidebar + footer, and an active-agent cap that gates +New — plus four UAT-driven fixes that root the main shell in the launched repo and surface the repo name.
- Pivoted window.py from one global LayoutModel-of-worktrees to one LayoutModel PER worktree-of-terminals: the canvas shows exactly one worktree's terminals, sidebar row activation swaps the whole workspace (rebuild-on-switch, live agents preserved), and a new worktree is born eager with 2 terminals — left agent (claude-fed) + right plain shell.
- Re-targeted the C-Space prefix onto the per-worktree-terminals model (h/j/k/l step terminals, n/p + number switch whole workspaces incl. main), summed RAM across each worktree's terminal process groups, made hibernate kill every terminal group and resume rebuild the default 2-terminal layout, and root-caused + fixed a nested-Gtk.Paned split collapse with a max-position proportional positioner — all confirmed via a manual UAT (A1: live agents survive workspace swap) after two fix rounds.
- Task = one branch across N member repos: `project.detect_member_repos` + `task_layout` path/symlink/argv builders + a `Task`/`RepoCheckout` session model that replaces `WorktreeSession`, all pure, GTK-free, and TDD-tested (43-test suite green).
- `window.py` migrated from `WorktreeSession` to `Task`: startup detects member repos (or the degenerate 1-repo project), the topbar shows the project name (no switcher), and "New task" picks 1+ repos + a branch to materialize a mirrored task folder (relative symlinks + chosen repos as worktrees via a per-repo chained `git worktree add`, never `--force`) opening a 2×N one-column-per-repo workspace.
- Re-targeted the whole task lifecycle in `window.py` so the unit of hibernate/RAM/cap/teardown is the TASK across all its repos — per-task hibernate kills every group (no orphan), resume rebuilds the default workspace via the shared `_build_task_workspace`, RAM/cap count tasks (not repos), startup rediscovers `../<root>-tasks/` as HIBERNATED without spawning, and close-a-repository kills only that repo's groups while never deleting from disk — capped by a live + headless acceptance pass that proved no orphans with real zsh process groups.
- `detect_member_repos` narrowed to `.git`-DIRECTORY subdirs so a 2-repo + 20-worktree root yields 2 not 22, plus a GTK-free `topbar.ChipState` modeling the toggled-ON default (D-02) and a non-destructive reflected-active set (D-03).
- Topbar now renders one toggleable Gtk.ToggleButton chip per member repo (dot + name) backed by ChipState, seeds the New-task dialog from the toggled-ON set, reflects the active task's repos, and folds >6 repos into a "+N" overflow menu — packed at one header end so Phase 7 / 07-04 badges fit beside it.
- 1. [Rule 1 - Bug] Active-project choice: cwd wins over last_active when launched inside a project
- 1. [Rule 1 - Bug] Stale hardcoded REPO path in test_project_switch_smoke.py
- The GTK-free STATUS-01 injection channel: an env-guarded stdlib-only Claude Code hook script that maps 7 hook events to the 5-state model and atomically writes per-terminal state files, plus the spawn.py `extra_env` seam through which `window.py` will inject `ARDUIS_STATE_FILE` per terminal.
- GTK-free `attention.py` — 5-state model, traversal-safe state-file plumbing, time/pid-based effective status, per-task aggregation, an additive/idempotent `~/.claude/settings.json` merge builder, notify + auto-suspend policies, and a `tomllib` config reader — every Phase-4 decision rule as a pure unit-tested function.
- Wired the tested Phase-4 domain into the GTK window: a 4-branch consent-gated hook installer, per-agent-terminal `ARDUIS_STATE_FILE` env injection, a `Gio.FileMonitor` that flips `TerminalRecord.status` on the main loop, live sidebar task-aggregate + pane-header per-terminal status dots (Dracula colors), `libnotify` waiting-notifications with a per-terminal replace-id, and state-file cleanup on every teardown path — Core Value "sempre sabendo qual deles te espera" is now visible in primary (hooks) mode.
- Opt-in idle auto-suspend (RAM-04) rides the 2s `_poll_ram` tick through a single `should_autosuspend` gate into the proven no-orphan `_hibernate_task` path, feeds `claude --continue` on resume so suspension costs nothing, and marks the row "suspensa"; consent-decliners get a 0.76-floor degraded signal (VTE `bell`→waiting, `contents-changed`→running/idle) with a "status limitado" re-invite — both verified live on a real `Vte.Terminal`.
- A frozen `Theme` dataclass + `THEMES` registry of 4 dark themes (Dracula default + Nord + Solarized Dark + Gruvbox Dark) with a `get_theme(name)` closed whitelist that always degrades to Dracula, carrying both the 16-color VTE palette and every UI color `window._CSS` hardcodes — GTK-free, with the 16-color/valid-hex `set_colors` invariants pinned by tests.
- Three GTK-free config layers (agent command -> safe argv/feed bytes, [keys] prefix + closed-set bindings merge, atomic section-preserving [theme] writer) plus the Phase-5 split/zoom/refeed keymap chords — all stdlib, zero new dependencies, fully unit-tested.
- Wired the three tested Phase-5 domains into window.py: the agent feed now comes from `[agent] command` (create/split/resume/refeed), the C-Space prefix machine reads `[keys]` (configurable prefix + bindings) and `_run_action` handles split/zoom/refeed, and a header "Tema" menu + `win.set_theme` switches the theme at runtime — replacing (not stacking) the CssProvider, re-coloring every live VTE, and persisting the choice.
- Two GTK-free domain modules — a tolerant `.arduis.toml [setup]` reader with a cd-guarded newline-joined shell-feed builder (ENV-01/ENV-02), and a sha256 content-hash trust gate with a fail-closed, atomically-written `~/.config/arduis/trusted_setups.toml` — pinned by 34 unit tests.
- Wired the Wave-1 `repoconfig`/`trust` domain into `window.py`: on CREATE-finalize only, each succeeded repo's `[setup]` is read from its worktree, already-trusted (repo-realpath, content-hash) repos feed silently into the task's SHELL terminal (`{task}:t1`) with a `cd`-guard, and the rest gate behind ONE consolidated `Adw.AlertDialog` showing the exact commands per repo — completing ENV-02 and criterion 4.
- GTK-free `compose.py` — branch->project-name sanitizer, the authoritative `docker compose config --format json` port reader, deterministic offset+probe port assignment with clustered collision-retry, and the load-bearing `ports: !override` override-byte generator (REPLACE not append), all pinned by 25 docker-free unit tests.
- GTK-free `containerstate.py` — tolerant read + atomic write of per-task `arduis.container.toml` (COMPOSE_PROJECT_NAME + on/off + resolved port map) plus the `[containers].port_offset` config read, the durable record that keeps ports/badges stable across restarts.
- `docker_service.run_compose_async` — a verbatim clone of `git_service.run_git_async` that routes compose argv through the HostRunner seam and runs `docker compose` off the GTK loop via `Gio.Subprocess` + `communicate_utf8_async`, the single non-blocking async boundary every Wave-3 compose call uses, pinned by 2 spawn-free routing tests.
- 1. [Rule 3 - Blocking] Plan referenced `_build_sidebar_row`; the real method is `_make_row`
- GTK-free `review.py` (read-only diff / porcelain clean-gate / never-`--force` worktree remove+prune / branch ahead-behind argv + tolerant parsers) and `review_cache.py` (TTL `is_fresh` + task-keyed `ReviewCache`), both TDD, 34 new tests, full suite 378 green.
- GTK-free `gh.py` (read-only `gh pr view --json` parse, the ONE allowed write `gh pr create --web`, gh-absent/exit-4 graceful degrade, glanceable sublines) plus a backward-compatible optional `cwd=` on `run_git_async` so gh infers its repo from the worktree dir.
- Read-only `git --no-pager diff` VTE leaf (`set_input_enabled(False)`), a branch + ahead/behind + PR-status sidebar subline behind a `gh_available` gate + `ReviewCache` TTL + `_pr_busy` in-flight debounce (no poll), and the `gh pr create --web` write — all wired as pure glue over the Plan-01/02 GTK-free argv builders via `run_git_async(cwd=worktree)`.
- The live human-acceptance checklist (08-HUMAN-UAT.md) was produced and the checkpoint closed on 2026-07-01 by explicit user risk acceptance without running the live items — all load-bearing logic and destructive safety (teardown order, dirty-tree refusal, no `--force`, D-10) are proven headless against real git with the full suite green (448 passed), and the 5 display/GitHub-dependent confirmations are recorded as skipped/risk-accepted, to be confirmed opportunistically in real use.
- Build/lint toolchain install deferred — passwordless sudo unavailable
- 1. [Rule 3 - Blocking] meson_version bump + ordering to satisfy the version-equality test
- Re-ran the complete Phase-9 automated gate to green — full pytest (436 passed), metainfo + desktop validate, `arduis_1.0.0_all.deb` builds with 0 lintian errors and the correct deps + no maintainer scripts, PKGBUILD parses, HostRunner native no-op confirmed — then handed the PO the DIST-04 / SC-3 hardware UAT (clean install + real-Wayland launch on Ubuntu 24.04 + Arch), which no command can satisfy.

---
