---
phase: 04-attention-detection-who-s-waiting
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified: []
autonomous: false
requirements: [STATUS-01, STATUS-02, STATUS-03, RAM-04]
user_setup: []

must_haves:
  truths:
    - "Headless smoke proves the full chain: a hook-script subprocess write into the live status dir flips the sidebar dot CSS class via the FileMonitor — no display needed"
    - "Live UAT confirms the phase gate: real approval → orange ≤1s; TUI redraw → no false orange; approve → clears; unfocused notification arrives; the user's own hooks still fire"
    - "Auto-suspend + --continue resume verified end-to-end with a real claude conversation"
  artifacts: []
  key_links:
    - from: "headless harness (throwaway, /tmp)"
      to: "src/arduis/window.py status pipeline"
      via: "gtk4-broadwayd + real state-file writes"
      pattern: "broadway"
---

<objective>
Phase acceptance: an autonomous headless verification pass (full suite + broadway GTK smoke of
the watcher→dot pipeline and the consent/merge safety properties), followed by the live
human-verify checkpoint covering the five roadmap success criteria and the UAT items the
research explicitly deferred to a live run (Esc-interrupt behavior A2, idle_prompt
co-occurrence A1, notification UX, real auto-suspend).

Purpose: criterion 3 ("waiting survives a TUI redraw, fires for a real approval prompt") is
the phase's differentiator and is only provable against a real claude TUI.
Output: verified phase; checkpoint feedback (if any) resolved as deviations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-attention-detection-who-s-waiting/04-CONTEXT.md
@.planning/phases/04-attention-detection-who-s-waiting/04-RESEARCH.md
@.planning/phases/04-attention-detection-who-s-waiting/04-03-SUMMARY.md
@.planning/phases/04-attention-detection-who-s-waiting/04-04-SUMMARY.md
@src/arduis/window.py
@src/arduis/attention.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Headless verification — full suite + broadway smoke of the watcher→dot pipeline</name>
  <files>(throwaway harness under /tmp only — no repo files)</files>
  <action>
1. Full suite: `/tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q` must be green
   (≥ the pre-phase 88 baseline plus all Phase-4 tests).
2. Headless GTK smoke (gtk4-broadwayd pattern from the dev-environment convention; harness
   scripts in /tmp, touching only sandbox dirs and a sandbox HOME so the REAL
   ~/.claude/settings.json is never written):
   a. **Consent safety:** launch the window with HOME pointed at a sandbox containing a copy
      of a hooks-rich settings fixture; drive the "install" response programmatically;
      assert: backup file created, merged settings parse as JSON, every pre-existing hook
      entry still present, arduis entries present for all 7 events, second launch shows NO
      dialog (idempotence). Then with a declined marker: assert no write, `self._degraded`
      True.
   b. **Pipeline:** with a fake ACTIVE task + agent record registered, run the REAL
      `src/arduis/hooks/arduis_hook.py` as a subprocess with ARDUIS_STATE_FILE pointing into
      the window's live `self._status_dir` and a `Notification/permission_prompt` payload;
      iterate the main loop; assert the task's sidebar dot label carries
      `arduis-dot-waiting` and the record.status == "waiting". Then feed a `PostToolUse`
      payload → assert `arduis-dot-active`/running. Then `Stop` → `arduis-dot-ready`.
   c. **Cleanup:** call `_on_hibernate` on the task; assert its state file is gone from the
      status dir and the row dot is grey.
   d. **Auto-suspend policy wiring:** with `_att_config.auto_suspend_minutes=1` monkeypatched
      and `_calm_since` backdated 120s with a ready aggregate, run one `_poll_ram` tick;
      assert the task flipped to HIBERNATED with `auto_suspended=True` and that the NEXT
      spawn callback would feed `AGENT_RESUME_FEED` (inspect the closure decision or feed a
      recording fake terminal). With a RUNNING aggregate at the same age: assert nothing
      suspended (Pitfall 6).
3. Record pass/fail per check in the executor log (these become the checkpoint's
   "what-built" evidence).
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>Suite green; broadway smoke proves consent safety (sandbox HOME), hook→file→monitor→dot for waiting/running/ready, cleanup on hibernate, and the suspend/never-suspend pair — all without a display.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live acceptance — the five phase criteria + research UAT flags</name>
  <files>(none — live verification only)</files>
  <action>Present the checklist below to the user with the Task-1 headless evidence summarized; pause for live verification on a real display. Any reported issue is resolved as a checkpoint-feedback deviation (03.2-03 precedent) before approval.</action>
  <what-built>
Hooks-first attention detection: consent-gated hook install into ~/.claude/settings.json
(env-guarded no-op script), per-terminal state files in $XDG_RUNTIME_DIR/arduis/status,
FileMonitor-driven sidebar/pane status dots (waiting=laranja, running=verde, ready=ciano,
idle=cinza-esverdeado), libnotify on waiting+unfocused, opt-in auto-suspend with
`claude --continue` resume, and degraded bell mode for consent-decliners.
  </what-built>
  <how-to-verify>
Run `./run.sh` in a real project and verify:

1. **Consent (D-02):** first launch shows the pt-BR install dialog. Accept. Confirm
   `~/.claude/settings.json.arduis-backup` exists and your own hooks (GSD, notify-send)
   still fire in an OUTSIDE-arduis claude session (env-guard: arduis hook is a no-op there).
2. **Criterion 1+2 (STATUS-01/02):** create a task; when claude is at its prompt the dot is
   CIANO (ready). Send a prompt → VERDE (running). Ask claude to run a command that needs
   approval (e.g. "rode docker ps") → dot fica LARANJA em ≤1s, no sidebar E no header do
   pane. [UAT-A2: also press Esc mid-response — dot deve voltar a ciano em até ~60s via
   idle_prompt self-heal; anote o comportamento real do Stop.]
3. **Criterion 3 (the gate):** while claude streams a long response (TUI repainting), o dot
   NUNCA fica laranja sem um prompt real (no false orange). Aprove a permissão pendente →
   laranja limpa para verde imediatamente (PostToolUse). [UAT-A1: deixe uma aprovação
   pendente >60s — laranja NÃO pode ser rebaixado pelo idle_prompt.]
4. **Criterion 4 (STATUS-03):** desfoque a janela (foque outro app), provoque um prompt de
   aprovação → notificação desktop "branch aguarda você" chega; com a janela FOCADA, não.
   Bursts não empilham (replace-id). [Nota: sua notify-send pessoal pode duplicar — Pitfall
   10, aceito.]
5. **Criterion 5 (RAM-04):** crie `~/.config/arduis/arduis.toml` com
   `[attention]\nauto_suspend_minutes = 1`, relance, converse com um agente, deixe-o ready
   por >1min → task auto-suspensa (notificação + subline "suspensa", processos mortos, RAM
   liberada). Retome pelo card "Retomar" → o agente volta com `claude --continue` e a
   CONVERSA ANTERIOR está lá. Hibernar/retomar MANUAL continua abrindo claude limpo.
6. **Degraded (D-13):** remova as entradas arduis do settings (ou use o backup), toque
   `~/.local/share/arduis/hooks_declined`... ou recuse o diálogo num HOME de teste: dot
   funciona via bell ("esperando?" no badge) + hint "status limitado" visível e clicável.
7. **Sem órfãos:** feche o app com agentes rodando → nenhum zsh/claude órfão
   (`ps -eo pgid,cmd | grep -i claude`); status dir limpo na próxima inicialização.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (each becomes checkpoint-feedback deviations, as in 03.2)</resume-signal>
  <verify>User confirmation of checklist items 1–7 (live display); issues iterated until approved.</verify>
  <done>All five roadmap Phase-4 success criteria confirmed live; A1/A2 observations recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| headless harness → user's real HOME | acceptance scripts must never touch the real ~/.claude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Tampering | smoke harness writing the user's real settings.json | mitigate | Harness runs with HOME pointed at a /tmp sandbox containing a fixture copy; assert the real file's mtime is unchanged after the run |
| T-04-24 | DoS | smoke leaving orphan zsh/broadwayd processes | mitigate | Harness kills its broadwayd + any spawned groups in a finally block (mirror the 03.2 acceptance harness pattern) |
</threat_model>

<verification>
- Full suite green; broadway smoke checks all pass.
- Live checklist items 1–7 confirmed by the user (or recorded as checkpoint-feedback deviations).
</verification>

<success_criteria>
- All five roadmap Phase-4 success criteria confirmed live; UAT flags A1/A2 observed and recorded (adjust thresholds in a follow-up if the live behavior contradicts the assumptions).
- Requirements STATUS-01, STATUS-02, STATUS-03, RAM-04 demonstrably complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-attention-detection-who-s-waiting/04-05-SUMMARY.md`
documenting: headless evidence, live UAT results per criterion, observed A1/A2 behavior, and
any checkpoint-feedback deviations (the 03.2-03 SUMMARY is the format precedent).
</output>
