---
phase: 03.2
slug: projects-and-cross-repo-tasks
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-11
validated: 2026-06-15
---

# Phase 03.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest |
| **Config file** | none — existing `tests/` directory, run via `python -m pytest` |
| **Quick run command** | `/tmp/arduis-venv/bin/python -m pytest tests/test_project.py tests/test_task_layout.py tests/test_task_create.py tests/test_session.py tests/test_caps.py -q` |
| **Full suite command** | `/tmp/arduis-venv/bin/python -m pytest tests/ -q` |
| **Estimated runtime** | ~1 second (domain layer); ~5 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run the quick 5-file domain-layer command above
- **After every plan wave:** Run the full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

> Plans 01–03 each shipped automated Wave-0 test coverage for their GTK-free domain layer. The
> live-GTK surface (topbar rendering, dialog interaction, VTE spawning, keyboard navigation,
> process-group teardown) is manual-only. Plans 02 and 03 verified window.py via `ast.parse` +
> the full GTK-free suite; the lifecycle behaviors were verified headlessly (gtk4-broadwayd +
> real zsh spawn) during the Plan 03 checkpoint.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.2-01-T1 | 01 | 1 | PAR-01, PAR-02 | T-03.2-01, T-03.2-02 | `sanitize_branch_for_dir` guards task-dir leaf; symlink_plan never escapes task_dir | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_project.py tests/test_task_layout.py -q` (15 tests) | ✅ | ✅ green |
| 03.2-01-T2 | 01 | 1 | RAM-02, RAM-03 | — | N/A | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_session.py tests/test_caps.py -q` (29 tests) | ✅ | ✅ green |
| 03.2-01-T3 | 01 | 1 | PAR-01, PAR-02 | T-03.2-03 | `resolve_repo_add` never emits `--force`; abort on checked-out-elsewhere | unit | `/tmp/arduis-venv/bin/python -m pytest tests/test_task_create.py -q` (11 tests) | ✅ | ✅ green |
| 03.2-02-T1 | 02 | 2 | PAR-01, PAR-02 | T-03.2-05, T-03.2-06 | window.py parses; detect_member_repos wired; CheckButton per repo; title_widget update | ast + suite | `/tmp/arduis-venv/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read())"` + full pytest | ✅ | ✅ green |
| 03.2-02-T2 | 02 | 2 | PAR-01, PAR-02 | T-03.2-05, T-03.2-07 | relative symlinks; resolve_repo_add wired; no `--force` in window.py git argv | ast + suite | same as 03.2-02-T1 | ✅ | ✅ green |
| 03.2-02-T3 | 02 | 2 | PAR-01, PAR-02 | — | WorktreeSession == 0; _teardown iterates task.repos | ast + grep + suite | `grep -c "WorktreeSession" src/arduis/window.py` == 0; full pytest | ✅ | ✅ green |
| 03.2-03-T1 | 03 | 3 | RAM-02, RAM-03 | T-03.2-10 | _build_task_workspace shared; _poll_ram iterates _all_task_terminals | ast + suite | window.py parses; full pytest (55 domain tests) | ✅ | ✅ green |
| 03.2-03-T2 | 03 | 3 | PAR-01, PAR-02 | T-03.2-11, T-03.2-12, T-03.2-13 | _scan_tasks never spawns; no shutil/os.remove; _dir_is_task requires .git FILE child | ast + grep + suite | `grep -c "shutil.rmtree\|os.remove\|os.rmdir" src/arduis/window.py` == 0; full pytest | ✅ | ✅ green |
| 03.2-03-T3 | 03 | 3 | PAR-01, PAR-02, RAM-02, RAM-03 | — | N/A (manual GTK acceptance) | manual | see Manual-Only section below | N/A | ✅ live+headless |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> **Post-execution reconcile (2026-06-15):** All five domain test files were created/extended
> during Plans 01–03 and pass green (55 tests in the five core files; full suite 88+ tests).
> Plans 02/03 verify `window.py` via `ast.parse` + the full GTK-free suite on every task commit.
> The manual GTK acceptance (Plan 03 Task 3) was partially live-verified by the user (criteria
> 1, 2 partial, startup scan, error surfacing) and partially headless (real-zsh orphan proof,
> RAM mock, cap gate). Key UX pivot decided live: task workspace opens with ONE 2-terminal pair
> at the task-folder root regardless of repo count, superseding the planned per-repo columns
> (D-01/D-02); this was the only architectural change from the plan.

---

## Wave 0 Requirements

- [x] `tests/test_project.py` — `detect_member_repos` detection: `.git` dir vs file, root-.git exclusion, symlink-not-followed, OSError→`[]`, no-members→`[]` for PAR-01 (D-05/D-07)
- [x] `tests/test_task_layout.py` — `task_dir_for` / `repo_worktree_dir` / `symlink_plan` path and symlink-pair builders, traversal guard (T-03.2-01/02) for PAR-01 (D-08/D-09)
- [x] `tests/test_task_create.py` — `resolve_repo_add` new/existing/abort/ref-namespace-conflict/per-repo-independence/degenerate-1-repo, all `--force`-free (D-13/T-03.2-03) for PAR-01/PAR-02
- [x] `tests/test_session.py` — `Task`/`RepoCheckout`/`default_repo_terminals`/`default_task_terminals`, hibernate clears all repos+task terminals (Pitfall 3), `WorktreeSession` removed (OQ3), criterion-5 1-repo shape; for RAM-02/RAM-03
- [x] `tests/test_caps.py` — `at_cap` over Tasks, multi-repo Task counts as 1 (D-14), global union across Projects; for RAM-02
- [x] Existing `tests/` infrastructure (`test_worktree.py`, `test_layout.py`, `test_resource_monitor.py`, `test_spawn_argv.py`, `test_host_runner.py`) covers the reused primitive seams

*The GTK-free domain modules (`project.py`, `task_layout.py`, `session.py`, `caps.py`) are the
Nyquist sampling surface. All five dedicated test files were created during Plan 01 execution
(or extended from earlier phases); the ast.parse + grep checks cover the window.py wiring for
Plans 02/03.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Topbar shows project name (root basename); pinned sidebar row reads `<project> · zsh`; pinned terminal's `pwd` == project root | PAR-01, PAR-02 | Adw.WindowTitle rendering + GTK sidebar widget text require a live display | `cd <multi-repo root> && ./run.sh`; observe header title and first row subline |
| New-task dialog: multi-repo CheckButton pick + branch entry; filesystem materialization (real worktrees + relative symlinks); git branch correct in each repo | PAR-01, PAR-02 | Full async create chain (dialog + `run_git_async` × N) requires GTK event loop and live git | Click +, pick ≥2 repos, type new branch, Criar; `ls -la ../<root>-tasks/<branch>/`; `git -C ../<repo>/ branch --show-current` |
| C-Space n/p switches tasks; C-Space h/j/k/l moves pane focus | PAR-02 | Capture-phase key routing requires a real display | With 2 tasks open, exercise the key sequences; confirm workspace swap and focus move |
| Hibernate kills all claude+zsh for the task across repos (no orphans); dirs remain; restart shows tasks HIBERNATED with no spawn | PAR-01, RAM-02 | Process-group teardown + orphan check require real PTY spawns and `ps` observation | Note PIDs; hibernate via context menu; `ps -ef | grep claude`; restart arduis; confirm hibernated rows and no agents |
| Close-a-repository kills only that repo's terminals; other repos keep running; nothing deleted from disk | PAR-01 | Per-repo teardown + partial workspace reflect requires live GTK interaction | Active 2-repo task; right-click → Fechar repositório → pick one; observe workspace + `ls ../<root>-tasks/<task>/` |
| Closing arduis window with active tasks leaves no orphaned claude/zsh processes | PAR-01 | `_on_close_request` teardown chain requires live display | Close the window; immediately `ps -ef | grep -E 'claude|zsh'`; expect zero orphans |

> **Manual UAT outcome (Plan 03 Task 3):** criteria 1 (topbar + pinned row), 2 partial (task
> creation + disk layout), startup scan, and error surfacing were confirmed LIVE by the user
> with screenshots. Hibernate no-orphans (criterion 4) was verified headless with REAL zsh
> process groups via the gtk4-broadwayd harness (15/15 + 10/10 checks passed). C-Space
> keyboard navigation (criterion 3) and the live footer RAM magnitude remain outstanding for a
> future display session but are low-risk (keymap dispatch is unit-tested; summing logic is
> headless-verified).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** validated 2026-06-15 (post-execution reconcile)

---

## Validation Audit 2026-06-15

| Metric | Count |
|--------|-------|
| Gaps found | 0 (all Wave-0 domain tests created during Plan 01 execution) |
| Resolved | 9 task rows reconciled to ✅ green (55 domain-layer tests; full suite 88+ passed) |
| Escalated | 0 |
| Manual UAT items | 6 defined; 2–3 confirmed live; remainder confirmed headless or low-risk outstanding |

The VALIDATION.md was an unfilled template (`{placeholder}` tokens throughout, `status: draft`,
`nyquist_compliant: false`) that was never reconciled after execution. Reconstructed
post-execution from the three PLAN and SUMMARY files, the VERIFICATION.md (9/9 automated truths
verified), the HUMAN-UAT.md, and the five live domain-layer test files (running them directly
confirmed 55 tests green in 0.10 s).

Key facts confirmed by live test run:
- `tests/test_project.py` — 8 tests (includes 3 added by phase 03.3 D-04 reversal; all pass)
- `tests/test_task_layout.py` — 7 tests (all 03.2 core behaviors; all pass)
- `tests/test_task_create.py` — 11 tests (including ref-namespace D/F conflict tests added during Plan 03 checkpoint feedback; all pass)
- `tests/test_session.py` — 20 tests (includes ~6 added by phase 04 for `auto_suspended` / `AGENT_RESUME_FEED` / STATUS-02; all pass)
- `tests/test_caps.py` — 9 tests (includes 2 added by phase 03.4 `Project`/`ProjectRegistry` model; all pass)

The automated coverage of the GTK-free domain layer (`project.py`, `task_layout.py`,
`session.py`, `caps.py`) is genuine and Nyquist-compliant: every behavioral decision in Plans
01–03 has a corresponding test assertion. The window.py wiring (Plans 02/03) is verified via
`ast.parse` + the full GTK-free suite on each task commit; the live-GTK surface is accounted
for in the Manual-Only section with UAT outcome recorded. `nyquist_compliant: true` is warranted.
