---
phase: 03.2-projects-and-cross-repo-tasks
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/arduis/window.py
autonomous: false
requirements: [RAM-02, RAM-03, PAR-01, PAR-02]
user_setup: []

must_haves:
  truths:
    - "Hibernating a task kills EVERY process group of EVERY repo (SIGHUP→SIGKILL), keeps all directories; no orphan claude/zsh survives"
    - "Resuming a task rebuilds the DEFAULT one-column-per-repo layout (agent+shell per repo) and re-spawns eagerly"
    - "A task row's RAM = the sum of every terminal process group of every repo; the footer aggregates across tasks"
    - "The active-agent cap counts active (non-hibernated) TASKS; a fat multi-repo task counts as 1"
    - "On startup arduis scans ../<root>-tasks/ and lists each valid task as HIBERNATED (disk is the source of truth; no state file)"
    - "The UI can CLOSE A REPOSITORY within a task (kills that repo's terminal groups) — arduis never deletes anything from disk"
    - "Closing the window tears down every task's every repo's every terminal — no orphans"
  artifacts:
    - path: "src/arduis/window.py"
      provides: "per-task hibernate/resume default layout, per-task RAM sum, cap-over-tasks, close-repo, startup task scan, no-orphan teardown"
      contains: "_scan_tasks"
  key_links:
    - from: "src/arduis/window.py:_poll_ram"
      to: "src/arduis/resource_monitor.py:group_rss_kb"
      via: "sum group_rss_kb over every repo's every terminal pgid"
      pattern: "group_rss_kb"
    - from: "src/arduis/window.py:_on_new_worktree_clicked (cap gate)"
      to: "src/arduis/caps.py:at_cap"
      via: "at_cap(self._store.all()) over Tasks (D-14)"
      pattern: "caps.at_cap"
    - from: "src/arduis/window.py:_scan_tasks"
      to: "../<root>-tasks/ on disk"
      via: "os.scandir at startup; each valid dir → HIBERNATED Task"
      pattern: "_scan_tasks"
---

<objective>
Re-target the full task lifecycle in `window.py` so the unit of hibernate/RAM/cap/teardown is the TASK (across all its repos), not a single worktree (D-14 + carried-forward 03.1 decisions). Add the startup scan of `../<root>-tasks/` (disk = source of truth, D-11), the "close a repository" action (kill that repo's groups only — arduis never deletes from disk, D-10), and verify no-orphan teardown across every repo. Conclude with a manual GTK acceptance checklist (criteria 3/4).

Purpose: This makes the task model actually behave like a task — hibernate frees all of a feature's agents across repos, RAM/cap reflect the real working set, and reopening arduis rediscovers past tasks from disk.
Output: `src/arduis/window.py` lifecycle complete; manual UAT performed and recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-RESEARCH.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-01-SUMMARY.md
@.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-02-SUMMARY.md

<interfaces>
From src/arduis/session.py (Plan 01): `Task`, `RepoCheckout`, `default_repo_terminals(task_id, repo_name)`, `hibernate_fields(task)` (clears every repo's terminals), `SessionState`, `AGENT_FEED`, `SessionStore`.
From src/arduis/task_layout.py (Plan 01): `task_dir_for`, `repo_worktree_dir`.
From src/arduis/project.py (Plan 01): `detect_member_repos`.
From src/arduis/caps.py (unchanged): `at_cap(tasks)`, `active_count(tasks)` — `state.value == "active"` over Tasks (D-14).
From src/arduis/resource_monitor.py (unchanged): `group_rss_kb(pgid)`, `format_ram_kb(rss_kb|None)`.
From src/arduis/window.py (Plan 02): `_create_task`/`_add_repo`/`_spawn_repo_terminals`/`_spawn_into`/`_member_repo_path`, Task-keyed sidebar, `_teardown_pgid` (SIGHUP→SIGKILL), `_reflect_layout`, `_workspace_layout(sid)`, `_swap_workspace`, `_layouts`, `_leaf_by_sid`/`_term_by_sid`.
</interfaces>

<carried_forward_decisions>
- Hibernate kills ALL the task's process groups across all repos (SIGHUP→SIGKILL), keeps directories; resume relaunches the default layout (03.1 D-08/D-09, re-targeted).
- Sidebar row RAM = sum of all terminal process groups of all the task's repos; footer aggregates across tasks (03.1 D-10).
- tmux model: panes = terminals, windows = tasks; C-Space h/j/k/l within the workspace, n/p/number switches tasks (03.1 D-06 — switch targets are tasks, already true since rows are tasks).
- D-14: the active cap counts active (non-hibernated) TASKS regardless of repo count.
- A `<root>-tasks/<x>/` dir is a VALID rediscoverable task iff it contains ≥1 real git worktree — a subdir whose `.git` is a FILE (worktree pointer), not a symlink/plain dir (RESEARCH A5 / Runtime State Inventory). Validate this in the scan.
</carried_forward_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-task hibernate/resume (default one-column-per-repo layout) + per-task RAM sum + cap-over-tasks</name>
  <read_first>
    - src/arduis/window.py §`_on_hibernate`/`_on_resume`/`_teardown_session_terminals` (post-Plan-02 bodies), `_poll_ram`/`_update_footer`, `_prompt_hibernate_then`, `_create_task`/`_add_repo` (to reuse the 2×N layout builder)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md (D-14 + carried-forward hibernate/RAM)
    - .planning/phases/03.1-worktree-as-terminal-workspace/03.1-CONTEXT.md (D-08/D-09 hibernate-discards-layout / resume-rebuilds-default)
  </read_first>
  <action>
1. `_on_hibernate(task)` — re-target to all repos (D-08/Pitfall 3):
   - `self._teardown_session_terminals(task)` already iterates every repo's terminals (Plan 02). Confirm it SIGHUPs every `t.pid`.
   - `hibernate_fields(task)` (Plan 01) flips state + clears every repo's pid/pgid.
   - Discard the task's saved layout: `self._layouts.pop(task.task_id, None)`.
   - Drop every terminal widget for the task — terminal ids are `{task}:{repo}:tN`, so the prefix `f"{task.task_id}:"` still matches all of them: pop matching keys from `_leaf_by_sid` and `_term_by_sid`.
   - If the hibernated task is the visible workspace, `self._swap_workspace(_MAIN_SID)`.
   - `self._rebuild_sidebar()` (grey dot the row).
2. `_on_resume(task)` — rebuild the DEFAULT one-column-per-repo layout (D-09, NOT a reattach). Factor the 2×N layout+leaf construction from Plan 02's `_create_task` into a reusable helper `_build_task_workspace(task, repos: list[str])` that:
   - builds a fresh `LayoutModel` with, per repo, agent `{task}:{repo}:t0` over shell `{task}:{repo}:t1` (vertical split within a column), columns joined horizontally (D-01/D-02);
   - creates the VTE leaves, registers widget maps, sets the task visible, reflects.
   Resume then: set `task.repos[*].terminals = default_repo_terminals(task.task_id, repo.repo_name)` fresh for each repo, `task.state = ACTIVE`, call `_build_task_workspace`, rebuild sidebar, and spawn each repo's agent+shell eagerly (agent fed `AGENT_FEED`, shell plain). Earlier extra splits are NOT restored (03.1 decision stands).
   - IMPORTANT: a resumed task must re-derive which repos it has. Since hibernate kept `task.repos` (only cleared pid/pgid), iterate `task.repos` directly — repo set is intact.
3. `_poll_ram` — per-task RAM sum across all repos (D-10/D-14):
   - For each ACTIVE task: `total = sum(group_rss_kb(t.pgid) for r in task.repos for t in r.terminals if t.pgid is not None)`, writing each `t.rss_kb` back. Update the row subline to `f"claude · {format_ram_kb(total or None)}"`.
   - `_update_footer`: `n = caps.active_count(self._store.all())` (active TASKS); aggregate total = sum of every active task's every repo's every terminal `rss_kb`.
4. Cap-over-tasks (D-14): `_on_new_worktree_clicked` already calls `caps.at_cap(self._store.all())` — confirm it now counts Tasks (Plan 01 made `caps` task-generic). `_prompt_hibernate_then` lists `task.branch` for active tasks and hibernates the chosen TASK (all its repos).
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read()); print('parse ok')" && /tmp/arduis-venv-ab12/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/arduis/window.py` contains `def _build_task_workspace` (shared by `_create_task` and `_on_resume`)
    - `_on_resume` calls `default_repo_terminals(` and re-spawns per repo (`grep -n "default_repo_terminals" src/arduis/window.py` appears in the resume path)
    - `_poll_ram` sums across repos: `grep -n "for r in.*repos.*for t in" src/arduis/window.py` appears in `_poll_ram`
    - `_on_hibernate` discards the layout via `self._layouts.pop(task.task_id` and drops widgets by the `{task}:` prefix
    - `_update_footer` uses `caps.active_count(` over `self._store.all()` (active tasks)
    - `window.py` parses; full GTK-free pytest suite exits 0
  </acceptance_criteria>
  <done>Hibernate kills every repo's groups + discards layout; resume rebuilds the default one-column-per-repo layout and re-spawns; RAM/cap/footer operate per task across all repos.</done>
</task>

<task type="auto">
  <name>Task 2: Startup task scan (disk = source of truth) + "close a repository" action (never delete)</name>
  <read_first>
    - src/arduis/window.py §`__init__` (the resolution + `_open_shell_leaf` + RAM-poll seeding), `_rebuild_sidebar`, `_make_row_menu_cb` (row context menu), `_close_terminal` (existing per-terminal kill)
    - src/arduis/task_layout.py (task_dir_for to locate ../<root>-tasks/), src/arduis/project.py
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-CONTEXT.md (D-10 close-repo never-delete, D-11 startup scan)
    - .planning/phases/03.2-projects-and-cross-repo-tasks/03.2-RESEARCH.md §"Runtime State Inventory" + §"Common Pitfalls" (Pitfall 6 — scan must NOT spawn)
  </read_first>
  <action>
1. Startup scan `_scan_tasks()` (D-11, called from `__init__` AFTER project resolution and BEFORE seeding the RAM poll, but it must NOT spawn any terminal — Pitfall 6):
   - Compute the tasks root: `tasks_root = os.path.dirname(task_dir_for(self._project_root, "x"))` — i.e. `../<root_base>-tasks` (or build `<parent>/<base>-tasks` directly).
   - If `tasks_root` doesn't exist, no tasks — return.
   - For each direct subdir `d` of `tasks_root`: it is a VALID task iff it contains ≥1 child dir whose `.git` is a FILE (a real worktree pointer) — implement `_dir_is_task(d)` scanning `d`'s children for `os.path.isfile(os.path.join(child, ".git"))` (RESEARCH A5). Symlink-only / no-worktree dirs are skipped.
   - For each valid task dir: build a `Task(task_id=<dirname>, branch=<dirname>, task_dir=d, repos=[...], state=HIBERNATED)` where each `RepoCheckout` is inferred from the child dirs that ARE worktrees: `RepoCheckout(repo_name=child, worktree_dir=os.path.join(d, child), branch=<dirname>, terminals=default_repo_terminals(<dirname>, child))` BUT with every terminal's pid/pgid left None (HIBERNATED — Pitfall 6: do NOT spawn). Add to the store.
   - DO NOT swap workspace / spawn — rows render dimmed; resume (Task 1) spawns on demand.
2. "Close a repository" action (D-10 — kill processes, NEVER delete from disk):
   - Add a `win.close_repo` Gio.SimpleAction (alongside hibernate/resume in `_install_row_actions`).
   - Decide the UI surface (Discretion): add a "Fechar repositório" entry to the row context menu for ACTIVE tasks that have >1 repo, OR a per-repo control. Recommended: a submenu/secondary action on the task row listing each repo; selecting one kills THAT repo's terminal groups only.
   - On invoke: for the chosen `RepoCheckout`, `for t in repo.terminals: if t.pid: self._teardown_pgid(t.pid)`; then clear that repo's terminals' pid/pgid and remove its leaves from the active layout (`model.close_leaf(tid)` per terminal id) + widget maps; reflect. The RepoCheckout stays in `task.repos` (directory kept on disk — D-10); only its processes die. If closing leaves the task with zero live terminals, fall back to `_swap_workspace(_MAIN_SID)`.
   - Explicitly DO NOT call `os.remove`/`shutil.rmtree`/`git worktree remove` anywhere — arduis never deletes (D-10). Add a comment asserting this.
  </action>
  <verify>
    <automated>/tmp/arduis-venv-ab12/bin/python -c "import ast; ast.parse(open('src/arduis/window.py').read()); print('parse ok')"; test "$(grep -c 'shutil.rmtree\|os.remove\|worktree.*remove' src/arduis/window.py)" = 0 && /tmp/arduis-venv-ab12/bin/python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src/arduis/window.py` contains `def _scan_tasks` and `def _dir_is_task` (or equivalent worktree-pointer validation using `os.path.isfile(...".git")`)
    - `_scan_tasks` creates tasks in `SessionState.HIBERNATED` and does NOT spawn (`grep -n "spawn_async\|_spawn_into" ` does not appear inside `_scan_tasks`)
    - `__init__` calls `_scan_tasks()` after project resolution
    - A `win.close_repo` action exists (`grep -n "close_repo" src/arduis/window.py` non-empty) and kills only the chosen repo's groups
    - NO disk-deletion calls: `grep -c "shutil.rmtree\|os.remove\|os.rmdir\|worktree.*remove" src/arduis/window.py` == 0 (D-10)
    - `window.py` parses; full GTK-free pytest suite exits 0
  </acceptance_criteria>
  <done>Startup rediscovers past tasks from `../<root>-tasks/` as HIBERNATED (no spawn); the UI can close a repo (kill its groups) without ever deleting from disk.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual GTK acceptance — full multi-repo task lifecycle (criteria 1-5, no orphans)</name>
  <action>Human-verify checkpoint (no code change): run the live GTK acceptance checklist in how-to-verify against a real multi-repo project. Those steps ARE the procedure.</action>
  <what-built>
The complete Phase 03.2 pivot: project = multi-repo root (topbar name), task = cross-repo worktree set (sidebar), per-repo terminals, per-task hibernate/RAM/cap, startup scan, close-repo, no-orphan teardown. All GTK-free logic is unit-tested (Plans 01-03); this verifies the live GTK behavior that cannot be asserted headlessly with confidence.
  </what-built>
  <how-to-verify>
Prep a real multi-repo project (or use the user's `livon/`): a root dir with ≥2 subdirs each a git repo with ≥1 commit, plus a root file (e.g. `CLAUDE.md`) and a non-repo dir.

1. **Launch + topbar (criterion 1):** `cd <root> && ./run.sh`. Confirm the topbar shows the PROJECT NAME (the root dir basename), the sidebar has the pinned `<project> · zsh` row, and the pinned terminal opened at the PROJECT ROOT (`pwd` in it == root).
2. **New task multi-repo (criterion 2):** click +, pick 2 repos, type a NEW branch `feat-x`, Criar. Confirm:
   - `ls ../<root>-tasks/feat-x/` shows BOTH repos as real worktree dirs + symlinks for the root file and non-repo dir (`ls -la` shows `->` arrows for the symlinks, and they are RELATIVE).
   - `cat ../<root>-tasks/feat-x/CLAUDE.md` reads the root file through the symlink.
   - `git -C ../<root>-tasks/feat-x/<repo> branch --show-current` == `feat-x` in each chosen repo.
3. **Workspace layout (criterion 3, D-01/D-02):** the task workspace shows 4 terminals: one column per repo, agent (claude) on top, shell (zsh) below. Confirm each agent column launched `claude` and each shell is a plain zsh in that repo's worktree dir.
4. **Switch + keys (criterion 3):** create a second task; C-Space n/p switches between tasks; C-Space h/j/k/l moves focus between the visible task's terminals.
5. **Per-task RAM (RAM-03):** each task row shows a RAM figure that is the SUM across its repos (rises with more repos/agents); the footer shows `N agentes ativos` counting TASKS, not repos.
6. **Cap (RAM-02/D-14):** with the cap at default 6, opening tasks past the cap prompts to hibernate a task first (counting tasks, not repos).
7. **Hibernate no-orphans (criterion 4):** note the agent PIDs (`ps -ef | grep claude`), hibernate the task. Confirm: all that task's claude+zsh across BOTH repos are gone (`ps` shows none), the directories REMAIN on disk, the row greys out. Resume → the default one-column-per-repo layout rebuilds and agents re-spawn.
8. **Close a repository (D-10):** in an active 2-repo task, close ONE repo. Confirm that repo's terminals die, the OTHER repo's keep running, and NOTHING is deleted from disk (`ls ../<root>-tasks/feat-x/` still shows both repo dirs).
9. **Startup scan (D-11):** quit arduis, relaunch in the root. Confirm the previously-created tasks appear in the sidebar as HIBERNATED (dimmed), NO claude processes spawned at startup (`ps -ef | grep claude` empty until you resume one).
10. **Degenerate 1-repo (criterion 5):** `cd` into a single git repo with no member subdirs, launch arduis. Confirm it behaves exactly like 03.1 (one column = agent+shell, task = the one repo), with no errors.
11. **No-orphan on close (criterion 4):** with ≥1 active task, close the arduis window. Confirm `ps -ef | grep -E "claude|zsh"` shows no orphaned children from arduis.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failure (which step, observed vs expected) so a gap-closure plan can target it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk dir names under `../<root>-tasks/` → Task model at startup | scanned dir/worktree names become task/repo identifiers |
| process groups (pgid) → `os.killpg` | teardown signals must target only arduis's own children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-10 | DoS / resource (orphan leak) | `_on_hibernate` / `_on_close_request` not killing every repo's groups | mitigate | `_teardown_session_terminals` iterates every repo's every terminal; `hibernate_fields` clears every pgid; UAT step 7/11 verifies `ps` shows no orphans. |
| T-03.2-11 | Resource exhaustion | startup scan spawning every past task's agents | mitigate | `_scan_tasks` creates tasks HIBERNATED with pid/pgid None and never spawns (Pitfall 6); UAT step 9 verifies no claude at startup. |
| T-03.2-12 | Tampering / data loss | accidental disk deletion in close-repo | mitigate | close-repo kills processes only; NO `os.remove`/`rmtree`/`git worktree remove` anywhere (D-10); verify grep == 0 + UAT step 8. |
| T-03.2-13 | Spoofing | a stray symlink-only dir under `<root>-tasks/` listed as a task | mitigate | `_dir_is_task` requires ≥1 child with a `.git` FILE (real worktree pointer); symlink-only dirs are skipped (A5). |
</threat_model>

<verification>
- `window.py` parses; full GTK-free suite green: `/tmp/arduis-venv-ab12/bin/python -m pytest -q` exits 0.
- No disk deletion: `grep -c "shutil.rmtree\\|os.remove\\|os.rmdir\\|worktree.*remove" src/arduis/window.py` == 0.
- Scan does not spawn: `_spawn_into`/`spawn_async` not present in `_scan_tasks` body.
- Manual UAT checklist (Task 3) approved — criteria 1-5 + no-orphans on hibernate/close confirmed live.
</verification>

<success_criteria>
- Hibernate/resume/RAM/cap operate per task across all repos; resume rebuilds the default one-column-per-repo layout.
- Startup rediscovers tasks from `../<root>-tasks/` as HIBERNATED with no spawn.
- Close-a-repository kills only that repo's groups and never deletes from disk.
- No orphan processes after hibernate or window close (UAT-verified).
- Degenerate 1-repo project preserves 03.1 behavior (UAT-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-projects-and-cross-repo-tasks/03.2-03-SUMMARY.md` documenting: the per-task hibernate/resume/RAM/cap re-target, the `_build_task_workspace` shared helper, the startup scan + `_dir_is_task` validation, the close-repo action, and the manual UAT result (which criteria passed; any deferred gaps).
</output>
