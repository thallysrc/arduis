---
phase: 03.3-topbar-repo-chips
plan: 03
type: execute
status: complete
wave: 3
---

# Plan 03.3-03 Summary: Acceptance

## Outcome

Acceptance executed in autonomous mode (PO engaged but delegated execution). Task 1 (headless
smoke) ran for real and is committed at `tests/smoke/test_topbar_chips_smoke.py`; Task 2 (live
human-verify) auto-resolved by persisting the checklist to `03.3-HUMAN-UAT.md`.

## Task 1 — Headless broadway smoke (DONE)

- Full suite: `336 passed`.
- Smoke (`tests/smoke/test_topbar_chips_smoke.py`, broadwayd :97, sandbox HOME, killed in finally):
  **8/8 PASS**.

| Check | Result |
|-------|--------|
| (a) clean multi-repo: members == [backend, frontend, keycloak], sorted | PASS |
| (a) chip bar renders 3 chips | PASS |
| (a) all selected by default (D-02) | PASS |
| (a) toggle off updates default_selection (D-02) | PASS |
| (a) reflect_active highlights then clears (D-03) | PASS |
| **(b) Livon-Saude shape: 2 repos not 22 — THE gate (D-04)** | PASS |
| (b) chip bar renders 2 chips, no overflow | PASS |
| (c) degenerate single-repo: 1 chip, no crash (criterion 5) | PASS |

Fixture (b) builds 2 real `.git`-DIR repos + 20 real linked worktrees (`git worktree add` → `.git`
FILE) and asserts the topbar shows exactly 2 chips — directly proving the user's complaint is fixed.

## Task 2 — Live acceptance (persisted as UAT)

`03.3-HUMAN-UAT.md` (status: partial), 6 items. The gate is item 2 (Livon-Saude → 2 chips not 22),
headless-proven, awaiting live confirmation on the real root.

## Notes

- This phase SUPERSEDES 03.2 D-06 (topbar = name only) and reverses 03.2's Pitfall-1 (linked
  worktrees no longer count as member repos — D-04), both recorded as intentional.
- Header `pack_end`/title region deliberately left free for Phase 7 / 07-04 container badges.

## Self-Check: PASSED

Suite green (336), smoke 8/8, no orphans, smoke file + UAT + SUMMARY written.
