---
phase: 03.4-topbar-multi-project-switcher-corrective
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/arduis/window.py
autonomous: true
requirements: [PROJ-01]
user_setup: []

must_haves:
  truths:
    - "window.py reads registry.active() instead of the _project_root/_member_repos/_store singletons"
    - "The topbar renders one tab button per open project (basename label) plus a '+ Abrir projeto' button"
    - "On launch the cwd project auto-registers and is selected; remembered projects load from projects.json"
    - "Clicking '+ Abrir projeto' opens a folder picker, registers the chosen root, persists, and switches to it"
    - "Switching projects swaps sidebar+workspace WITHOUT tearing down the previous project's terminals (both alive)"
    - "Switching projects re-grabs focus on the swapped-in terminal"
  artifacts:
    - path: "src/arduis/window.py"
      provides: "ProjectRegistry-backed window: project tabs, FileDialog open, switch, per-project retained maps"
      contains: "ProjectRegistry"
  key_links:
    - from: "window.py topbar tabs"
      to: "registry.set_active + workspace swap"
      via: "win.switch_project action / ToggleButton"
      pattern: "switch_project"
    - from: "window.py startup"
      to: "projects_store.load_projects + ensure_project"
      via: "seed registry + select last_active or cwd"
      pattern: "load_projects"
    - from: "window.py '+ Abrir projeto'"
      to: "Gtk.FileDialog.select_folder"
      via: "folder picker → register → persist → switch"
      pattern: "select_folder"
---

<objective>
The structural lift: generalize window.py's single-project singletons onto a `ProjectRegistry`, render
the topbar as switchable PROJECT tabs + a "+ Abrir projeto" folder picker, wire `projects.json`
persistence + launch auto-register (D-07), and make project switching "both alive" (D-08) by retaining
each background project's GTK leaf/terminal/layout maps and only re-reflecting the active project's tree.

This is the largest plan. It does NOT touch teardown/remove/cap-union wiring (that is Plan 04) — switching
never tears anything down here. Leaving teardown for Plan 04 keeps this plan's window.py edit bounded and
leaves `main` launchable (multi-project switch works; remove + exit-all-teardown land next).

Purpose: deliver SC-1 (tabs swap sidebar+workspace), SC-2 (open + persist across restart), SC-3 (both
alive on switch), SC-4 (launch auto-registers cwd), SC-6 (real Project model encapsulates state).
Output: a registry-backed ArduisWindow with the project switcher.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-CONTEXT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-RESEARCH.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-UI-SPEC.md

<interfaces>
<!-- Plan 01 GTK-free spine this plan consumes. -->
From src/arduis/project.py:
```python
@dataclass
class Project:
    root: str; member_repos: list[str]; store: SessionStore
    last_active_task: str | None; compose_path: str | None; container_state: dict
    @property
    def name(self) -> str: ...          # basename(root)
class ProjectRegistry:
    def add(self, p): ...; def get(self, root): ...; def all(self): ...
    def active(self): ...; def set_active(self, root): ...; def remove(self, root): ...
def ensure_project(registry, root, member_repos) -> Project: ...
def project_term_id(project_root, term_id) -> str: ...   # A3 status-file namespacing
```
From src/arduis/projects_store.py:
```python
def save_projects(path, roots, last_active) -> None: ...
def load_projects(path) -> tuple[list[str], str | None]: ...
```
From src/arduis/project.py (D-12, unchanged): `detect_member_repos(root) -> list[str]`

<!-- Existing window.py funnels to REUSE (do not reinvent). -->
- `_swap_workspace(sid)` (~1748): unparents all mapped leaves, re-hangs the active subset, grabs focus.
  VTE survives unparent (proven, _reflect_layout ~3811). The PROJECT switch is this same pattern one level up.
- `_resolve_project()` (~2117): resolves cwd → root/member_repos (D-05/D-07 degenerate case + git rev-parse).
- `_rebuild_sidebar()` rebuilds the sidebar from the active store.
- `_chip_bar` Box at `header.pack_start` (~430-441): the physical slot to reuse for project tabs (UI-SPEC).
- `_MAX_VISIBLE_CHIPS` (~119): reuse for tab overflow (+N MenuButton, D-03).

<!-- FileDialog (GTK 4.10+, host 4.14.5 — verified). -->
```python
dialog = Gtk.FileDialog(); dialog.set_title("Abrir projeto")
dialog.select_folder(self, None, self._on_project_folder_chosen)
# in callback: folder = dialog.select_folder_finish(result)  # raises GLib.Error on cancel
# root = folder.get_path()
```

<!-- XDG path (window.py caller only): GLib.get_user_config_dir() + "arduis/projects.json" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the registry; route window.py's active-state accessors + per-project widget maps through registry.active()</name>
  <read_first>
    - src/arduis/window.py lines 270-380 (singleton fields + all per-project GTK maps in __init__)
    - src/arduis/window.py lines 2117-2196 (_resolve_project + _member_repo_path)
    - src/arduis/window.py lines 1748-1834 (_swap_workspace + _show_hibernated_placeholder)
    - src/arduis/window.py lines 3811-3900 (_reflect_layout — VTE-survives-unparent funnel)
    - src/arduis/project.py (Project/ProjectRegistry/ensure_project/project_term_id from Plan 01)
    - src/arduis/projects_store.py (load_projects/save_projects from Plan 01)
  </read_first>
  <action>
    Per RESEARCH Pattern 2 variant (A): give the window a `self._registry = ProjectRegistry()` and move
    the per-project state so each `Project` owns it, with window.py reading the active project.
    1. In __init__: add `self._registry = ProjectRegistry()`. Keep the GTK widget MAPS
       (`_leaf_by_sid`, `_term_by_sid`, `_layouts`, `_active_workspace_sid`, `_dot_by_sid`,
       `_pane_dot_by_tid`, `_notif_by_tid`, `_subline_by_sid`, `_record_by_state_file`, `_calm_since`,
       `_activity_ts`, `_compose_busy`, `_review_cache`/`_pr_busy`) but PARTITION them per project: store
       them as plain dict attributes on each `Project` (e.g. `proj.leaf_by_sid`) populated by window.py,
       OR keep a `self._maps_by_root` dict and a helper `self._m()` returning the active project's map set.
       Pick ONE shape and apply consistently (RESEARCH recommends moving onto Project as untyped dicts so
       project.py stays gi-free — window.py is the only module that puts Gtk/Vte VALUES in them).
    2. Add accessor properties/helpers so existing call sites read the active project:
       - `self._store` → `self._registry.active().store` (add a `_store` property returning it, or
         replace the ~40 `self._store` references; a read-only `@property _store` is the lowest-churn path).
       - `self._project_root`/`_project_name`/`_member_repos`/`_compose_path`/`_container_state` →
         properties delegating to `self._registry.active()` (return safe defaults when active() is None,
         preserving the "no project" guards that already exist).
       Keep `_repo_root`/`_repo_name` as aliases of the active project root/name (the pinned-leaf code).
    3. `_reflect_layout` and `_swap_workspace`: iterate ONLY the active project's leaves. When unparenting,
       unparent the active project's mapped leaves; background projects' leaves stay unparented and ALIVE
       (D-08) — never destroyed. (The existing unparent loop already does the right thing once the maps are
       per-project — verify the loop reads the active project's `leaf_by_sid`.)
    4. Status-file paths: wherever `attention.state_file_path(self._status_dir, term_id)` is called
       (~3549, ~3730, ~4266, ~4284), wrap the term_id with `project_term_id(active_root, term_id)` so two
       projects with same-named branches don't share a status file (A3). Apply at every call site
       consistently (the watcher's `_record_by_state_file` key must match what is written).
    Do NOT change teardown loops or the cap call yet (Plan 04). Keep the app launchable: with exactly one
    project (today's cwd), behavior is unchanged.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; grep -q "ProjectRegistry" src/arduis/window.py &amp;&amp; grep -q "project_term_id" src/arduis/window.py &amp;&amp; python -m pytest tests/test_window_conclude.py -q 2>&amp;1 | tail -4</automated>
  </verify>
  <done>window.py parses; references `ProjectRegistry` + `project_term_id`; the conclude regression test still passes (the bare-window stub still constructs); a single active project preserves prior behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire startup — load projects.json, auto-register cwd (D-07), seed registry, select active</name>
  <read_first>
    - src/arduis/window.py lines 500-525 (startup call order: _install_row_actions / _resolve_project / _scan_tasks)
    - src/arduis/window.py lines 2117-2180 (_resolve_project as written in Task 1)
    - src/arduis/window.py lines 2358-2460 (_scan_tasks — per-project disk scan)
    - src/arduis/projects_store.py (load_projects/save_projects)
    - src/arduis/project.py (ensure_project, detect_member_repos)
  </read_first>
  <action>
    Implement the launch sequence (RESEARCH Pitfall 7 ordering, D-05/D-06/D-07):
    1. Add `self._projects_json = os.path.join(GLib.get_user_config_dir(), "arduis", "projects.json")`.
    2. At startup (after `_install_row_actions`, before showing the canvas): call a new
       `self._init_projects()`:
       a. `roots, last = projects_store.load_projects(self._projects_json)` (skips missing roots, D-06).
       b. For each remembered root: `detect_member_repos(root)` → `ensure_project(self._registry, root, members)`.
          Compute that project's `compose_path` (root docker-compose.yml/compose.yaml) the same way
          `_resolve_project` did, and store on the Project. Run `_scan_tasks` for that project's store
          (the existing scan, parameterized to a given project/root instead of the singletons).
       c. Resolve the cwd project via the existing `_resolve_project` logic → get `(cwd_root, members)`;
          if `cwd_root` and not already registered, `ensure_project(...)` it (auto-register, D-07).
       d. Choose active: `last` if still a registered root, else the cwd project's root, else (no projects)
          leave active None. `self._registry.set_active(chosen)`.
       e. `projects_store.save_projects(self._projects_json, [p.root for p in registry.all()], chosen)`
          (persist the possibly-newly-added cwd root).
    3. Make `_scan_tasks` operate on a passed Project (or active project) rather than the singletons, so it
       can be called per remembered project at startup. Preserve its existing disk-scan logic verbatim.
    4. After seeding, drive the initial render through the existing path: build the sidebar from the active
       project's store and swap to its `last_active_task` or `_MAIN_SID`.
    Tolerate zero projects (cwd not a git root): active() None, "+ Abrir projeto" still shown, New-btn
    insensitive (existing guard).
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; grep -q "_init_projects\|load_projects" src/arduis/window.py &amp;&amp; grep -q "save_projects" src/arduis/window.py &amp;&amp; echo OK</automated>
  </verify>
  <done>window.py has `_init_projects` calling `load_projects`/`ensure_project`/`save_projects`; `_scan_tasks` runs per-project; startup tolerates 0 remembered + auto-registers cwd (D-07).</done>
</task>

<task type="auto" smoke="true">
  <name>Task 3: Render the project-tab switcher + "+ Abrir projeto" picker + switch action; headless smoke for tab render + both-alive</name>
  <read_first>
    - src/arduis/window.py lines 410-445 (header build, _new_btn, the _chip_bar slot at pack_start)
    - src/arduis/window.py lines 1748-1775 (_swap_workspace — the switch reuses this for the active task)
    - src/arduis/window.py lines 3944-3975 (_install_row_actions — register win.switch_project here)
    - src/arduis/window.py _build_css region lines 172-197 (.arduis-chip / .arduis-chip-active rules to reuse)
    - .planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-UI-SPEC.md (tab visual/interaction/copy contract)
    - tests/smoke/test_topbar_chips_smoke.py (broadway harness pattern to adapt)
  </read_first>
  <action>
    Render the switcher per UI-SPEC, reusing the `_chip_bar` Box slot at `header.pack_start`:
    1. Add `_build_project_tabs()`: clear the tab Box; for each `registry.all()` build a `Gtk.ToggleButton`
       (`.flat`, child a single `Gtk.Label(set_text=project.name)` — NEVER set_markup, T-03-09; tooltip =
       full root path). Group them via `set_group` (linked toggle group → GTK enforces exactly-one-active).
       The active project's button: `.add_css_class("arduis-chip-active")` + semibold (600) label (reuse the
       existing `.arduis-chip-active` rule body — accent border + surface fill; do NOT inline hexes,
       UI-SPEC Color). No status dot on tabs (label-only — deferred).
    2. Overflow: if `len(all()) > _MAX_VISIBLE_CHIPS`, render the first N as tabs and fold the rest into a
       `+N` `Gtk.MenuButton` (tooltip "Mais projetos") whose `Gio.Menu` items target `win.switch_project(root)`
       — reuse the 03.3 overflow shape (RESEARCH note).
    3. Add a trailing `Gtk.Button` "+ Abrir projeto" (`.flat`, tooltip "Abrir projeto") that triggers
       `Gtk.FileDialog().select_folder(self, None, cb)`; in `cb` call `select_folder_finish` (return on
       GLib.Error = cancel), get `folder.get_path()`, then `self._open_project(root)`.
    4. `_open_project(root)`: `detect_member_repos(root)` → `ensure_project(registry, root, members)` →
       compute compose_path → `_scan_tasks` for it → `save_projects(...)` → `set_active(root)` → switch.
       (Picker validation that the dir is a plausible project root is Claude's discretion per CONTEXT;
       if added, surface the UI-SPEC toast copy. If skipped, a no-git dir registers as degenerate — fine.)
    5. `_switch_project(root)` (and a `win.switch_project(root)` `Gio.SimpleAction`, "s" variant, registered
       in `_install_row_actions`): `registry.set_active(root)`; rebuild the sidebar from the new active
       project's store; `_swap_workspace(active.last_active_task or _MAIN_SID)` (which re-grabs focus,
       Pitfall 2); `_build_project_tabs()` to restyle; persist `last_active_project` via `save_projects`.
       CRITICAL (D-08): the switch NEVER calls any teardown — the previous project's leaf/term maps stay
       alive and unparented. Remember the outgoing project's current `_active_workspace_sid` into its
       `last_active_task` before switching so switch-back restores the same workspace.
    6. Tab basename labels via `set_text` only (T-03-09).
    Write a headless-broadway smoke `tests/smoke/test_project_switch_smoke.py` (adapt the 03.3 harness:
    sandbox HOME, /tmp multi-repo git fixtures for TWO project roots, broadwayd on a free display, SKIP if
    absent). Assert WITHOUT manual interaction:
      (a) constructing the window with two remembered roots renders exactly two project tabs + a
          "+ Abrir projeto" button (D-03);
      (b) after `win._switch_project(rootB)`, project A's `leaf_by_sid`/`term_by_sid` entries still exist
          and reference the same widget objects (NOT recreated/destroyed) — proving "both alive" (D-08);
      (c) exactly one tab carries `arduis-chip-active` at a time.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; grep -q "_build_project_tabs\|select_folder\|switch_project" src/arduis/window.py &amp;&amp; GDK_BACKEND=broadway python tests/smoke/test_project_switch_smoke.py 2>&amp;1 | tail -8</automated>
  </verify>
  <done>Project tabs render one per project + "+ Abrir projeto"; switch re-points the store, swaps workspace, restyles, persists; the smoke harness prints PASS for tab-count, both-alive (A's terminals survive switch), and single-active styling (or SMOKE_SKIP if broadwayd absent).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → Gtk.FileDialog | user picks an arbitrary host folder that arduis will scan + later spawn shells/compose in |
| project root/basename → GTK labels + git/scan argv | filesystem-derived strings reach UI labels and command argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-07 | Tampering | project tab labels | mitigate | `Gtk.Label.set_text(project.name)` only — never `set_markup` on a filesystem-derived basename (T-03-09 / V5 label-injection guard) |
| T-03.4-08 | Tampering | detect_member_repos / _scan_tasks on a chosen root | mitigate | scanned dir names only ever reach git argv as discrete `-C <path>` list elements via HostRunner.wrap_argv (existing T-03.2-04 discipline); no shell strings |
| T-03.4-09 | Spoofing/Elevation | "+ Abrir projeto" arbitrary dir | accept | same trust model as today's launch dir — the user chose it; arduis only reads git/scan + spawns the user's own shell there (RESEARCH security domain (b)) |
| T-03.4-10 | (resource leak) | switch retains background terminals (D-08) | accept | intentional RAM tradeoff bounded by the global cap (D-09, wired Plan 04) + per-task hibernate; PO-decided |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('src/arduis/window.py').read())"` → parses
- `grep "ProjectRegistry\|_build_project_tabs\|select_folder\|switch_project\|project_term_id" src/arduis/window.py` → all present
- `python -m pytest -q` → full suite green (conclude regression intact)
- `GDK_BACKEND=broadway python tests/smoke/test_project_switch_smoke.py` → PASS (tabs, both-alive, single-active) or SMOKE_SKIP
- Manual (deferred to Plan 05 UAT): launch in a repo → its tab appears active; open a 2nd project → both tabs; switch back → A's agent still running
</verification>

<success_criteria>
- SC-1: project tabs swap sidebar + workspace on click (D-03)
- SC-2: "+ Abrir projeto" registers + persists a root (loaded next launch — verified Plan 05 UAT)
- SC-3/D-08: switching keeps the other project's terminals alive (smoke asserts widget identity survives)
- SC-4/D-07: launch auto-registers the cwd project and selects it
- SC-6/D-01: window.py reads registry.active() — singletons replaced by the Project model
- A3: status-file paths namespaced per project
- `main` launchable; teardown/remove/cap-union deferred to Plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-03-SUMMARY.md`
</output>
