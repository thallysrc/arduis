---
status: partial
phase: 03.4-topbar-multi-project-switcher-corrective
source: [03.4-05-PLAN.md]
started: 2026-06-15
updated: 2026-06-15
---

## Current Test

[awaiting human testing on the real machine — native launch, real multi-repo roots, live agents/containers]

## Tests

Launch arduis natively (`./run.sh`) from inside a real multi-repo root (e.g. `~/Livon-Saude`).

### 1. SC-4 / D-07 — launch auto-register
expected: topbar shows ONE active tab = launch project's basename; New button enabled; sidebar shows that project's tasks.
result: [pending]

### 2. SC-5 / D-12 — repos live in the New-task dialog, not the topbar
expected: "Nova task" → per-repo checkboxes list this project's member repos (all checked by default); topbar has NO repo chips.
result: [pending]

### 3. SC-2 — open + persist across restart
expected: "+ Abrir projeto" → pick a SECOND multi-repo root (e.g. KarveLabs) → second tab appears + becomes active. Fully QUIT and relaunch → BOTH projects still listed.
result: [pending]

### 4. SC-1 — switching a tab swaps sidebar + workspace
expected: clicking each project tab swaps both the sidebar (its tasks) and the workspace; typing goes to the swapped-in terminal.
result: [pending]

### 5. SC-3 / D-08 — "both alive"
expected: start an agent on a task in project A; switch to B; `pgrep -af claude` shows A's agent STILL running; switch back to A → same live session (no respawn).
result: [pending]

### 6. D-10 — remove tears down but never deletes worktrees
expected: right-click project B's tab → Remover. With a live task it shows the destructive confirm "Remover {nome} e encerrar suas tasks?"; confirm → B's agents/containers die (`pgrep`/`docker ps`) but B's worktree/task DIRS still exist on disk (`ls`). A no-live-task project removes silently.
result: [pending]

### 7. D-11 — no orphans on exit
expected: with 2 projects open + live agents in each, close the arduis window → `pgrep -af "claude\|zsh"` (arduis-spawned pgids) shows ZERO orphans; `docker ps` shows no leftover arduis task stacks.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps
