---
phase: 03.4-topbar-multi-project-switcher-corrective
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - src/arduis/window.py
  - tests/test_window_projects.py
autonomous: true
requirements: [PROJ-01]
user_setup: []

must_haves:
  truths:
    - "The global active cap counts the union of every open project's tasks before allowing a new task"
    - "Removing a project with live tasks shows a destructive confirm, then tears down its tasks (kill pgids + compose down -v), never deleting disk dirs"
    - "Removing a project with no live tasks drops it from the list + projects.json silently"
    - "App-exit tears down EVERY open project's tasks + containers (no orphans across projects)"
    - "App-exit issues a SEPARATE compose down per project using that project's OWN project_name/compose_path (not the active project's for all)"
  artifacts:
    - path: "src/arduis/window.py"
      provides: "cap-union gate, per-project remove teardown (D-10), all-projects exit teardown (D-11)"
      contains: "registry.all()"
    - path: "tests/test_window_projects.py"
      provides: "headless regression: cap-union gate, remove-teardown order, exit-all-projects loop with per-project compose argv"
      min_lines: 60
  key_links:
    - from: "window.py new-task cap gate"
      to: "caps.at_cap(union across registry.all())"
      via: "flat comprehension"
      pattern: "for p in self._registry.all"
    - from: "window.py _on_close_request"
      to: "every project's store + container_state"
      via: "outer loop over registry.all()"
      pattern: "registry.all"
    - from: "window.py remove_project"
      to: "_teardown_session_terminals + compose.down_argv"
      via: "per-task teardown then registry.remove + save_projects"
      pattern: "remove_project"
---

<objective>
Close the lifecycle correctness gaps the switcher opens: the three concrete bugs RESEARCH found at real
call sites, fixed at the right scope.
1. D-09 cap-union: the new-task gate must count agents across ALL projects, not just the active store.
2. D-10 per-project remove: a destructive confirm (when live tasks exist) → tear down its tasks (kill
   pgids + `compose down -v`) → drop from the registry + projects.json. NEVER delete disk dirs.
3. D-11 app-exit: iterate `registry.all()` so EVERY project's agents + containers are torn down (no
   cross-project orphans), each with its OWN compose project_name/compose_path.

This plan finishes the window.py work begun in Plan 03 (file-ownership → sequential wave) and EXTENDS
`tests/test_window_projects.py` (created in Plan 03 Task 2 with the D-07 auto-register case). After it,
`main` is fully correct: switch + open + persist + both-alive (Plan 03) AND cap + remove + exit teardown
(this plan).

Purpose: SC (D-09/D-10/D-11) — the "teardown garantido / RAM-first" guarantees, extended across projects.
Output: cap-union gate, a remove-project affordance + teardown, all-projects exit teardown, headless
regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-CONTEXT.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-RESEARCH.md
@.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-UI-SPEC.md

<interfaces>
<!-- Existing teardown machinery to REUSE verbatim (RESEARCH Don't Hand-Roll). -->
From window.py:
```python
def _teardown_session_terminals(self, task: Task) -> None   # ~4243: SIGHUP→SIGKILL every term pgid + clear state files
def _clear_task_state_files(self, task) -> None             # state-file cleanup
def _isolation_available(self) -> bool                       # ~1454: docker + compose present
# app-exit container loop pattern (~4320): per task, sync subprocess.run(compose.down_argv(project, task_dir), timeout=10)
```
From src/arduis/compose.py:
```python
def down_argv(project_name, task_dir) -> list[str]   # docker compose -p <name> -f ... down -v (argv LIST)
def sanitize_project_name(branch) -> str
```
From src/arduis/caps.py (unchanged — feed it the union):
```python
def at_cap(sessions, cap=ACTIVE_CAP_DEFAULT) -> bool
def active_count(sessions) -> int
```
From src/arduis/project.py:
```python
class ProjectRegistry: def all(self): ...; def remove(self, root): ...; def active(self): ...
# each Project carries its OWN compose_path + container_state — use the project's, not the active one's
```

<!-- Current single-store call sites that must become union/all-projects (RESEARCH Pitfalls 3,4,5). -->
- new-task cap gate (~2470): `if caps.at_cap(self._store.all()):` → feed union across registry.all()
- _on_close_request (~4294-4337): iterates `self._store.all()` only → loop registry.all() outer
- UI-SPEC D-10 confirm dialog: Adw.AlertDialog, set_response_appearance("remove", DESTRUCTIVE),
  set_default_response("cancel"); copy in 03.4-UI-SPEC Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the new-task cap gate count the UNION across all projects (D-09)</name>
  <read_first>
    - src/arduis/window.py lines 2462-2535 (_on_new_worktree_clicked + _prompt_hibernate_then)
    - src/arduis/window.py lines 1835-1850 (_resume_gated cap gate — same union fix applies)
    - src/arduis/caps.py (at_cap / active_count — unchanged)
    - tests/test_caps.py (the union test from Plan 02 — the behavior to wire)
  </read_first>
  <action>
    Replace every `caps.at_cap(self._store.all())` / `caps.active_count(self._store.all())` with the union
    over all open projects (RESEARCH Pitfall 4, D-09):
    `union = [t for p in self._registry.all() for t in p.store.all()]`
    then `caps.at_cap(union)` / `caps.active_count(union)`. Apply at the new-task gate (~2470) AND the
    resume gate (~1837) AND inside `_prompt_hibernate_then` (the `active_count` heading and the active list
    it shows). For `_prompt_hibernate_then`: the chooser may still list only the ACTIVE project's active
    tasks (cross-project hibernation in the chooser is a deferred nicety per RESEARCH Open Q2), but the
    COUNT in the heading must be the global union count. Add a small helper
    `self._all_tasks()` returning the union list and use it everywhere.
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; ! grep -n "at_cap(self._store.all())" src/arduis/window.py &amp;&amp; grep -q "_all_tasks\|for p in self._registry.all" src/arduis/window.py &amp;&amp; echo OK</automated>
  </verify>
  <done>No `at_cap(self._store.all())` remains; a union helper feeds every cap/active_count call; the cap now spans projects (D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Add per-project remove with destructive-confirm-on-live-tasks teardown (D-10)</name>
  <read_first>
    - src/arduis/window.py lines 4243-4292 (_teardown_session_terminals + _clear_task_state_files)
    - src/arduis/window.py lines 1454-1470 (_isolation_available)
    - src/arduis/window.py lines 4313-4337 (the exit-time per-task compose-down pattern to mirror)
    - src/arduis/window.py lines 3270-3330 (the existing "Concluir task" Adw.AlertDialog DESTRUCTIVE pattern)
    - .planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-UI-SPEC.md (D-10 dialog copy + DESTRUCTIVE appearance)
    - src/arduis/compose.py (down_argv / sanitize_project_name)
  </read_first>
  <action>
    Add the remove affordance + teardown (D-10, UI-SPEC):
    1. Add a right-click context menu on a project tab (or a per-tab close affordance) → `win.remove_project(root)`
       `Gio.SimpleAction` ("s" variant) registered in `_install_row_actions` (mirror the existing row
       context-menu + action shape). UI-SPEC: removal is via tab context menu.
    2. `_remove_project(root)`:
       - Look up the Project; compute its live (ACTIVE) tasks `[t for t in proj.store.all() if t.state == ACTIVE]`.
       - If NO live tasks: drop silently — `registry.remove(root)`, rebuild tabs, and
         `save_projects(self._projects_json, [p.root for p in registry.all()], <active root>)`. If the
         removed project was active, pick a new active (first remaining, or None) and switch to it. No dialog.
       - If live tasks exist: present an `Adw.AlertDialog` with the UI-SPEC copy — heading
         `Remover {nome} e encerrar suas tasks?`, body about encerrar agentes/containers + worktrees preserved,
         responses `Cancelar` (default/close) and `Remover` with
         `set_response_appearance("remove", Adw.ResponseAppearance.DESTRUCTIVE)`. On "remove":
         for each live task run `_teardown_session_terminals(task)` + `_clear_task_state_files(task)`, and
         if `_isolation_available()` and the task's container_state is enabled, run a best-effort
         `compose.down_argv(<THIS project's project_name>, task.task_dir)` using the REMOVED project's own
         compose identity (never the active project's). THEN `registry.remove(root)` + rebuild tabs +
         `save_projects(...)` + reselect active if needed.
       - NEVER delete worktrees/task dirs on disk (D-10 hard rule — arduis never deletes). The teardown is
         kill pgids + compose down -v ONLY.
    3. Container teardown argv MUST be a list via `compose.down_argv` (never a shell string) so a crafted
       project/branch name can't inject (security).
  </action>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; grep -q "remove_project" src/arduis/window.py &amp;&amp; grep -q "DESTRUCTIVE" src/arduis/window.py &amp;&amp; echo OK</automated>
  </verify>
  <done>`win.remove_project` action + `_remove_project` exist; live-tasks path shows a DESTRUCTIVE Adw.AlertDialog and tears down (pgids + compose down -v) before dropping from registry + JSON; no-live-tasks path is silent; no disk-dir deletion anywhere in the method.</done>
</task>

<task type="auto" smoke="true">
  <name>Task 3: App-exit tears down ALL projects (D-11) + headless regression for cap-union, remove-teardown, exit-loop (per-project compose argv)</name>
  <read_first>
    - src/arduis/window.py lines 4294-4337 (_on_close_request — currently single-store)
    - src/arduis/window.py lines 4243-4292 (_teardown_session_terminals)
    - tests/test_window_projects.py (created Plan 03 Task 2 — extend it; reuse its bare-window stub + the D-07 case)
    - tests/test_window_conclude.py (the bare-window-stub regression harness pattern to reuse)
    - src/arduis/project.py (ProjectRegistry / Project — each carries its own compose_path/container_state)
    - src/arduis/compose.py (down_argv)
  </read_first>
  <action>
    1. `_on_close_request` (D-11, RESEARCH Pitfall 3): keep the RAM-poll + status-monitor cleanup + scratch
       shell teardown. Then wrap the per-task teardown in an OUTER loop over `self._registry.all()`:
       for each project, for each task in `proj.store.all()`: `_teardown_session_terminals(task)` +
       `_clear_task_state_files(task)`. Then the container-down loop also iterates all projects: for each
       project's enabled task, `subprocess.run(self._runner.wrap_argv(compose.down_argv(<THIS project's
       project_name>, task.task_dir)), timeout=10, check=False)` (the existing brief-sync-at-exit
       convention). Use EACH project's OWN `container_state`/`compose_path`/project_name — NOT the active
       project's for all. No project's processes survive (no orphans).
    2. EXTEND `tests/test_window_projects.py` (already created in Plan 03 Task 2; reuse its bare-window
       `__new__` + monkeypatch-stub pattern; no display). Add these cases:
       - test_cap_gate_counts_union: stub a window with a `_registry` holding two projects whose stores
         have 4 + 3 ACTIVE tasks; assert `_all_tasks()` length 7 and that the new-task gate path detects
         at_cap (the prompt-hibernate branch is taken) — pin Pitfall 4.
       - test_remove_with_live_tasks_tears_down_then_drops: stub registry with one project holding a live
         task; record-stub `_teardown_session_terminals` + container-down; drive the "remove" confirm
         response path; assert teardown was invoked for the task, `registry.remove` was called, and NO disk
         deletion call (there is none) — D-10.
       - test_close_request_tears_down_all_projects: stub registry with TWO projects, EACH with a DISTINCT
         compose project_name/compose_path and each holding one live (isolated/enabled) task. Monkeypatch
         (record) BOTH `_teardown_session_terminals` AND the compose-down channel — capture the argv passed
         to `compose.down_argv` (or the runner). Call `_on_close_request`. ASSERT:
           (a) `_teardown_session_terminals` invoked once per task across BOTH projects (2 calls, not 1);
           (b) the compose-down channel was called TWICE with TWO DISTINCT per-project identifiers
               (projectA's project_name/compose_path for A's task, projectB's for B's task) — a bug that
               reused the ACTIVE project's name for all would produce two identical identifiers and MUST
               fail this assertion (Pitfall 3 / D-11 per-project correctness).
       - test_remove_no_live_tasks_is_silent: project with only HIBERNATED tasks → `_remove_project` drops
         it without constructing a dialog (assert registry.remove called, no teardown invoked).
  </action>
  <acceptance_criteria>
    - `_on_close_request` loops `registry.all()` for both the killpg teardown and the container-down loop.
    - test_close_request_tears_down_all_projects asserts exactly 2 `_teardown_session_terminals` calls
      (one per project's task) AND exactly 2 distinct compose-down argv calls, each carrying ITS OWN
      project's compose identifier (project_name/compose_path) — never the active project's name for both.
    - All four cases in tests/test_window_projects.py pass alongside the Plan 03 D-07 case; conclude
      regression intact.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/thallysrc/Projects/arduis &amp;&amp; python -c "import ast; ast.parse(open('src/arduis/window.py').read())" &amp;&amp; python -m pytest tests/test_window_projects.py tests/test_window_conclude.py -q 2>&amp;1 | tail -6</automated>
  </verify>
  <done>`_on_close_request` loops `registry.all()` for both the killpg teardown and the container-down loop using each project's own compose identity; `tests/test_window_projects.py` passes all cases including the D-07 auto-register case (Plan 03), cap-union gate, remove-teardown-then-drop, the close-all-projects test asserting TWO distinct per-project compose-down calls (not the active project's name reused), and silent-no-live-remove; the conclude regression still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project/branch name → docker compose argv | per-project/app-exit teardown builds `down -v` argv from project + branch-derived names |
| registry remove → on-disk worktrees/task dirs | a remove must not be allowed to delete disk content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-11 | Tampering | compose down argv on remove/exit | mitigate | argv built as a LIST via `compose.down_argv` + `HostRunner.wrap_argv` — never a shell string, so a crafted project/branch name can't inject (CLAUDE.md / RESEARCH security (c)) |
| T-03.4-12 | Tampering | _remove_project disk side-effects | mitigate | D-10 hard rule enforced + tested: remove only kills pgids + `compose down -v` + drops the JSON/registry entry; NO `git worktree remove`, NO `rm`, NO dir delete (test_remove_with_live_tasks_tears_down_then_drops asserts no deletion) |
| T-03.4-13 | (resource leak / no-orphans) | _on_close_request | mitigate | D-11 outer loop over `registry.all()` for BOTH killpg + compose-down, each with the project's OWN compose identity; regression test asserts 2 distinct per-project teardown+compose calls (Pitfall 3); live UAT process-tree check in Plan 05 |
| T-03.4-14 | (resource leak) | global cap across projects | mitigate | D-09 union feed to `caps.at_cap` at every gate; test_cap_gate_counts_union pins it (Pitfall 4) |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('src/arduis/window.py').read())"` → parses
- `! grep "at_cap(self._store.all())" src/arduis/window.py` → cap-union applied
- `grep "remove_project\|registry.all" src/arduis/window.py` → remove + exit-loop present
- `python -m pytest tests/test_window_projects.py tests/test_window_conclude.py -q` → green (incl. the per-project compose-argv assertion)
- `python -m pytest -q` → full suite green
- Live UAT (Plan 05): close app with 2 projects → `pgrep`/`docker ps` show zero orphans (D-11); remove a live project → its agents/containers die, dirs survive (D-10)
</verification>

<success_criteria>
- D-09: new-task + resume cap gates count the union across all projects
- D-10: remove tears down live tasks (pgids + compose down -v) behind a DESTRUCTIVE confirm; silent when no live tasks; never deletes disk dirs
- D-11: app-exit tears down every project's tasks + containers (no cross-project orphans) using each project's OWN compose identity
- Headless regression locks all three (incl. per-project compose-argv correctness); conclude regression intact
- `main` fully correct end-to-end (pending live UAT in Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-topbar-multi-project-switcher-corrective/03.4-04-SUMMARY.md`
</output>
</content>
