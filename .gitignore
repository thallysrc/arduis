# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Flatpak build artifacts (deferred to v2 — kept ignored)
build-dir/
.flatpak-builder/
repo/
*.flatpak

# Mockup renders (gerados a partir do .html)
docs/mockup/*.png

# Claude Code agent worktrees (execução GSD)
.claude/worktrees/

# Debian / meson build artifacts (generated by dpkg-buildpackage)
debian/.debhelper/
debian/arduis/
debian/arduis.substvars
debian/debhelper-build-stamp
debian/files
obj-*-linux-gnu/

# Stamps do hook de testes (Claude Code Stop hook)
.claude/.pytest-green.stamp
.claude/.pytest-red.stamp
